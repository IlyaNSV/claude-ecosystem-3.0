# Changelog

All notable changes to Ecosystem 3.0 are documented in this file.

Format based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

---

## [Unreleased]

### Added

- **`/product:status` — детерминированный collector + ghost-check очередей (DEC-DEV-0217).** Новый `.claude/hooks/product/lib/status-collector.cjs` (dependency-free, read-only, tolerant, exit 0 — паттерн `handoff-staleness.cjs`): цензус артефактов со статусами + NFR-split, синглтон-метрики (RPM roles / DS tokens+components / BG term sections), handoffs со staleness-вердиктом через реюз G22-lib (всегда БЕЗ `--write`), pending-очереди с пометкой **ghost-записей** (entry ссылается на артефакт, отсутствующий на диске — осадок хук-записей от прогонов на изолированных ref/worktree), session-снапшот (+ `last_artifact_exists`), инвентарь DA-находок, stale drafts >14d, Integrator `tools_count` + PA-тоталы. Команда `status.md` переписана: все счётчики — только из collector'а (fallback «degraded manual mode» с пометкой approximate, без фабрикации collector-only полей); шаблон выровнен с фактическими форматами артефактов (RM без `stage`-поля, BG-термины без статусов, session без `process`/`step`, DA-находки в их родных шкалах severity); PENDING-тотал определён = queue entries (PA — отдельной строкой, не суммируется); секция RECENT DA FINDINGS и ghost-warning обязательны при ненулевых данных; нереалистичный перф-контракт «under 2 seconds» заменён честным (collector <2s, полный проход ~1-2 мин). Live-валидировано на слепке пилота против независимого эталона (2 recon-субагента + детерминированный цензус).

### Fixed

- **`bg-extractor.js`: фильтр зон переведён с блоклиста на allowlist источников (DEC-DEV-0219).** Запись перегенерированных `handoffs/*.md` порождала сотни мусорных BG-кандидатов из шаблонных лейблов (live: 223 за один прогон), а `.design-sessions/` ранее дал ghost-кандидатов — блоклист не знал этих директорий. Теперь извлечение идёт ТОЛЬКО из 13 типовых директорий артефактов + корневых синглтонов (кроме самого `glossary.md`); производные и рабочие директории отсекаются по построению. +5 юнит-тестов.

- **`status-collector.cjs`: дренированная очередь (`entries: []` / `candidates: []` / голый `[]`) больше не помечается `unparseable` (DEC-DEV-0217 addendum).** Ветка «0 распарсенных записей» не отличала валидно-пустую очередь от парс-фейла; именно пустые формы оставляет вычистка ghost-записей — первый же прогон на пилоте пометил `app-map-pending.yaml` как unparseable. Теперь пустые формы = 0 записей без note; настоящий мусор — по-прежнему `unparseable` + note «inspect manually». +1 юнит-тест (15/15).

- **`/integrator:add` Stage 2 propose теперь рендерит `environment_tiers` (пер-тир suitability + notes) и surface-ит обязательный prod-only warning при `local_dev.suitability: none` ПЕРЕД approve-гейтом (DEC-DEV-0214; SPEC §4.2.1 «Install integration»).** Раньше блок собирался Stage-1 профайлером, но propose его не показывал, а warning не был реализован нигде — вскрыто догоном E1/S4 кампании (greenfield `vercel@latest`); требование жило только в research-protocol. Ветки для `environment_agnostic: true` и stale-профиля без блока («re-profile», не фабриковать).
- **Deploy-гейт читал contract-status из КОПИИ в манифесте, а не из CNT-файла — live-флип контракта не доходил до гейта (DEC-DEV-0215; вскрыт E5-B).** После флипа `CNT-005 → active` гейт продолжал требовать `--accept-draft-contract` для уже доверенного контракта. `deploy-manifest.cjs` теперь резолвит status из CNT SSOT (`.claude/integrator/contracts/CNT-NNN.yaml`, primary по имени + secondary скан по `contract.id`); манифест — фолбэк с disclosure (отсутствует/нечитаем ⇒ «слеп ≠ нашёл», не крах); провенанс `contract_status_source: cnt|manifest|null`; существующие поля не переименованы (DEC-DEV-0012), фикстуры без CNT ⇒ поведение байт-в-байт прежнее.

---

## [1.12.0] — 2026-07-15

> **Minor: Epic E — deploy/rollback-контур live-валидирован на VM-пилоте.** Накоплено с 1.11.0: первый живой staging-деплой линии (scene-bootstrap → codegen → build → migrate → атомарный флип → healthcheck), сердце fail-после-флипа → авто-rollback на `auto @ дефолтный L1`, floor непробиваем живьём; цепочка фиксов живого прогона — prisma codegen как шаг манифеста + запрет bare ExecStart (DEC-DEV-0205), структура ожидания build-test гейта + развод «не смог измерить»/«код провалил» (DEC-DEV-0206), ранний BLOCKED в точке вердикта env-пробы + structured capture-don't-fix во всех стадиях (DEC-DEV-0211) и у диспетчера (DEC-DEV-0213); смоук-агрегаты 0177 скорректированы независимым пересудом (DEC-DEV-0197 D13 / DEC-DEV-0204). Смоук Epic E: S1-S7 закрыты (SSOT — `dev/gates/EPIC_E_SMOKE_TEST_PLAN.md` + журнал кампании `dev/_archive/campaign-prod/PROD_READINESS_CAMPAIGN.md`).

### Added

- **Epic E · E.B — scene-bootstrap: деплой теперь СТРОИТ сцену, а не только описывает её.** Фаза `Deploy` процесса `deploy-to-stage` получила мутирующую стадию `scene-bootstrap` (**строго после гейта §3.2** — она создаёт каталоги, пишет файл секретов и ставит systemd-юниты, т.е. это мутация, и в префлайте она обошла бы floor): разворачивает `deploy_root` (`~` → `$HOME`), `mkdir -p` для `releases/` + `shared/{.env,logs,uploads}`, сеет `shared/.env` из корня проекта (staging v1 = dev-tier секреты; нет и там ⇒ честный `ENV_NOT_READY`, **никакой фабрикации**), выкладывает `releases/<ts>`, симлинкует `shared` внутрь релиза, материализует systemd-юниты из шаблонов (+ `daemon-reload` / `enable`). Идемпотентна (повторный деплой ничего не ломает и не дублирует; новый `releases/<ts>` — это и есть контракт Capistrano, он же делает возможным откат). Флип `current` — **атомарный** (`ln -sfn` + `mv -T`, никогда `rm && ln`).
- **`orchestrator/lib/deploy-manifest.cjs` — детерминированное чтение deploy-манифеста (CLI-шов + агент-транспорт).** Парсер YAML-подмножества на голом Node (без зависимостей — либа копируется в пилот), CRLF-толерантный, **без часов** (timestamp приходит аргументом ⇒ N прогонов байт-идентичны). Отдаёт `present` / `status` / `steps` / `healthcheck` / `migrate` / `release_layout` (прежние поля, без переименований) + `deploy_root` (развёрнутый), `scene` (все абсолютные пути), `units`, `blocking_defects`. Вшит в цепь `verify` (`test:orchestrator`).

- **Epic E · E.B/E.C — процессы `deploy-to-stage` + `rollback-release` (fabric deploy-брекет).** Оркестратор получил конвейер деплоя: `deploy-to-stage` (build → P7 boot-гейт → **§3.2 autonomy-резолвер** → deploy staging → healthcheck) и `rollback-release` (симлинк-swap). Чартер `feature-production-line` v4→v5: после `runtime_gate` линия ведёт в `deploying_staging` → на успех `done`, на провал healthcheck → авто-`rolling_back` → `rolled_back` (owner-surface). **Деплой физически не может обойти floor:** мутация достижима только после `disposition === 'auto'` от резолвера (проверено на двух слоях — fabric-prescription + внутри-процессный readiness-aware гейт). Реальный деплой (build/migrate/симлинк/systemctl/healthcheck) — VM-gated (E.D/E.G).
- **Epic E · E.E — healthcheck-звено P7 оживлено.** `runtime-smoke-readiness` получил CLI-флаг `--health-check "GET .../health"` — пост-деплойная liveness-проба вплетается в `smokePlan.success_signals` (машинерия и юнит-покрытие уже были; мёртвым было только CLI-звено).
- **`/integrator:provision <capability>` — команда-триггер deploy-capability (Epic E · E.A).** Спавнит `deployer`-субагента, получает deploy-setup + CNT-контракт (draft), персистит в `.claude/integrator/contracts/` через approve-гейт [Y/E/N]. Оснащает, не исполняет (§8.3). prisma-absent — note-and-proceed.
- **Epic E · E.A — Integrator оснащает deploy-capability (scaffold, owner-gated).** Новый role-агент `agents/integrator/deployer.md` + скилл `skills/integrator/deployment-provisioning.md`: Integrator **авторит** deploy-setup (systemd-unit-шаблоны для `@app/{api,web,worker}`, `releases/<ts>`+`current`-layout, шаг `prisma migrate deploy`, healthcheck-спека на базе P7 failure-таксономии) + CNT-контракт deploy-capability (`producer: deployer` / `consumer: deploy-to-stage`). Граница §8.3 закрыта **структурно**: `deployer` несёт `tools: Read, Grep, Glob` — без `Bash`/`Write`, поэтому исполнить deploy (`systemctl`/симлинк-флип) физически не может; исполнение — за Orchestrator-процессом `deploy-to-stage` (E.B, ещё не построен). Сама команда-триггер и live-валидация — за owner-gate + восстановлением VM.
- **Каталог скиллов — генератор + строгая проверка (`docs/guide/08-skills.md`).** Построен `gen-skill-catalog.cjs` (симметрично каталогу команд): каталог скиллов генерируется из frontmatter `description:`, `gen:skills:check` вшит в цепь `verify`. `/ecosystem:verify` Step 4 получил инвентарь скиллов + строку `SKILLS` в отчёте (Step 9). Закрывает **пустой пронг «скилл»** harness-контракта — теперь swap/rename скилла с сохранением числа **больше не проходит молча** (прежде держался лишь на floor-затычке). `DEF-CTX-5` закрыт.

### Changed

- **Autonomy F3-core: уровень L3 теперь реально отличается от L2 в prod-сегменте (Epic E · B4).** Прежде L3 был затычкой (≡ L2 до Epic E). Теперь: `L3 × staging` для не-floor операций даёт `auto` (вместо `consilium-gate` на L2) — автономный staging-деплой обменивает пред-деплойное жюри на пост-деплойную сетку healthcheck→auto-rollback; **floor остаётся абсолютно непробиваемым на всех уровнях** (`prod_deploy` × L3 → human-gate, доказано свипом). Новый operation-класс `rollback` (staging = `auto` на любом уровне, чтобы auto-rollback срабатывал без человека; prod = `human-gate` всегда). Fabric-состояния теперь могут объявлять per-state `env_tier` (`meta.env_tier`; отсутствует ⇒ прежнее глобальное поведение 1:1).

### Fixed

- **`/orchestrator:run`: диспетчеру вписан structured-запрет мутаций субстрата — «substrate prep» больше не легитимная идея (DEC-DEV-0213; live-прогон ld6vrs).** Фикс 0211 закрыл все стадии процесса, но диспетчер-сессия осталась единственным актором без capture-don't-fix — и на ре-ране S5 сама подняла погашенный оператором Redis («Substrate prep… This should clear the readiness axis»), стерев поставленный сценарий до исполнения env-readiness. Pre-flight `run.md` теперь несёт «🚫 THE SUBSTRATE IS READ-ONLY FOR THE DISPATCHER»: перечисление запрещённых классов команд (docker/systemctl/пакеты/`shared/.env`), переопределение цели (`BLOCKED × ENV_NOT_READY` на погашенной зависимости = **successful, valid run**), пост-терминальное правило (env-фиксы и ре-драйвы — ход владельца, не диспетчера). Grep-инвариант в `fabric-dispatcher-wiring.test.cjs`.
- **Orchestrator deploy-to-stage: ENV_NOT_READY от env-пробы теперь коротит префлайт ДО manifest-parse/build-test; всем субстрат-смежным стадиям вписан structured-запрет мутаций среды (DEC-DEV-0211; live-прогон lah60w).** Раньше вердикт пробы доезжал до BLOCKED-арки только в конце префлайта — manifest-parse и build-test успевали отработать на мёртвом субстрате, и агент build-test «полечил» его сам (`docker start mft-redis`), задним числом обнуляя readiness-гейт. Теперь: ранний `BLOCKED × ENV_NOT_READY` сразу после пробы (flipped:false, failure_class `env-not-ready-preflight`, disposition/autonomy: null — гейт §3.2 честно не достигнут, поимённые readiness_reasons, полный FIND-D-трейл); DEGRADED по-прежнему НЕ коротит (ось решения — едет в §3.2, applyReadinessGuard auto→human-gate). Промпты стадий: build-test — «substrate is READ-ONLY» с перечислением запрещённых классов команд и «environment failures are EVIDENCE» (упавший из-за среды сьют = завершённый RED, не повод чинить сервис и не measurement-incomplete); env-readiness / runtime-readiness — read-only мандат; scene-bootstrap / deploy-flip — MUTATION BOUNDARY (легитимные деплой-мутации перечислены, docker/инфра/пакеты запрещены); healthcheck — запрет поднимать сервисы ради 2xx. +5 wiring-тестов, исполняющих реальный предикат (new Function).
- **🔴 Ложно-негативный `DEPLOY_FAILED`: build-test-гейт деплоя убивал ~7.5-мин сьют по 5-мин Bash-таймауту и сжигал годный прогон в escalate (DEC-DEV-0206, вскрыт живым прогоном l1fi9c).** Стадия `build-test` процесса `deploy-to-stage` не давала агенту НИКАКИХ инструкций по ожиданию — стратегия оставалась стохастике инстанса: один агент выставил Bash-таймаут 300000, получил kill на 5m0s (exit 143), перезапустил сьют в фоне (`run_in_background`), поллил ~1 мин и вернул `passed:false` («completion UNKNOWN»); брошенный фон доработал сам и был 100% зелёным. `DEPLOY_FAILED` роутится в escalate («код провалил») — но код был в порядке, не завершилось ИЗМЕРЕНИЕ. Промпт стадии теперь СТРУКТУРИРУЕТ ожидание: build — один блокирующий вызов с явным `timeout: 600000`; сьют — пер-workspace последовательные блокирующие вызовы (`pnpm --filter <ws> test`, каждый с явным `timeout: 600000`; заодно durable-защита — весь сьют уже ~7.5 мин при потолке инструмента 10 мин, единый `pnpm -r test` однажды перестанет влезать); запрещены `run_in_background` для тестов, возврат вердикта до финального exit-code каждого workspace и коллапс UNKNOWN в `passed:false`. Развод осей измерения и вердикта: `BUILD_SCHEMA` получил `suite_completed:boolean` (additive, без переименований — DEC-DEV-0012; absent ⇒ completed, RED-путь байт-в-байт прежний), и `passed:false` при `suite_completed:false` даёт **`BLOCKED × READY`** с `failure_class: 'test-gate-incomplete'` → `evt:deploy.gated` → `runtime_gate_retry` (**ре-ран**), НЕ `DEPLOY_FAILED` → escalate. Реальный RED (`suite_completed:true`) роутится как прежде. Смешение «не смог измерить» и «код провалил» — та же ошибка осей, что дедлок DEC-DEV-0201, на слой ниже.
- **Epic E · deploy-оснастка: prisma codegen теперь шаг манифеста, и его отсутствие блокируется (DEC-DEV-0205 / FIND-C1, вскрыт третьим живым прогоном — первым, дошедшим до build внутри релиза).** Релиз ставит ЧИСТЫЙ пер-релизный `node_modules` (`pnpm install --frozen-lockfile` внутри `releases/<ts>` — решение о самодостаточности отката), где Prisma-клиент никогда не сгенерён — манифест без codegen-шага детерминированно ронял `pnpm -r build` (`packages/db: TS2305 — '@prisma/client' has no exported member 'PrismaClient'`); dev-чекаут пилота маскировал это исторически сгенерённым клиентом. Скилл `deployment-provisioning.md` §3 предписывает codegen-шаг (`pnpm --filter @app/db exec prisma generate`, `working_directory: {{RELEASE_DIR}}`) **первым** в step-list (пер-релизный install уже выполнен scene-bootstrap'ом, build впереди), тем же runtime-признаком, что и migrate; migrate обязан держать литерал `prisma` видимым (opaque-скрипт вроде `db:migrate:deploy` слепит гейт — задокументировано и запинено тестом). Гейт: `deploy-manifest.cjs` → `blocking_defects: ['manifest-missing-prisma-codegen']` → `deploy-to-stage` даёт `ENV_NOT_READY` («capability мис-оснащена»), лекарство `/integrator:provision`. Манифест без prisma вовсе (flyway/no-DB) codegen не требует.
- **Epic E · systemd-юниты: bare-команды в `ExecStart`-семействе запрещены и блокируются (DEC-DEV-0205 / FIND-C2).** Живой web-шаблон нёс `ExecStart=pnpm --filter @app/web start` — systemd не резолвит команды по PATH юзер-шелла (fixed compile-time path; nvm/corepack/`~/.local/share/pnpm` для него невидимы), юнит умирает на exec (`status=203/EXEC`), не отдав ни одного запроса. Скилл §1: первый токен `ExecStart`/`ExecStartPre`/`ExecStartPost` — только абсолютный путь или `{{…}}`-плейсхолдер; введён `{{PNPM_BIN}}`, материализуемый scene-bootstrap'ом через `command -v pnpm` (симметрично `{{NODE_BIN}}` — плейсхолдеру сразу назначен материализатор, урок 35 применён к самому фиксу). Гейт: `blocking_defects: ['unit-execstart-bare-command']` (systemd-префиксы `-@:!+` учтены; комментарии не судятся; нечитаемый шаблон ⇒ null-вердикт, «слеп ≠ нашёл»). ⚠️ Инстанс пилота начнёт флагаться **после ре-провижена** по обновлённому скиллу.
- **🔴 E.A написал рецепт — кухню не построил никто: сцена деплоя не материализовалась (вскрыт вторым живым прогоном deploy-контура).** `deploy-manifest.yaml` объявлял `deploy_root: "~/deploy/…"`, layout `releases/<ts>` + симлинк `current` + `shared/{.env,logs,uploads}` и три systemd-юнит-шаблона — а `deploy-to-stage` **не материализовал ничего из этого**: в процессе не было ни одного вхождения `deploy_root` / `homedir` / `expand`, `~` никогда не разворачивался, `~/deploy/` не создавался, `releases/<ts>` не выкладывался, `shared/.env` (откуда `migrate` берёт `DATABASE_URL`) не создавался, юниты не ставились. **Первый деплой был невозможен в принципе** — та же форма, что и дедлок DEC-DEV-0201: две правильные половины и пропасть между ними; обязанность *создать* сцену не была записана нигде, и потому её не исполнял никто. Починено стадией `scene-bootstrap` (см. Added).
- **🔴 systemd-юниты были прибиты к КОНКРЕТНОМУ релизу — деплой падал бы ЗЕЛЁНЫМ.** Шаблоны (`skills/integrator/deployment-provisioning.md` §1) параметризовались `{{RELEASE_DIR}}`, т.е. конкретным `releases/<ts>`. Такой юнит **глух к флипу**: `current` переезжает, юнит — нет, `systemctl restart` поднимает **старый** релиз — и healthcheck **проходит** (старый код жив, здоров, на том же порту). Прогон вернул бы **`DEPLOYED`, не задеплоив ничего**, а эта ложная зелень — ровно тот `contract_evidence`, который флипает неверифицированный `draft`-CNT в `active`: сломанная capability сертифицировала бы **сама себя**. Тем же молчанием ломался и rollback (E.C): флип обратно тоже ни на что не влиял. Шаблоны переведены на **`{{CURRENT_LINK}}`** (контракт Capistrano: юнит смотрит на `current`, флип и есть деплой; `{{RELEASE_DIR}}` остаётся законным в шаге `build` манифеста — сборка принадлежит одному релизу). Consumer **принуждает** это: `deploy-manifest.cjs` читает каждый `.service.template` и на release-pinned юните отдаёт `blocking_defects: ['unit-template-release-pinned']` → `deploy-to-stage` даёт `ENV_NOT_READY` («capability мис-оснащена») → `BLOCKED`, `flipped: false`, лекарство = `/integrator:provision`. **Оркестратор шаблон Интегратора не переписывает** (§8.3): он отказывается исполнять сломанную оснастку, а чинится она на стороне E.A. ⚠️ Существующие инстансы пилота обязаны быть **пере-оснащены** (`/integrator:provision <capability>`) — до этого деплой будет честно блокироваться.
- **🔴 Readiness-гейт деплоя стоял на стохастическом парсинге YAML.** `present` / `status` / `steps` манифеста брались у **sonnet-субагента**: один и тот же неизменённый файл был распарсен как полный 4-шаговый список (→ READY) и, через 18 минут, как `step-list=MISSING` (→ `ENV_NOT_READY`); один раз агент вернул самопротиворечивую пару `present:true` + `steps:[]`. «Есть ли в файле step-list» — **детерминированный факт о байтах**, и он обязан извлекаться детерминированно: ложно-отрицательный флейк случайно блокирует годные деплои, ложно-положительный провёл бы искалеченный манифест в **реальную мутацию**. Парсинг вынесен в `deploy-manifest.cjs` (CLI-шов + агент-транспорт — та же форма, что у `autonomy-policy.cjs`); агент теперь **только релеит JSON** и ему явно запрещено подменять его собственным чтением файла. Схема результата не переименована (DEC-DEV-0012).
- **Аудит-след упавшего деплоя был беднее, чем у заблокированного — и корень был не в процессе, а в леджере.** `run.json` прогона с `DEPLOY_FAILED` нёс только `result`/`readiness`: `disposition: auto` (факт, что гейт §3.2 **санкционировал мутацию**) приходилось доставать из транскрипта, а «переехало ли что-нибудь живое?» было по записи неотвечаемо. Return-арки процесса несли эти поля **всё время** — их выбрасывал `run-ledger.cjs::summarizeResult`, проецировавший возврат на фиксированный набор ключей. Добавлен **`decision_trail`** (`disposition` / `flipped` / `release` / `healthcheck` / `failure_class` / `contract_status` / `contract_evidence` / `disclosures`; `null` у процессов, которым нечего трейсить ⇒ формы P2/P3/P4 не меняются; `ledger.ndjson` остаётся компактной). Плюс все result-несущие return-арки `deploy-to-stage` приведены к **единому набору ключей** (+ `failure_class` поднят на верхний уровень) — wiring-тест это принуждает, так что «тонкую» арку больше нельзя завезти незаметно. **DEPLOY_FAILED — ровно тот случай, когда след нужнее всего: мутация могла состояться.**
- **🔴 Первый деплой был невозможен в принципе: `draft`-контракт читался как неготовность среды (вскрыт первым живым прогоном deploy-контура).** `deploy-to-stage` отказывался деплоить capability, чей CNT-контракт в статусе `draft` — считал это `ENV_NOT_READY` («не смог подготовить деплой») и возвращал `BLOCKED`. Но Интегратор **обязан** отдавать контракт `draft` (он не вправе объявить `active` до живой верификации), а верифицировать контракт можно **только успешным деплоем** ⇒ замкнутый круг: **первый деплой недостижим ни при каких настройках**. При этом `ENV_NOT_READY` было просто неправдой — оснастка лежала на диске (манифест 254 строки, парсится, step-list полный), и в живом `run.json` стояло `disposition: null`: **резолвер §3.2 даже не вызывался**, префлайт закорачивал раньше (мутирующая ветка гейта на живом субстрате не исполнялась ни разу). **Корень — категориальная ошибка:** смешаны две разные оси — *«МОЖЕМ ли вообще?»* (среда/оснастка → **block**) и *«КТО решает?»* (контракт никто не верифицировал → **human-gate**). **Отсутствующая capability — это блок; отсутствующий человек — это гейт.** Оси разведены: `present` теперь чистый факт (файл есть + парсится + несёт исполнимый step-list) и `status` в нём **не участвует** — `present=false` по-прежнему даёт `ENV_NOT_READY`; `status: draft` больше **не понижает readiness**, а едет в резолвер как `--contract-status`. В `autonomy-policy.cjs` добавлен **`applyContractGuard`** (зеркало `applyReadinessGuard`, тоже **только понижает**): `draft` → `auto` становится `human-gate` (**не** `block`), а `--accept-draft-contract` = **явная санкция владельца** на первый деплой по непроверенному контракту (без акта человека `auto` по `draft` не бывает). **Floor непробиваем как и был:** guard стоит ниже floor-проверки и никогда не повышает диспозицию — `prod_deploy` × L3 × `draft` × `--accept-draft-contract` = `human-gate` + `floor_hit: true`. Цикл замкнут: успешный деплой возвращает `contract_evidence` (CNT + `run_id` + вердикт) и раскрывает владельцу, что контракт можно флипнуть в `active` — но **§8.3 соблюдён: Orchestrator только СООБЩАЕТ, флип контракта пишет Интегратор** (`.claude/integrator/**` — не его зона). Отсутствие `--contract-status` ⇒ поведение резолвера **байт-в-байт** прежнее (P5/P6/fabric не затронуты).
- **Run-ledger терял ИСХОД прогона: упавший бут писался в трейс как `null` (вскрыт живым прогоном P7).** `summarizeResult()` читал `r.result`, а P7 `runtime-smoke-readiness` отдаёт исход под ключом **`p7_result`** ⇒ прогон, чей бэкенд реально не поднялся (`FAILS_TO_START`), лёг в `run.json` как `"result": null` — **неотличимо от зелёного**. Той же болезнью болел **P5** `feature-to-tdd-impl`: его GO-гейт лежит в `go_gate` (top-level `result` у процесса нет вовсе) ⇒ исход терялся на **каждом** P5-прогоне, и `impl-evidence.cjs` (который по докстрингу ищет «latest gate verdict across P5/P6-class runs») не видел ни одного P5. Леджер, зеленящий провал, не является трейсом — а именно на него опирается компонент 2 гейта `SUBSTRATE_GRADUATION_GATE`. Починено расширением **чтения**, без переименования схемы процессов (DEC-DEV-0012): контракт `OUTCOME_KEYS = result | p7_result | go_gate` (собственные имена процессов, по приоритету) + разбор обёрнутой формы (`verdict.<key>` — оттуда достаются и исход, и `readiness`, ранее читавшийся как `null` поверх живого `verdict.readiness: DEGRADED`). `readiness_reasons` теперь **доезжают** до `run.json` и ndjson-строки — обещание, записанное в коде PR #190 («the gate must be auditable from run.json alone»), до этого не исполнялось. Пять исходных ключей `result_summary` сохранили имя/позицию/смысл — формы P6 / `deploy-to-stage` / `rollback-release` суммируются как прежде (покрыто backward-compat-тестом). Новое: `outcome_key` (провенанс — из какого ключа взят исход; `null` ⇒ процесс исхода не давал, как P2/P3/P4) и `unread_outcome_keys` (**алярм**: outcome-образный ключ, который леджер видит, но читать не умеет — в ndjson попадает только когда не пуст). Против рецидива класса — тест-сверка **реальных исходников** `orchestrator/processes/*.mjs` с контрактом леджера (падает, если процесс заведёт исход под неизвестным ключом; при непарсящемся источнике падает громко, а не молчит).
- **P7 месяцами выносил вердикт БЕЗ проверки §6 capability — контрактный рассинхрон ключа фичи (вскрыт живым прогоном).** `runtime-smoke-readiness` (и пред-флип ре-проба `deploy-to-stage`) резолвит фичу в `.product/features/` по FM-id, а `commands/orchestrator/run.md` предписывал подавать **cc-sdd-слаг** из `.kiro/specs/` — другой каталог, другой ключ. Промах давал `capabilities_unknown: true`, т.е. **вердикт выносился, не проверив, нет ли boot-блокирующей capability**. Не всплывало из-за подстрочной лотереи: у 5 из 6 фич пилота kiro-слаг случайно оказался подстрокой имени FM-файла (`auth` → `FM-001-authentication`), и fuzzy-матч маскировал разрыв. Починено: контракт в `run.md` исправлен (`feature` = ключ `.product/features`, напр. `FM-006`), нерезолв стал **громким** (`capabilities_unknown_reason` в результате + дисклоз называет искомый ключ И саму ловушку), мёртвый `SPEC_DIR` (окаменелость того же разрыва — мапил FEATURE в `.kiro/specs/`) удалён с guard-тестом против возврата. Логика матчинга не тронута.
- **Монорепо: бэкендная фича смоукалась против фронтенда — флаг `--app` не доходил до пробы.** Либа `runtime-readiness.cjs` флаг умеет и сама же советует его в дисклозе («pin explicitly with `--app <dir>`»), но процессы его не передавали, а в команде ключа не было ⇒ совет был физически невыполним. При этом `sourceRank` ставит `scripts.dev` выше `scripts.start`, поэтому `apps/web` детерминированно обгонял `apps/api`: живой прогон FM-006 (`has_ui: false`) бутнул Next.js. Бэкендный дефект (env / миграция / секрет) на статичной главной странице не всплыл бы. `--app` пробрасывается в P7 и в пред-флип ре-пробу E.B; отсутствие флага ⇒ поведение байт-в-байт прежнее.
- **Гейт автономии деплоя стал аудируемым: `readiness_reasons` больше не выбрасываются.** `applyReadinessGuard` понижает `auto → human-gate` по `DEGRADED`, но причины readiness терялись (логировались и не попадали в результат) — в `run.json` не было **ни строки** о том, почему деплой ушёл к человеку. Теперь `readiness_reasons` едут в результате P7 и во **всех** return-арм `deploy-to-stage` (включая локальные понижения: непровиженный манифест, пред-флип вердикт). Плюс `env-readiness` reasons стали **actionable**: вместо «pg_isready not installed» — с командой установки. Практический смысл: без клиентских утилит БД проба вечно возвращает `DEGRADED` ⇒ staging-деплой даже на L3 всегда human-gated, а причина («не установлен psql-клиент») от симптома отстоит далеко.
- **`/ecosystem:bootstrap` не удалял хуки, выпиленные из манифеста (prune-асимметрия).** `bootstrap` делал additive-union записей хуков, тогда как `update` их re-derive'ит — поэтому хук, удалённый из манифеста, переживал `bootstrap` в `settings.json` пилота и продолжал стрелять (`Cannot find module`, если `.js` уже удалён), а любой флаг отключения оказывался тихим no-op. `bootstrap` Step 6b переведён на ту же re-derive-семантику, что `update` (ecosystem-owned записи пере-выводятся из манифестов, сторонние — сохраняются verbatim). Противоречие Step 6a↔6b устранено. `DEF-CTX-3`.
- **`/ecosystem:bootstrap` утаскивал в `.claude/` пилота лишнее** — всё дерево `tests/` (мусор вне зоны синка `update` ⇒ никогда не чистился) и затирал захваченный `ECOSYSTEM_HEAD` в `unknown` (чтение временной директории после её удаления). Оба пути починены; фикстуры извлекаются до фильтрации источника. `DEF-CTX-2` (мёртвое поле `enabled_when`) удалено из манифестов.
- **`.gitattributes` теперь доставляется в `.claude/` пилота** (create-if-absent, новый `gitattributes.template`) — скрипты экосистемы (`*.mjs`/`*.cjs`/`*.js`/`*.sh`) пинятся к LF, чтобы Windows-`autocrlf` не материализовал CR-байт, ломающий запуск Workflow-процессов. Существующий `.claude/.gitattributes` проекта не перезаписывается. `FB-LR-27`.
- **NFR-артефакты теперь проходят проверку схемы (V-18).** Устранён enum-дрейф `sanity_check` (спека ↔ скилл), два эталонных примера в `NFR.md` приведены к собственной схеме, inline-валидатор (`artifact-validate.js`) расширен веткой `NFR`, `validation.md` §V-18 покрывает `NFR-*`. `DEF-CTX-1`.
- **Security (принятый долг из ревью 2026-07-11).** Stored XSS в генерируемом `app-map.html` (данные артефактов, влияемые prompt-injection, экранируются при вставке в inline-`<script>`); path-traversal при чтении `.git/HEAD` в детекторе параллельных сессий (ref валидируется). Оба — в доставляемых хуках. (Dev-only command-injection в `next-dec-dev.js` / `check-information-map.js` тоже закрыты, но в поставку не входят.)
- **Ложная сводка смоук-батча DEC-DEV-0177 растиражирована по документам — исправлена** (DEC-DEV-0197 / D13). Судья (opus) верно выставил все **22 per-сценарных вердикта**, но **ошибся в собственной агрегации**: назвал `11 PASS / 2 PARTIAL / 6 N/A / 0 FAIL` — **сумма 19**, не сходится ни с заявленными 22 грейд-точками, ни с его же таблицей (перечислил пять N/A, назвав шесть; назвал два PARTIAL, перечислив три; опустил carry-forward-строки S-LE.2 и S-LE.6). Ложные числа были скопированы verbatim в бриф → DEV_JOURNAL → CHANGELOG `[1.9.1]` → ROADMAP → память и прожили там ~2 суток. Истина по таблице судьи — **`14 PASS / 3 PARTIAL / 5 N/A / 0 FAIL`** (сумма 22 ✓; подтверждено двумя независимыми пересчётами транскрипта). **Per-сценарные вердикты не пересуживались и не менялись** — правились только агрегаты и две пропущенные строки; улика (прежние числа) сохранена в каждом месте правки. SSOT — `dev/gates/SMOKE_BATCH_2026-07-11_BRIEF.md` §Outcome. **Урок:** сводку LLM-судьи нельзя тиражировать, не пересчитав её против его же таблицы — сумма не билась с n=22, и этого не проверил никто.
- **Per-сценарные вердикты смоука DEC-DEV-0177 пересужены независимым судьёй — гипотеза K9 («судья-автор мог завысить») закрыта как «подтверждена частично»** (DEC-DEV-0204). Судья sonnet (вне семьи opus — исходный судья и исполнители были opus-4-8), рубрика зафиксирована до прогона, корпус расширен первичкой GitHub-пилота (реальные коммиты/диффы, которых исходный судья не видел). Итог: **20/22 вердиктов устояли** (шесть — подтверждены evidence сильнее исходной), **2 понижены PASS→PARTIAL** (PATCH_1.3.3 S1 — per-tier-таблица research не подтверждена evidence; PHASE_6 S4 — 1 из 4 критериев подтверждён, write-путь не упражнён). Агрегат: `14/3/5/0` → **`12 PASS / 5 PARTIAL / 5 N/A / 0 FAIL`**. Инфляция мягкая и локализованная (9% строк), ни один N/A не прятал FAIL, качественные выводы прогона (Phase-7 validated, флип lesson-gate, 0 FAIL) не переворачиваются. Прежние числа сохранены как улика в местах правки; пересуд полностью выполнен на статичном корпусе + GitHub — ре-прогон на VM не потребовался.

---

## [1.11.0] — 2026-07-11

**Minor release — accumulated since 1.10.0:** две Tier-0 способности кампании «хвосты до PROD». **G02 result-ingest** — `/product:impl-sync` + детерминированный evidence-сенсор `impl-evidence.cjs`: обратный поток реализации в `.product/` замкнут (approve-gate [Y/E/N], FM `status → shipped` + опциональный canonical-блок `impl_sync`; DEC-DEV-0192). **F2 autonomy-wiring полный v1** — L2/L3 эмитят `consilium-gate` (floor непробиваем; confidence из synth-strength, τ default 0.8, human-fallback; конфиг `autonomy:` в product.yaml с профилями default=L1/autonomous=L3; P5/P6 несут поле `autonomy`; DEC-DEV-0193). Counts без изменений (24/44). **Deferred (carried, not lost):** live-прогон `/product:impl-sync` на реальном пилот-состоянии + live-грейд consilium-гейта — pilot-gated; Epic E kickoff выполнен (DEC-DEV-0194, `dev/gates/EPIC_E_READINESS.md`) — build E1 после спайка VM-реальности.

### Added

- **Autonomy Epic F, инкремент F2 построен — L2/L3 consilium-gate + именованные профили + врезки P5/P6 (DEC-DEV-0193).** F1 дал детерминированный resolver L0/L1 + floor + precedence; **F2** доводит уровни L2/L3 из «деградируют к L1» до рабочего consilium-gate. **Семантика L2/L3 (`orchestrator/lib/autonomy-policy.cjs`):** на `dev` — как L1 (auto); на `staging`/`prod` — новая диспозиция **`consilium-gate`** (жюри Epic D вместо человека до floor); **L3 ≡ L2** в матрице до F3 (prod-сегмент, Epic E) — с громкой why-нотой; L0/L1 бит-в-бит как раньше (regression-контракт). **Floor непробиваем by construction:** `prod_deploy`/`destructive`/`spend_money`/`provision_real_secret` → всегда `human-gate` + `floor_hit` ПЕРВЫМ приоритетом, `consilium-gate` на floor не эмитится никогда (`FLOOR_LOCKED` — сужение floor остаётся отдельным явным opt-in, обычный `floor:`-ключ конфига игнорируется громко). **Consilium-fold (safe-fallback):** новые чистые ф-ции `confidenceFromSynth(synth)` (детерминированный мэппинг выхода `consilium-synth.cjs`: `strong→1.0`/`split→0.5`/`none→0.0`/мусор→`0.0`) + `applyConsiliumVerdict(envelope, synth, gateCfg)` — `confidence ≥ τ` И есть `recommended` → `auto`, иначе → **`human-gate`** (слабый/расходящийся вердикт = человек, не слепой auto); `τ` = `policy.consilium_gate.confidence_threshold` или дефолт **0.8**; никогда не бросает. **Config-слот ПОСТРОЕН:** `parseAutonomyConfig(yamlText)` — line-state-machine парсер блока `autonomy:` из `.claude/product.yaml` БЕЗ js-yaml-зависимости (прецедент `agent-roster.cjs`), поддержаны inline `{...}` и block-форма; именованные профили **`default`=L1 / `autonomous`=L3** (Vision Locked), явный `default_level` перекрывает профиль (с why-нотой); инвалидные уровни/мусор — tolerant-ignore с why; **absent блок → `{}` (== built-in L1, 1:1 — soft-миграция как `product_class` 0079)**; `loadAutonomyPolicy(root)` fail-tolerant (нет файла → `{}`). **Fabric-wiring:** `fabric-engine.cjs shellEnv` мержит `Object.assign({}, loadAutonomyPolicy(<project root>), limits.json.policy || {})` — product.yaml = проектный дефолт, `limits.json.policy` = fabric-локальный override (отсутствие product.yaml → поведение 1:1 с pre-F2). **Врезки P5/P6 (аддитивные):** `feature-to-tdd-impl` (P5) и `validate-feature-impl` (P6) кладут в result-файл поле **`autonomy`** = `applyReadinessGuard(resolve(<op>, riskTier, envTier, policy, override), readiness)` — авторитетная диспозиция для актуатора (диспетчер `run.md` / fabric ingest); harness `.mjs` **не может** `require()` либу (DEC-DEV-0073 §D.1), поэтому диспозиция вычисляется через новый **CLI-seam** `node autonomy-policy.cjs resolve …` внутри agent()-промпта (тот же relay-паттерн, что env-readiness/capability-probe); riskTier — консервативное честное правило (чистый GO без конфликтов/§6-блоков = LOW, иначе HIGH); существующая логика вердиктов/полей возвратов НЕ переписана. **Диспетчер `run.md`:** `--autonomy L0|L1` → `L0|L1|L2|L3`; новая секция «consilium-gate prescription» (собрать жюри `architecture-consilium` → `consilium-synth.cjs` → `resolve-consilium` → auto продолжает брекет / human-gate в owner-queue + STOP; reversibility-first рельс). **Границы скоупа (сознательно):** Integrator **approve-gate** (`/integrator:debug` y/n) и **DA per-finding review** НЕ на этом контуре — захардкожены на человека как анти-sycophancy рельса; floor залочен; **live-грейд L2/L3-петли pilot-gated** (жюри работает, сквозной dogfood не прогонялся). **Тесты:** `autonomy-policy.test.cjs` расширен (матрица L2/L3, floor@L2/L3, L3≡L2, `confidenceFromSynth`, `applyConsiliumVerdict`, `parseAutonomyConfig`, `loadAutonomyPolicy`, CLI round-trip; 2 старых F1-теста про деградацию L2/L3 флипнуты — единственная санкционированная правка) + P5/P6 wiring-тесты (новое поле `autonomy` + resolve-seam) + fabric-engine/dispatcher зелёные. **Counts без изменений (24/44 — резолвер не артефакт-тип).** Consumer-zone (`orchestrator/` + `commands/` + `docs/`); всё аддитивно.

- **Result-ingest: команда `/product:impl-sync` + детерминированная либа evidence-сбора — замкнут обратный поток реализации в `.product/` (закрыт разрыв аудита G02, DEC-DEV-0192).** Цепь Product→handoff→adapter→external tool была **однонаправленной**: результат имплементации никогда не возвращался, и `FM.status` навсегда висел в `planned`/`in-progress` даже после того, как фича реально уезжала в production. **Новое:** (1) `hooks/product/lib/impl-evidence.cjs` — детерминированный **read-only сенсор** (Node builtins, pure-core + CLI): для каждой FM собирает evidence из 4 fail-tolerant источников — **runs** (`.claude/orchestrator/runs/*/run.json`, упоминающие FM → последний gate-вердикт GO/NO-GO/MANUAL_VERIFY_REQUIRED по `finished_at`), **fabric** (`.claude/orchestrator/fabric/*/state.json`, матч по FM-id ИЛИ subject-в-handoff → `fabric_done`), **external** (`.kiro/specs/*` каталоги, упоминающие FM или совпадающие по slug заголовка — читаются ВСЕ top-level файлы, не только `spec.json`), **handoff** (source SC/BR/IC + advisory-coverage). **Переиспользует** id-экстракторы `orchestrator/lib/coverage-oracle.cjs` (`extractIds`/`extractSourceIds`/`computeCoverage`) через кросс-зонный require — regex-алгоритмы НЕ дублируются (прецедент `handoff-staleness.cjs`); per-FM disposition = `already-shipped`/`deprecated`/`no-evidence`/`gate-not-passed`/`validation-blocked` (V-01)/`ready-to-ship`. CLI `--root`/`--fm`/`--json`/`--at`; exit 0 всегда на штатной работе (сенсор, не гейт), секретов/env не читает. (2) `commands/product/impl-sync.md` — команда `[FM-NNN] [--all] [--dry-run]`: отчёт-таблица per-FM → **approve-gate `[Y] apply / [E] evidence+diff / [N] skip`** («Without an explicit Y, nothing in `.product/` is modified») → на Y пишет `FM.status: shipped` + `updated` + опциональный canonical-блок `impl_sync` (`synced_at`/`gate`/`run_id`/`evidence`/`coverage_missing`) + запись `DEC-SYNC` в `.product/.decisions/journal.md` (зеркало `DEC-PROMOTE`). Явный anti-pattern-список запрещённых полей (DEC-DEV-0012): ❌`implementation_status`/`impl_status`/`sync_date`/`synced_on`/`implementation_sync` — canonical ТОЛЬКО `impl_sync`. **Границы скоупа (сознательно):** Product — единственный писатель `.product/` (Integrator SPEC §8.3; Orchestrator/fabric оставляют лишь note-prescription `project_fm_shipped_hint`); НЕ автозапись без Y; НЕ хук (никакого auto-sync на SessionStart — v1 только явная команда); НЕ трогает Orchestrator/fabric/Integrator-зону, `.kiro/` только читается; НЕ новое правило валидации — V-01 переиспользуется, coverage advisory; блок `impl_sync` опционален (absent == поведение 1:1, soft-миграция как `product_class` 0079 / `domain_fit` 0169). **Wiring (cross-component awareness, DEC-DEV-0082):** реген каталога команд (`/product:* 22→23`, всего 50) + запись в `ecosystem-map.overlay.json` (лейн HO) + узел `IMPL-SYNC` в карте процессов (product-lane, reverse-flow: cross-ребро `P6o →delegate→ IMPL-SYNC` + data-ребро `IS.apply →produces→ FM`; реген + selftest зелёные); FM.md (блок `impl_sync` + lifecycle-нота writer'а), Product SPEC §3.2 (Поддерживающие операции 6→7, счётчик команд 22→23), processes.md §7.3 (обратный поток shipped-перехода); юниты `tests/product/impl-evidence.test.cjs` (26 кейсов: oracle-reuse liveness, runs/fabric/external/handoff, coverage, V-01 + CRLF-регрессор DEC-DEV-0190, все 6 disposition-веток, scanProject-сводка + already-shipped идемпотентность, CLI round-trip) в `test:product`→`verify`-цепи. **Counts без изменений (24/44 — команды/хуки в счётчики не входят).** Consumer-zone (`commands/` + `hooks/` + `docs/`); всё аддитивно.

### Fixed

---

## [1.10.0] — 2026-07-11

**Minor release — accumulated since 1.9.1:** кампания «хвосты до PROD», Волны 0+2. **Волна 0 (D7-гигиена):** G22 handoff-staleness на стороне Integrator с персистом вердикта (DEC-DEV-0179) · G28 проактивный sweeper истёкших `approve_overrides` + SessionStart-warn (DEC-DEV-0180) · G30 детерминированный depth-floor guardrail для adaptive-depth DA (DEC-DEV-0182) · G24 session-audit path-resolve + видимый opt-in (DEC-DEV-0183). **Волна 2 (строительная):** G36 Deep Discovery субагенты `market-researcher` + `competitor-analyst` — D1 Deep стал функциональным (DEC-DEV-0186) · G21 Vision-эпики A/B/C/D/G вписаны в тело Product/Design SPEC (DEC-DEV-0187) · E6 doc-UX Волна 2: C5 «объясни путь» + B2 анатомия хаба + 3 consistency-фикса (DEC-DEV-0188) · **HIGH security-фикс: command injection в 3 PostToolUse-хуках устранён** (DEC-DEV-0189, отчёт `dev/gates/SECURITY_REVIEW_2026-07-11.md`). Counts без изменений (24/44). **Deferred (carried, not lost):** `screen-generator` (Design C2, G36-соседний cut — свой триггер не сработал) · C4 «ты здесь» (за гейтом D-7 до пилот-валидации) · runtime smoke-догоны 1.3.3 S2/S4/S5 + PHASE_6 S1/S3 + `/integrator:update cc-sdd --repair` — ближайший VM-визит.

### Added

- **doc-UX Волна 2 (E6) закрыта — интерактивная карта процессов + анатомия хаба** (DEC-DEV-0188). **C5 «объясни путь»** — новый режим карты процессов (`docs/guide/ecosystem-processes.html`): кнопка «🧭 Объясни путь» → кликаешь стартовый узел, затем цель → BFS по flow-рёбрам (поток + делегация) подсвечивает цепочку процессов/гейтов между ними (Esc — выход; переиспользует edge-highlight). Путь считается по статическим данным графа, поэтому резолвится даже при свёрнутых процессах. **B2 «анатомия одной картинкой»** — coarse Mermaid-схема (4 модуля × пайплайн D1–D6 × `handoff.md`-граница × вердикт `P3→P6`) встроена в L0 хаба `docs/guide/README.md` (обновляется только при смене топологии модулей/доменов, как `docs/MAP.md`). Правится шаблон/overlay/генератор карты, не сгенерированный HTML; `procmap.smoke.mjs` расширен ассертами C5 (путь P1A→P6o непуст, монотонен по таймлайну, направлен). Counts без изменений (24/44).

- **Проактивный sweeper истёкших `approve_overrides` — закрыт разрыв аудита G28** (DEC-DEV-0180). D2-механика overrides (`validation.md` §9.4, DEC-DEV-0012 C.5) допускает временный `approve_overrides[]` с опциональным `expires_at`; инлайн-валидатор (`artifact-validate.js` → `buildOverrideMap`) деактивирует истёкшую запись **только при чтении** конкретного артефакта — до этой поставки ничто проактивно не находило и не подметало истёкшие записи, они копились мёртвой конфигурацией во frontmatter. **Новое:** `hooks/product/lib/override-sweep.cjs` — детерминированный сканер `.product/`-артефактов + CLI. Классификация **инлайн-консистентна** (никогда не расходится с валидатором): `expired` (`expires_at` парсится И `< now` — то же условие, что деактивирует запись в `buildOverrideMap`) · `active` · `no-expiry` (нет `expires_at` → валидатор держит активным вечно) · `invalid-date` (непарсящийся `expires_at` → валидатор трактует как **активный**, поэтому это репортится как config-smell, но **не** подметается — не истёк). Поведение по умолчанию — **dry-run отчёт**; мутация только по явному флагу `--clean` (убирает **только** `expired`-записи; т.к. валидатор их уже игнорирует, удаление validation-нейтрально — не воскрешает и не роняет ни один гейт). Флаги: `--clean` / `--json` / `--strict` (exit 1 при найденных expired — CI-сигнал) / `--quiet`. **Точка вызова:** новый SessionStart-хук `hooks/product/override-sweep-check.js` (detect-only, warn через `additionalContext`) — по образцу `hooks/integrator/drift-check.js`: no-op вне `.product`-проектов (вкл. репо самой экосистемы), при найденных expired/malformed — компактная нота «прогони `override-sweep.cjs --clean`»; non-blocking (exit 0 всегда), fail-open, тумблер `PRODUCT_OVERRIDE_SWEEP=0`. Мутирующий reap живёт **только** за явным CLI, не в хуке. Wiring: manifest (bootstrap auto-регистрация SessionStart), юнит-тесты `tests/product/override-sweep.test.cjs` (15 кейсов: expired/active/no-expiry/битая-дата + clean + tree-walk + CLI round-trip) в `test:product`→`verify`-цепи; hook-smoke 43/43. **Граница скоупа (осознанно):** это точечный проактивный sweeper, НЕ cron/temporal-актуатор (Periodic §3.5 остаётся отложенным до live-триггеров фазы 4 Process Fabric). Counts без изменений (24/44 — хуки в счётчики не входят). Consumer-zone (`hooks/product/`); всё аддитивно.
- **Детерминированный depth-floor guardrail для adaptive-depth DA (G30, DEC-DEV-0182).** Adaptive-depth-модель P-RULE-01/02 (DEC-DEV-0012) позволяла subagent'у `product-devils-advocate` **самому** классифицировать каждое изменение BR/IC как `cosmetic` (→ quick-check) или `significant` (→ full 6-lens). Это чистое LLM-суждение без backstop: false-cosmetic на реально значимом изменении молча пропускал требуемый review (watchdog G05/G06 страхует **факт** спавна DA, но не выбранную им **глубину**). Добавлена чистая, юнит-тестированная либа `hooks/product/lib/da-depth-floor.cjs`: хуки `ic-change-trigger.js` / `br-change-trigger.js` сканируют тот же git-diff набором **структурных высокоточных** сигналов (`creation`, `activation`=status→active, `severity-critical`, `entity-change` [IC], `category-change` [BR]) и при срабатывании штампуют в da-pending-entry `depth_floor: significant` + `depth_floor_signals` и выдают громкий stderr-override; brief (`feature-session.md`) и агент (`devils-advocate.md`) обязаны при `Depth-floor: significant` идти в full 6-lens, игнорируя самоклассификацию. **Границы (сознательно НЕ детерминизировано):** «statement semantic rewrite» и BR «parameter TYPE vs value-tune» неотличимы регуляркой от документированных cosmetic-кейсов → остаются за adaptive-LLM. Пол только **повышает** глубину; **нет сигнала → нет поля → поведение 1:1 как до G30** (absent==старое). Fail-open (lib недоступна → пола нет). Юниты `tests/product/da-depth-floor.test.cjs` (15) в `test:product`; doc — `processes.md` §6.2. Consumer-zone (`hooks/` + `skills/` + `agents/`); counts без изменений (24/44 — guardrail, не новый validation-тип).
- **Deep mode Discovery субагенты `market-researcher` + `competitor-analyst` — D1 Deep стал функциональным (закрыт разрыв аудита G36, DEC-DEV-0186).** Точка входа `/product:init --deep` и ветка Deep в `discovery-session.md` ссылались на два субагента с изолированным контекстом (SPEC §5.1/§5.2, processes.md §14.3), но самих файлов агентов не было — Deep D1.2/D1.3 был **spec-only** с Phase 3 (deferred DEC-DEV-0012; bring-forward trigger — доведение Deep до рабочего состояния). **Новое:** `agents/product/market-researcher.md` (8-фазный пайплайн scope→plan→retrieve→triangulate→synthesize→critique→refine→package; выход — credibility-scored MR draft) + `agents/product/competitor-analyst.md` (6-стадийный пайплайн discovery→filtering→scraping→extraction→synthesis→our-positioning; выход — CA draft с feature-matrix, positioning-map, strengths/weaknesses, market gaps). Оба — **read-only + web** (`tools: Read, Grep, Glob, WebFetch, WebSearch`; **нет Write/Edit/Bash**): исследуют в изолированном контексте и **возвращают draft**, а файл `.product/{market-research,competitive-analysis}.md` (status=draft) пишет **вызывающая сессия** — так PostToolUse-хуки (`product-artifact-validate.js`, `bg-extractor.js`) срабатывают в main-сессии и BG-кандидаты попадают в очередь (тот же паттерн «advisors read-only + writer-step», что в completeness-loop). Оба несут canonical `subagent_type`-контракт (DEC-DEV-0064): «agent not found» = громкая setup-ошибка, **никакого тихого fallback в general-purpose**. Модель — `claude-opus-4-8` (MDP analysis-тир: триангуляция + credibility-scoring + позиционный синтез = «выводы по собранному», не sonnet-recon; сам backlog специфицировал opus). **Wiring:** ветки Deep в `discovery-session.md` D1.2/D1.3 конкретизированы (brief-формат + spawn + «main session writes»-контракт); **Quick-путь не тронут — absent==`--deep` → старое поведение 1:1**. Смоук `tests/product/deep-discovery-agents-wiring.test.cjs` (87 ассертов: frontmatter/name-match, opus-пин, read-only roster без Write/Edit/Bash, 8 фаз MR + 6 стадий CA в порядке, subagent_type-контракт, spawn-wiring в скилле + `--deep` в init) в `test:product`→`verify`-цепи. **Границы скоупа (сознательно):** `screen-generator` (Design C2) НЕ строился — другой cut с несработавшим триггером; отдельные Deep-скиллы `deep-research-8-phase.md` / `competitive-intel.md` (SPEC §4.5) НЕ создавались — пайплайн встроен прямо в промпты агентов (как lens'ы в advisor-агентах), лишняя lazy-load зависимость не нужна; strict MCP-требования не форсятся — при неполном стеке `init.md`/скилл деградируют в Quick. Counts без изменений (24/44 — агенты не артефакт-тип). Consumer-zone (`agents/` + `skills/` + `commands/`); всё аддитивно.

### Modified

- **G21 закрыт — Vision-эпики A/B/C/D/G вписаны в тело Product/Design SPEC, дрейф-баннеры сняты** (DEC-DEV-0187; остаток DEC-DEV-0170, который «промаркировал, не вписал»). Построенная Autonomous-Pipeline-машинерия жила в SPEC-ах только заголовочными ⚠-баннерами «построено, но в тело не вписано»; теперь описана связно. **`docs/product-module/SPEC.md`:** баннер → pointer-строка; новая **§2.4** (архитектура completeness-loop: `completeness-oracle` + zone→persona `zone-router` + опциональный `agent-roster`-слой + consilium + durable-runners + рельс каноничности персон), **§3.2b** (`/product:consilium` — Epic D, `/product:batch-enrich` — Epic C-i) + обогащён `/product:complete` (Epic B), **§5.4** (profile-персоны `architect-advisor`/`qa-advisor`/`ux-advisor`), **§6.8** (`zone-change-trigger.js`); описательные счётчики под факт (команды 20→22, subagents +3, hooks 8→12). **`docs/design-module/SPEC.md`:** баннер **расщеплён** — половина «код впереди SPEC» (`ux-advisor`) вписана (§2.1/§5.2/§6-нота, с исправлением пути `agents/product/`→фактический `agents/design/ux-advisor.md`); половина «SPEC впереди кода» (`screen-generator`, **G36**) оставлена честным суженным баннером + §5.1 помечен «НЕ построен». **Документационная ревизия, не spec-change поведения:** нормативные контракты (canonical fields, `24` типа артефактов / `44` правила) не тронуты — `check-counts.js` их и не сторожит (сверяет только артефакты/правила), зелёный. Consumer-zone (`docs/`); Integrator SPEC не тронут (G21-баннера там нет).

### Fixed

- **Command injection в 3 PostToolUse-хуках Product-модуля (`br`/`ic`/`zone-change-trigger.js`) — устранён** (DEC-DEV-0189; находка H-1 систематического security-прохода `dev/gates/SECURITY_REVIEW_2026-07-11.md`). Хуки авто-исполняются в проекте пользователя на каждом Write/Edit в `.product/` и строили git-diff команду **шелл-строкой** с интерполяцией пути артефакта (`execSync(\`git … -- "${relPath}"\`)`). Имя артефакта проходит только фильтр `…/[^/]+\.md$`, который допускает `$()`/backtick/`;` в теле имени → на POSIX подстановка команд внутри двойных кавычек вычисляется, и файл вида `.product/business-rules/x$(…).md` (создаваемый агентом под prompt-injection) исполнял произвольную команду **без подтверждения**. Фикс: переход на `execFileSync('git', ['-C', projectRoot, 'diff', 'HEAD', '--', relPath], …)` — без шелла, метасимволы передаются дискретными argv-элементами. Поведение диффа 1:1 сохранено (функциональный тест + injection-PoC + hook-smoke 43/43). Consumer-zone (`hooks/product/`); counts без изменений (24/44).
- **3 consistency-фикса словаря-аудита E2 (DEC-DEV-0188; surfaced-deferred из DEC-DEV-0139).** **①** Гайд: в `docs/guide/05-implementation.md §1` добавлена разводка «почему `P3–P6`, а не `P1–P7`» — полное семейство рантайм-процессов Orchestrator `P1–P7`, оператор запускает середину `P3–P6` (P1/P2 — внутренняя подготовка, **P7 runtime-smoke — детерминированная нога внутри вердикта P6**), со ссылкой на «Оси именования» глоссария; снят рассинхрон диапазонов в гайде. **②** Node-id карты процессов: product-lane процессы получили `p`-суффикс (`P4`→`P4p`, `P5`→`P5p`) — снята коллизия с orchestrator `P4o`/`P5o`, карта приведена к уже-задокументированной в глоссарии/overlay конвенции (реген карты + smoke-гард на переименованные id). **③** Membership уровней ревью: `NFR`=🟠 Strategic и `AM`=🟢 Confirmation добавлены в таблицы `docs/guide/00-concepts.md §5` и `docs/guide/06-gates.md §1` (совпали с SSOT glossary/overlay — раньше опускались). Consumer-zone (`docs/guide/`); counts без изменений (24/44).

- **G22 — handoff staleness на стороне Integrator: пересчёт хэшей теперь реально выполняется и персистится** (DEC-DEV-0179; разрыв аудита APPENDIX-B / GAPS §70). handoff-spec §10/§13 обещали, что при использовании handoff Integrator'ом (`add`/`update`) и при `/integrator:verify` embedded `artifact_hashes` пересчитываются от `.product/` и расхождение помечает handoff `stale` — но шага пересчёта в `add.md`/`update.md` не было (grep пуст), а единственный drift-сигнал жил в Product-зоне (`product-handoff-gate.js`, PostToolUse) и был **stderr-эфемерным** (не персистился). Новая тонкая либа `hooks/integrator/lib/handoff-staleness.cjs`: сканирует `.product/handoffs/*-handoff.md`, резолвит каждый embedded-артефакт по `id` в `.product/`, **переиспользует hash-SSOT `hooks/product/lib/hash.js`** (тот же SHA-256 / strip-frontmatter / LF-normalize — не дублирует алгоритм) и сравнивает; вердикт per-handoff = `fresh`/`stale`/`error`, персистится в **Integrator-зону** `.claude/integrator/handoff-staleness.yaml`. **Read-only относительно `.product/`** — Integrator не пишет `status: stale` в frontmatter самого handoff (флаг живёт в снапшоте; регенерацию инициирует владелец через `/product:handoff <FM-id> --regenerate`, Product-действие). Wiring: `/integrator:add` + `/integrator:update` вызывают либу с `--write` (persist), `/integrator:verify` читает снапшот + считает fresh (без записи — сохранён single-write-инвариант verify). Detect-only, tolerant (нет `.product/handoffs/` → no-op с нотой), exit 0 всегда. Тесты: новый `tests/integrator/handoff-staleness.test.cjs` (12 юнитов, вкл. регрессор «frontmatter-only bump НЕ флипает stale» + «никогда не пишет `.product/`»), подключён в `test:integrator`; полный verify EXIT=0. Consumer-zone (`hooks/` + `commands/` + `docs/`); counts без изменений (24/44).
- **G24 — D7 Session Audit: хрупкий opt-in + хардкод пути хука** (DEC-DEV-0183). **(1) Path-resolve (leg f):** `session-audit.js` при переезде репо экосистемы **тихо** переставал писать маркеры (абсолютный путь хука в `settings.local.json` пилота протухал → `node` не находил скрипт). `findRepoRoot` теперь резолвит `$ECOSYSTEM_ROOT` (override) → cwd-walk → расположение самого скрипта, а при неудаче пишет **громкий** stderr с ремедиацией вместо тихого skip (неверный `$ECOSYSTEM_ROOT` тоже warn'ит). Резолвер вынесен в тестируемый модуль (юниты `tests/audit/session-audit-resolve.test.cjs`, оба кейса — валидный и «переехавший»; подключены в `test:audit`). **(2) Opt-in видимость (leg d), БЕЗ смены поведения всех пилотов:** `/ecosystem:bootstrap` получил **default-OFF** шаг 11.5 — явно предлагает включить D7-аудит только если это ecosystem-dev пилот (идемпотентный вызов `/ecosystem:enable-d7-audit`); а `/meta:audit-smoke` на пустом Pending теперь печатает **диагноз** (opt-in не включён / путь протух после переезда + как проверить) вместо безликого no-op. `enable-d7-audit.md` документирует `ECOSYSTEM_ROOT` и re-run-при-переезде. (SessionStart-warn в репо экосистемы сознательно отвергнут — dev-хуки регистрируются в gitignored-конфиге, был бы непортируем; см. DEC-DEV-0183.) Consumer-zone (`commands/ecosystem/`); counts без изменений (24/44).

---

## [1.9.1] — 2026-07-11

**Patch release — accumulated since 1.9.0:** итоги batch-прогона 4 смоук-планов (DEC-DEV-0177, судья: 14 PASS / 3 PARTIAL / 5 N/A / 0 FAIL — *агрегаты исправлены 2026-07-13, D13 / DEC-DEV-0197: ранее здесь стояло ложное `11 / 2 / 6` из ошибочной самосводки судьи; per-сценарные вердикты не менялись*) конвертированы в поставку: **LESSON-гейт PreToolUse флипнут warn→strict** (решение владельца; условие — S-LE ре-прогон PASS фикса 0143) + **три follow-up фикса** (DEC-DEV-0178): DEF-SMK-1 (drift-оси видят реальную схему через CNT-контракты), ложный C-03 (whitelist → `v1.x`), деплой adapter-фикстуры в `.claude/adapters/fixtures/`. Counts без изменений (24/44). **Post-delivery шаг в пилоте:** `/integrator:update cc-sdd --repair` — докатить C-03-фикс до instance-адаптера (D2/D3 покажут drift reference↔instance).

### Changed

- **LESSON-гейт PreToolUse (`lesson-presence-gate.js`) переведён из warn в strict по умолчанию** (решение владельца 2026-07-11 после S-LE ре-прогона; DEC-DEV-0177). Условие флипа из `S_LE_LESSON_GATE_SMOKE.md` выполнено по существу: специфический блокер — exemption-самодедлок протокола `/product:lesson` — устранён target-carve-out'ом DEC-DEV-0143 и **live-подтверждён на ре-прогоне** (deny + exemption PASS, VM-пилот, CC 2.1.205): при open-уроке мутирующие вызовы отклоняются с видимой причиной, а сам протокол разрешения пишет маркер и цели `.product/lessons/**` беспрепятственно. Известный caveat (задокументирован, не блокер): S-LE.1 Stop-prong'а — `preventedContinuation=false` под bypassPermissions (feedback-нога работает; ограничение CC-runtime, не хука). Откат без кода: `LESSON_GATE_MODE=warn`. Wiring: дефолт в хуке + manifest-description; hook-smoke 43/43 (strict-кейсы 0143 покрывают новый дефолт). Consumer-zone (`hooks/product/`); counts без изменений (24/44).

### Fixed

- **Follow-up фиксы smoke-batch 2026-07-11 (DEC-DEV-0178; дефекты — DEC-DEV-0177 / бриф §Outcome).** **(1) DEF-SMK-1** (`hooks/integrator/lib/drift-checks.cjs`): drift-оси D1/D2/D3 были слепы на реальном пилоте — либа ждала поле `adapter` в `active-tools.yaml`, которого в реальной схеме нет. Теперь адаптеры резолвятся из контрактов `CNT-*.yaml` (`consumer` → `transformation.script`, только `type: adapter_script`; толерантный парсер, битый контракт → skip) ∪ legacy-поле `adapter`; multi-adapter = worst-of по осям; страховочная сетка — unattributed-пары файлов `adapters/` reference↔instance проверяются отдельной строкой отчёта. Форма JSON-выхода не изменена (контракт `verify.md`). Юниты 24→**33** (фикстуры — РЕАЛЬНОЙ формы, урок 0177; регрессор: drift ловится на схеме без `adapter`); smoke-кейс `drift-injected` переведён на реальную форму; hook-smoke 43/43. **(2) C-03 ложный warning** (`adapters/handoff-to-ccsdd.js`): whitelist генераторов обрывался на v1.4 → каждый handoff 1.5+ пилотов ловил ложный C-03 (live: FM-006, DEC-INT-0013). Ре-верификация выполнена live (Stage-6 контракт-тест против реального v1.6-handoff, DEC-INT-0014) → регекс расширен до `product-module-v1.x`; мажор (v2+) остаётся warning; контракт-тест 4→**5** (регрессор v1.6/v1.9 pass + v2.0 warn). Доставка в instance пилота: `/integrator:update cc-sdd --repair` (D2/D3 покажут drift reference↔instance после `/ecosystem:update`). **(3) Bootstrap-гэп фикстуры**: Stage-6 verify ссылался на repo-only путь `tests/fixtures/FM-FIXTURE-001-handoff.md`, который в проект не доставлялся — теперь `/ecosystem:bootstrap` (2d) и `/ecosystem:update` (Step 5.2) деплоят `FM-FIXTURE-*.md` в `.claude/adapters/fixtures/`; пути в `add.md`/`update.md` обновлены (fallback для pre-1.10 инсталляций — реальный handoff). Consumer-zone (`hooks/` + `adapters/` + `commands/`); counts без изменений (24/44).

---

## [1.9.0] — 2026-07-11

**Minor release — accumulated since 1.8.1:** **Integrator Phase 7 (maintenance) построен** — `/integrator:verify` + `/integrator:debug` + `/integrator:docs` + проактивный drift-слушатель `drift-check.js` (SessionStart) + разделяемая либа `hooks/integrator/lib/drift-checks.cjs` (DEC-DEV-0176; закрывает G14 на 3/4, G15 полностью, G16 по существу; cuts v1.1+ = `:replace` / `contract-validate.js` / `--light`) + фикс DEF-OD7-CLOSE — charter v4 с owner-выходом со всех парковочных гейтов (DEC-DEV-0175). Counts без изменений (24/44). **Deferred (carried, not lost):** runtime smoke Phase 7 S1-S5 (`dev/gates/PHASE_7_SMOKE_TEST_PLAN.md`) — этот релиз и режется ради batch-прогона 4 накопленных смоук-планов на VM-пилоте; R6/`evt:env.up` — event-gated.

### Added

- **Integrator Phase 7 — maintenance-группа модуля построена: `/integrator:verify` + `/integrator:debug` + `/integrator:docs` + проактивный drift-слушатель** (DEC-DEV-0176; закрывает разрывы аудита G14 на 3/4 команды, G15 полностью, G16 по существу). **`/integrator:verify`** — read-only health-check Integrator-зоны (граница с `/ecosystem:verify` = целостность инсталляции vs runtime-здоровье зоны): tools реально резолвятся, контракты CNT-* валидны (вкл. dangling-adapter), `pmo-mapping.yaml` без orphan'ов в обе стороны, adapter-drift D1/D2/D3; рекомендует (`update --repair`/`debug`), НЕ чинит; единственная запись = штамп `last_audit` per-tool. **`/integrator:debug <error> [--tool]`** — one-shot диагностика по SPEC §7.3 (journal lookup → contract check → гипотеза → **approve-gate y/n/details** → fix → regression → журнал `DEC-INT-NNNN`, вкл. `entered/cancelled`); при systematic-классе явно роутит в `/product:validation-tune` propose confidence-downgrade — недостающая точка входа confidence-lifecycle SPEC §4.4 (G15). **`/integrator:docs [--tool|--tool=all]`** — генерация operating manual `.claude/integrator/tool-docs/<tool>.md` через существующий скилл tool-docs-generator (структура SPEC §14.2, universal English); `<!-- manual: -->`-блоки переживают реген (§14.4); без активных tools — честный отказ. **Drift-инфра:** разделяемая либа `hooks/integrator/lib/drift-checks.cjs` (D1 semver / D2 CONTRACT_SCHEMA_VERSION / D3 header-stripped body / staleness >90d; local-only модель DEC-DEV-0045, tri-location; detect-only, exit 0 всегда, CLI-seam `--json`) + `hooks/integrator/drift-check.js` — SessionStart-хук: no-op вне Integrator-проектов, при drift/staleness — компактная additionalContext-нота «прогони /integrator:verify»; warn-only, fail-open, тумблер `INTEGRATOR_DRIFT_CHECK=0`. **CUT → v1.1+ (bring-forward triggers в DEC-DEV-0176):** `/integrator:replace` (нужен 2-й D2-Tech инструмент) · `contract-validate.js` · `verify --light`/periodic · profile/cross-tool drift. Wiring: overlay ×3 + карты/каталог regen (46→49 команд), manifest (bootstrap auto-регистрация), новые `tests/integrator/` (drift-checks 24 юнита; `test:integrator` в verify-цепь), hook-smoke 41→43; попутно **`gen:catalog:check` подключён в `npm run verify`** (каталог `02-commands.md` был фактически stale при доке, заявлявшем гейт — приведено к заявленному контракту). Runtime smoke — `dev/gates/PHASE_7_SMOKE_TEST_PLAN.md` (S1-S5, next VM-визит). Counts без изменений (24/44 — команды в счётчики не входят). Consumer-zone (`commands/` + `hooks/` + `docs/`); всё аддитивно.

### Fixed

- **DEF-OD7-CLOSE — у парковочных гейтов не было owner-выхода: `evt:owner.close` теперь обрабатывается со ВСЕХ human-gate парковок (charter v4)** (DEC-DEV-0175; вскрыт live R1 re-run'ом 2026-07-11: движок отклонил закрытие линии из `awaiting_capability_impl` — charter v3 дал терминал `closed_without_runtime` только из `escalated`, у await-гейтов единственный выход = resume-событие, т.е. закрыть запаркованную линию без фейкового резолва или двух семантически ложных forced-событий было НЕЛЬЗЯ; executor честно отказался и оставил линию в honest-resting-state). Charter v4: `evt:owner.close → closed_without_runtime` добавлен в `awaiting_product` / `awaiting_capability` / `awaiting_capability_impl` / `runtime_gate_retry` (в `escalated` был с 0174); derived resume-события всех гейтов НЕ изменены (pa-first порядок deriveResumeEvent; юнит-регрессор проверяет все пять гейтов + end-to-end закрытие capability-парковки). Owner-гард ANOM-OD7-2 действует: close по-прежнему требует PA-фиксации решения владельца (`--force-manual "PA-NNN: …"`). Wiring: `run.md` §owner-decision events, `07-fabric.md` §6. Юниты fabric-engine 49→**50**; полный verify EXIT=0. Consumer-zone (`orchestrator/` + `commands/` + `docs/`); аддитивно, counts без изменений (24/44). Контекст: **R1 re-run = PASS судьи → OD7 live-валидирован по совокупности** (SPEC OD7-строка + GAPS G04 обновлены; вердикт — `dev/process-fabric/OD7_R1_RERUN_BRIEF.md` §Outcome; остаток R6/`evt:env.up` — event-gated).

---

## [1.8.1] — 2026-07-11

**Patch release — accumulated since 1.8.0:** **контур OD7 await→resume построен, live-валидирован до условного GO судьи и закалён фиксами всех вскрытых дефектов.** Состав: payload-мост ingest→PA + charter-парковка `awaiting_capability_impl` + resume брекет-ре-раном (DEC-DEV-0171); live-прогон на VM-пилоте 2026-07-10 — условный GO (R2-R5/R7/R8 PASS; отчёты `dev/process-fabric/OD7_LIVE_RUN_LOG.md` + `OD7_LIVE_RUN_GRADE_REPORT.md`, журнал 0173); фиксы дефектов прогона (DEC-DEV-0174): ingest bracket-guard — ledger-backed `--run-id` обязателен (DEF-OD7-2), charter v3 — fallback `blocked+go_gate:null` → manual_verify (DEF-OD7-3) + терминал `closed_without_runtime`/`evt:owner.close` (ANOM-OD7-1), capability-probe читает `.env`/`.env.local` + `env_source` (DEF-OD7-1), owner-гард на голый tick `evt:owner.*` (ANOM-OD7-2). Counts без изменений (24/44). **Deferred (carried, not lost):** R1 re-run целевым путём `/orchestrator:run --fabric` (снимет единственный FAIL-по-букве R1); ветка `runtime_gate_retry`/`evt:env.up` — event-gated.

### Added

- **OD7 await→resume построен — асинхронный контур «capability-запрос → durable-парковка → provision → продолжение с места остановки» замкнут через Process Fabric** (DEC-DEV-0171; SPEC Orchestrator OD7 — был «гипотеза RUN 01, не проверена»; gap G04 аудита). Три аддитивных куска. **(1) Payload-мост движка** (`orchestrator/lib/fabric-engine.cjs`): charter ingest-правило получает опциональный `payloadPath` (dot-path) — срез result-файла процесса едет payload'ом события в `events.ndjson` и, если тик паркует human-gate, **врезается fenced-json секцией в PA-запись гейта** (транкация >2000 симв. с указателем на полный payload в events; machine-маркеры pa-scan не задеваются) — владелец/Integrator видят ЧТО провижнить (secret_env, routes), а не только «линия стоит»; новая чистая `ingestEmits()` → `[{event,payload}]`, `applyIngest` — обратно-совместимая обёртка; правило без `payloadPath` — бит-в-бит старое поведение. **(2) Charter v2** (`feature-production-line.json`): новое состояние-парковка `awaiting_capability_impl` (resume `evt:pa.resolved` → `implementing`), событие `evt:impl.blocked_capability`, ingest-правило P5 по новому полю + `payloadPath: "capability_blocked"`; P7-правило `BLOCKED_ON_CAPABILITY` несёт `payloadPath: "requests"`. **(3) P5-возврат** (`feature-to-tdd-impl.mjs`): новое поле `capability_blocked` = §6-items с disposition BLOCK (жёсткие блокеры без dev stand-in; deferred не паркуют линию). **Resume = обычный полный брекет-ре-ран**: план-стадия P5 уже фильтрует `done`-таски из tasks.md → линия продолжает с заблокированных тасков, не с нуля (mid-process resume без спец-механики восстановления; ScheduleWakeup-вариант ожидания из SPEC отброшен — durable-парковка Fabric переживает сессии). Контракт диспетчера — `run.md` (§prescription human-gate + §resuming); SPEC OD7-строка → «построено, live-валидация pending»; `07-fabric.md` §4. **Live-валидация подготовлена, не прогнана:** пре-регистрированный бриф `dev/process-fabric/OD7_LIVE_RUN_BRIEF.md` + рубрика судьи `OD7_LIVE_RUN_REVIEW_HANDOFF.md` (VM-прогон, класс B + A-критерий границы). Тесты: fabric-engine 35→**40**, wiring 11 + P5-wiring 13 зелёные; полный verify EXIT=0. Consumer-zone (`orchestrator/` + `commands/` + `docs/`); аддитивно, counts без изменений (24/44).

### Fixed

- **Дефекты OD7 live-прогона (условный GO судьи 2026-07-10): ingest bracket-guard DEF-OD7-2, charter-гэп `blocked+go_gate:null` DEF-OD7-3, probe-presence из `.env` DEF-OD7-1 + дизайн-решения ANOM-OD7-1/2** (DEC-DEV-0174; SSOT дефектов — `dev/process-fabric/OD7_LIVE_RUN_GRADE_REPORT.md` §Рекомендации, журнал 0173). **DEF-OD7-2 [P0]** (`orchestrator/lib/fabric-engine.cjs`): `ingest` получает bracket-guard — близнец tick'ового DEF-3 (0163) с ledger-стороны: `--run-id` теперь ОБЯЗАТЕЛЕН и обязан существовать в run-ledger (`<fabric-root>/../runs/ledger.ndjson`; override `--ledger-file`) — результат raw-Workflow-запуска вне брекета (дыра прогона: `wf_c6a17829-426` был принят) в фабрику больше не заходит; аудируемый эскейп — тот же rec#4-контракт `--force-manual "PA-NNN: …"` (PA обязана существовать; маркер `forced-manual:` штампуется в `why[]` тиков); дедуп по run_id сознательно ПЕРЕД гардом (повтор записанного рана — чистый no-op, не пере-суд); общая валидация эскейпа вынесена в `requireForceManual()`. **DEF-OD7-3 [P1]** (charter `feature-production-line.json` v3): ingest-правило-fallback P5 `blocked` непуст → `evt:impl.manual_verify` (→ `escalated`, queue_owner) — исход «все таски заблокированы, гейт не бежал» (`blocked+go_gate:null`, live-парковку пришлось эмитить вручную, events seq 10) теперь машинный; прецедентность сохранена (conflicts > capability_blocked > явный go_gate > blocked-fallback). **DEF-OD7-1 [P1]** (`orchestrator/lib/capability-probe.cjs`): presence секрета читается из `process.env` ∪ `.env` ∪ `.env.local` под `--root` (dotenv-lite парс, только непустые значения; значения секретов не выводятся) — ложноположительный BLOCK на реально оснащённой фиче (OPENAI_API_KEY лежал в `.env` весь прогон, оба executor'а это независимо вскрыли) устранён; новое поле `env_source` дисклозит удовлетворивший источник. **ANOM-OD7-1 [P2]** (charter v3): новый терминал `closed_without_runtime` + `evt:owner.close` из `escalated` — канонический выход «владелец сплитнул/отложил остаток, автолиния завершена без runtime» (live пришлось перегружать `owner.abort`, хотя линия не провалена). **ANOM-OD7-2 [P2]** (`fabric-engine.cjs`): голый `tick` любого `evt:owner.*` отказывается (executor не проецирует owner-решение сам — live-разрыв границы: executor тикнул `owner.abort`/`manual_verify` по собственной интерпретации сплита); санкционированный путь — PA-флип владельца + `pa-scan --tick` (не тронут), ручной owner-тик — только `--force-manual "PA-NNN: <owner decision>"` с PA, фиксирующей решение. Wiring: `run.md` (гард в брекет-блоке + секция owner-decision events), help движка/пробы. Юниты: fabric-engine 40→**49** (гард ingest ×4, charter-fallback ×2, owner-гард/close/pa-scan-регрессия ×3), capability-probe 12→**15**; полный verify EXIT=0. Consumer-zone (`orchestrator/` + `commands/`); charter-изменения аддитивны (v3), counts без изменений (24/44). R1 re-run на VM целевым путём `/orchestrator:run --fabric` — следующий шаг (снимет «FAIL по букве» единственного критерия).

---

## [1.8.0] — 2026-07-10

**Minor release — accumulated since 1.7.0:** **Process Fabric — межпроцессный statechart-координатор, построен и GRADUATED на live-прогоне VM-пилота 2026-07-10** (ядро `fabric-engine.cjs` + charter `feature-production-line` 0153; диспетчер-актуатор `--fabric`/`--autonomy` в `/orchestrator:run` 0154; PA-мост `pa-scan --tick` + первый SessionStart-хук 0155; consumer-док оператора `docs/guide/07-fabric.md` 0166; live-фиксы прогона — hookEventName-инжект 0162 + DEF-3 bracket-guard 0163; upstream-долг DEF-4 workspace-скан / ANOM-5 owner-queue dequeue / `--force-manual`→PA 0168); **E5-волна Autonomous Pipeline Vision** (жюри-подготовка эскалированных решений `/product:consilium` 0145; макро-батч обогащения `/product:batch-enrich` 0150; agent-roster + participation-matrix слоем над zone-router 0151); **наблюдаемость + MDP-дисциплина** (run-ledger durable-сводок оркестратор-прогонов 0147; 56 model-пиновок во всех Workflow-процессах + смоук-гейт 0146; позиционные doc-якоря vibe→agentic 0144); **слой доменной экспертизы + гейт Domain Fit D1.0b на входе идеи** (реестр 96 подкатегорий × C1–C10, порог 75, owner-override, режимы discovery/assess 0169; handoff-проброс `limiters` + migration-промпт backfill 0172); **`subagent-watchdog`** — первый SubagentStop-хук (pending-review очереди G05 + каноничность персона-спавнов G06) 0159; фиксы: LESSON-гейт self-deadlock carve-out 0143, счётчики шаблона + templates-скан реконсилятора 0156, каталог↔раннер валидации + линтер `check:validation-sync` 0158, doc-drift quick-wins G17/G18/G21/G33/G35 0170. Counts: **без изменений 24/44** (всё аддитивно). **Deferred (carried, not lost):** live-валидация гейта D1.0b на реальном `/product:init` + одноразовый migration-промпт `domain_fit` в пилоте (записи 0169/0172 ниже); флип LESSON-гейта warn→strict до PASS live-ре-прогона S-LE.1/S-LE.3; B-d real-resolve калибровка wave-runner; runtime smokes `PATCH_1.3.3 S1–S5` + `PHASE_6 S1–S7` (next pilot session); live-хвосты E5 (consilium на реальном форк-PA; batch-enrich ≥2 FM; roster-override на пилоте).

### Added

- **Domain Fit → handoff: advisory-проброс `limiters` внешнему реализатору + migration-промпт backfill для существующих проектов** (DEC-DEV-0172; follow-up 0169 по согласованному перечню остатков). Handoff теперь несёт зону ответственности спеки: если в `product.yaml.domain_fit` вердикт задан И `limiters` непуст (типовой случай — `decision: proceed-with-risks`), `handoff-generator.md` эмитит строку `Domain fit:` в §1 Executive Summary + машиночитаемый frontmatter-блок `domain_fit` (subcategory/score/verdict/decision/limiters) + bullet в §12 Integration context — receiver явно видит, **что осознанно за скобками поведенческого контура** (родственно §13 Out of Scope, но уровнем продукта; «не реализовывать на всякий случай»). **Чистый fit не эмитится вовсе** (нет downstream-сигнала; отсутствие блока = поведение 1:1 до 0172); вердикт/баллы на handoff-тайме не пересчитываются — derive из SSOT `product.yaml`. Тот же паттерн эмиссии, что `product_class` (0079), включая anti-pattern поля в Step 9. Wiring: `handoff-spec.md §1/§5/§12`, `handoff-generator.md` Step 2/8/9, `domain-expertise.md §7/§8`. Migration-промпт для проектов с уже пройденным Discovery — в записи 0169 ниже. Consumer-zone (`docs/` + `skills/`); counts без изменений (24/44).

- **Слой доменной экспертизы + гейт Domain Fit (D1.0b) на входе идеи в Discovery** (DEC-DEV-0169; данные — из оценки универсальности 2026-07-10, `dev/universality-assessment/REPORT.md`). Новый промежуточный этап `/product:init`: идея классифицируется до подкатегории реестра доменной экспертизы (**96 подкатегорий** A1..P4, максимальная детализация, правило «по ядру ценности, не по обвязке»; `product_class.archetype` из D1.0 — подсказка) → lookup совокупного балла (0–100) → вердикт по порогу **75**: `fit` (одна строка, без паузы) / `conditional-fit` / `misfit` (стоп: разбивка по критериям + ограничитель из реестра + варианты `adapted`·`proceed-with-risks`·`aborted`) / `unmapped` (домен вне реестра — advisory, НЕ блокирует; зеркало `archetype: other`). **Единственный класс-гейт экосистемы** — в отличие от advisory `product_class` (0079); owner-override всегда легален и фиксируется письменно (`decision` + `limiters`/`notes`). Гибриды — декомпозиция на компоненты `core`/`supporting`, вердикт по ядру. Новые файлы: `docs/pmo/domain-expertise.md` (SSOT концепта: гейт-семантика, отличие от product_class, протокол обновления реестра — точечная правка баллов запрещена, только переоценка методом), `docs/pmo/domain-expertise-registry.md` (матрица 96×C1–C10 + агрегаты + ограничители, consumer-копия оценки), `skills/product/domain-fit.md` (режимы `discovery`/`assess`, канонический блок + anti-pattern поля). Вердикт — опциональный блок `domain_fit` в `product.yaml` (soft-миграция по прецеденту 0079: отсутствие = поведение 1:1 как раньше; backfill — режим `assess`). Wiring: `discovery-session.md` шаг D1.0b + state-таблица, `init.md` (next_steps/делегирование/summary), `bootstrap.md` Step 7 (шаблон блока), `processes.md` P1.A пре-шаг 0 (попутно закрыт дрейф: D1.0 из 0079 там отсутствовал). Consumer-zone (`docs/` + `skills/` + `commands/`); документы концепта — не артефакт-типы → counts без изменений (24/44).

  **Migration — existing projects (Discovery пройден до 0169):** после `/ecosystem:update` блок `domain_fit` в `product.yaml` отсутствует. Одноразовый промпт в проекте продукта (skill выведет «что за продукт» из существующих артефактов, ты подтверждаешь классификацию; гейт-семантика та же — ниже порога получишь варианты решения, «остановиться» здесь означает лишь зафиксировать misfit в блоке, идущий проект не прерывается):

  ```text
  Load .claude/skills/product/domain-fit.md in assess mode and fill the
  domain_fit block in .claude/product.yaml: derive "what the product is" from
  my existing .product/ artifacts (PS, MVP scope, FM titles) + product_class,
  confirm the registry subcategory with me, look up the score, apply the
  gate threshold and record my decision. Do not change any artifacts.
  ```

- **`docs/guide/07-fabric.md` — consumer-док Process Fabric: руководство оператора по линии фичи между сессиями** (DEC-DEV-0166; пост-graduation обязательство №1 из `dev/process-fabric/EXECUTION_ROADMAP.md`). Восьмой док USER-слоя (How-to · Explanation): что такое charter/инстанс/событие/prescription, как завести линию (`--fabric`), как читать `status` и единую owner-queue, как разрешить человеческий гейт через PA-мост (`Status: done` → `pa-scan --tick`), страховки движка (bracket-guard/`--force-manual`, идемпотентность по run-id, floor F1, `replay`), чеклист «как добавить процесс в charter» (ingest-маппинг по фактическим полям result-файла) и честные границы (что live-валидировано 2026-07-10, что нет). Wiring: строка роутера «Я хочу…» + таблица ролей в `docs/guide/README.md` (anti-orphan чекер `check:doctype` зелёный), секция-указатель §6 в `05-implementation.md`, Fabric-указатель + актуализация ORC-узла (P1–P7) в `docs/MAP.md`, секция «Process Fabric» + дополненное дерево `lib/`+`charters/` в `orchestrator/README.md`. Consumer-zone (`docs/` + `orchestrator/README`); только документация → counts без изменений (24/44).

- **`subagent-watchdog` — первый SubagentStop-хук экосистемы: детерминированный сенсор над pending-review-очередями (G05) и каноничностью персона-спавнов (G06)** (DEC-DEV-0159; gaps из `dev/process-fabric/audit/APPENDIX-B-gap-analysis.md`). Новый `hooks/product/subagent-watchdog.js`, три pronga одним файлом: **SubagentStop** — завершившаяся каноническая персона (product-devils-advocate / architect-advisor / qa-advisor / ux-advisor) фиксирует потребление artifact-ID из своего spawn-prompt (хвост транскрипта главной сессии) в sidecar `.product/.pending/.watchdog-state.json` — producer-yaml'ы НЕ мутируются (их форматтеры whitelist'ят поля); персона-бриф, исполнившийся под general-purpose/claude → громкий stderr **«S8 P1 REGRESSION — ревью не валидно, respawn каноническим типом»** (recurring regression DEC-DEV-0038/0043 наконец получил слой детекции, не только prompt-guard'ы); **PostToolUse (Write|Edit на da/advisor-pending.yaml)** — запись исчезла без зафиксированного ревью → громкий stderr (сигнатура live-инцидента `1ff552c0c6b4`); **Stop** — незакрытые ревью-обязательства на закрытии сессии → напоминание. Ключ (artifact, queued_at): re-queue сбрасывает потребление. Warn-only (exit 0 всегда; lesson-gate остаётся единственным blocking-хуком), fail-open, тумблер `SUBAGENT_WATCHDOG=0`. Riders: `settings.json.template` pre-seed `"SubagentStop": []`; manifest-entry (bootstrap Step 6b подхватит). Smoke +5 кейсов, hook-smoke 36→**41/0**; полный verify зелёный. Consumer-zone (`hooks/` + root template); аддитивно → counts без изменений (24/44).
- **Process Fabric фаза 2 (2b+2c) — PA-мост замыкает обратный контур G03/G04 + первый shipped SessionStart-хук `fabric status`** (DEC-DEV-0155; по `dev/process-fabric/EXECUTION_ROADMAP.md`). **2b:** applied-тик, паркующий инстанс в human-gate-состоянии, теперь зеркалится engine-shell'ом канонической записью в `.claude/pending-actions.md` (schema `user-action-tracker`; машиночитаемые маркеры `fabric-instance`/`fabric-state`/`resume-event` в Details; дедуп (instance, state, pending); create-if-absent с PA-000 sentinel; `--pa-file` override) — и новая подкоманда **`fabric-engine.cjs pa-scan [--tick]`** закрывает резолюционную половину: PA с `Status: done` → детерминированный tick записанного `resume-event` (идемпотентные скипы: инстанс исчез / ушёл из состояния / charter не держит событие), **`dismissed` → только surfaced** (abort vs resume — решение владельца). PA — shell-side-effect ВНЕ event-sourcing (`replay` бит-в-бит не зависит). Контур «гейт → PA → done-флип → pa-scan --tick → линия продолжена» замкнут end-to-end машинно (генерализация lesson-gate-паттерна: детерминированный слушатель на ОБОИХ концах очереди). **2c:** `hooks/orchestrator/session-fabric-status.js` + `manifest.yaml` — **первый shipped SessionStart-хук экосистемы**: warn-only (exit 0 всегда, 15s timeout, тумблер `FABRIC_STATUS_INJECT=0`), read-only шелл `fabric status`, инжект компактного `additionalContext` (инстансы + топ-5 приоритизированной owner-queue, cap 8k); no-op без fabric-state. Riders: `settings.json.template` pre-seed `"SessionStart": []` (прецедент LESSON-*); `update.md` regex ecosystem-owned хуков += `orchestrator/`. **2e (charter №2 product-front) срезан** до прохождения graduation (substrate-дисциплина DEC-DEV-0148) — отложен до фазы 4 по live-триггеру. Тесты: fabric-engine 23/23 (+5 PA), wiring 11/11, hook-smoke 34→**36/0**; полный verify зелёный. Consumer-zone (`orchestrator/` + `hooks/` + `commands/` + root template); аддитивно → counts без изменений (24/44).
- **Process Fabric фаза 2 (2a+2d) — диспетчер-актуатор в `/orchestrator:run` + F1 end-to-end: `--fabric`/`--autonomy`, переезд `autonomy-policy.cjs` в `orchestrator/lib/`** (DEC-DEV-0154; по `dev/process-fabric/EXECUTION_ROADMAP.md`). `commands/orchestrator/run.md` получает секцию «Process Fabric» сразу после run-ledger-wiring — диспетчер становится первым живым актуатором ядра 0153: **opt-in `--fabric`** заводит линию (init по deployed-пути `.claude/orchestrator/charters/feature-production-line.json` + `tick evt:line.start`; отклонённый старт = **FB-004 backpressure**, не ошибка — вторую линию не форсировать), **после каждого `run-ledger finish`** тот же result-file и `$RUN_ID` уходят в `fabric ingest` (идемпотентный мост), полученная prescription маршрутизируется: `disposition: auto` → следующий процесс своим полным bracket'ом (ledger start → Workflow → finish → ingest — машинное продолжение линии), `human-gate` → приоритизированная owner-queue + **STOP** (PA-проекция — фаза 2b), `final` → линия закрыта; resume-события (`evt:pa.resolved`/`evt:env.up`/`evt:owner.resume|abort`) и `replay` задокументированы. **`--autonomy L0|L1`** — сквозной per-invocation F1-override: флаг команды → CLI движка (`init|ingest|tick --autonomy`) → `env.override` → 5-й аргумент `resolve()`; floor непробиваем (юнит). **Деплой-фикс:** `autonomy-policy.cjs` переезжает `lib/` → `orchestrator/lib/` рядом с потребителем — root-`lib/` не имел деплой-маппинга (`/ecosystem:update` его не синкает → require бился бы в user-проекте после первого update); примеры-перечисления `update.md` выровнены (`{processes,lib,charters}` managed; `{registries,ledger,runs,fabric}` **preserved** — fabric-state в явном wipe-protection списке); `verify.md` Step 4 считает `charters/*.json`. Новый wiring-тест `fabric-dispatcher-wiring.test.cjs` (11 asserts — lockstep run.md ↔ charter ↔ update.md ↔ require-граф) + 2 юнита override (fabric-engine 18/18) в `test:orchestrator`; полный verify зелёный. Consumer-zone (`commands/` + `orchestrator/`); аддитивно → counts без изменений (24/44).
- **Process Fabric — пилотное ядро межпроцессного statechart-координатора: `fabric-engine.cjs` + первый charter `feature-production-line` + юниты** (DEC-DEV-0153; дизайн-SSOT `dev/process-fabric/CONCEPT.md`, эмпирика — аудит `dev/process-fabric/AUDIT_2026-07-07.md`: 18 де-факто машин состояний / enforced-переход у 3 / 36 разрывов; каталог 94 процессов `catalog.yaml`). Новая либа `orchestrator/lib/fabric-engine.cjs` (CJS, builtins-only, pure-core + CLI `init`/`ingest`/`tick`/`status`/`replay`): декларативные JSON-charter'ы (подсет XState-семантики: массивы кандидатов first-guard-wins, `invoke` = запуск существующего Workflow-процесса, счётчики bounded-loop) + **event-sourcing** (`.claude/orchestrator/fabric/<instance>/events.ndjson` первичен, `state.json` — материализация, replay бит-в-бит) + **prescriptions** (движок ничего не исполняет сам — предписывает актуаторам: диспетчер/PA/человек). События — ТОЛЬКО материализация structured-результатов процессов (`ingest`-маппинг по фактическим полям возвратов P3o–P7o; путь P5 сверен с реальным `go_gate`). **Каждая run-process-prescription проходит F1 `lib/autonomy-policy.cjs`** — floor непробиваем by construction, `meta.autonomy='human-gate'` на invoke-состоянии флорит диспозицию только вверх; первый фактический потребитель F1 (семя F2-wiring). **Backpressure**: WIP-guard per lane (кодифицирует FB-004 «один orchestrator-run на repo») + единая приоритизированная owner-queue (floor>conflict>gate>review). Таймстемпы — входы (`--at ISO`, дисциплина run-ledger), instance-id детерминирован, идемпотентный ingest по run_id. Собран конвейером «исполнитель → адверсариальная панель ×3 → фикс»: 12 находок, 4 confirmed-important применены с тестами-регрессорами. Тест 16/16 в `test:orchestrator`; полный verify EXIT=0. **Built ≠ validated** — graduation-критерии CONCEPT §9; диспетчер-wiring в `run.md` сознательно НЕ включён (фаза 2, отдельный PR). Consumer-zone (`orchestrator/`); аддитивно → counts без изменений (24/44).
- **Agent-roster конфиг + participation-matrix слоем над zone-router — Epic G минимум (G1+G2)** (DEC-DEV-0151; kickoff-решения DEC-DEV-0145 е/ж/з). Новая dual-use либа `hooks/product/lib/agent-roster.cjs`: **G1** — опциональный per-project `.product/agent-roster.yaml` (per-персона `enabled` / `model` / `depth_threshold` / `extra_lenses` + именованные пресеты состава); **absent файл == встроенный дефолт, поведение байт-в-байт прежнее** (юниты ассертят шов против реального выхода `zone-router.route()`). **G2** — `resolveFiring(routeResult, roster)`: слой ПОВЕРХ zone-router (решение «е»: стопка, не четвёртый firing-механизм) — `enabled:false` снимает персону, per-персона `depth_threshold` только ПОДНИМАЕТ планку над зонным гейтом (никогда не опускает), все выпали → честный `fire:false` + `dropped[]` surfaced. Wiring в `zone-change-trigger.js` — fail-open (сломанный ростер не блокирует запись и не гасит панель; warnings — non-blocking строкой в stderr). **Пресеты `lean`/`full`** (решение «ж»: бывш. D3≡G4 — одна реализация) built-in + user-override; `getPreset`/`resolvePanel` экспортированы для будущего потребления D1b-панелью (wiring отложен, bring-forward). Malformed/unknown-конфиг деградирует громко с warnings, тайпо не гасит персону молча. G3 (панель метрик) — cut по kickoff. Юниты `agent-roster.test.cjs` в `test:product`; roster-шаблон сознательно не шипается (схема-SSOT в header либы). Consumer-zone (`hooks/`); аддитивно, ростер = конфиг → counts без изменений (24/44).
- **`/product:batch-enrich <FM-NNN ...> | --all-planned` — макро-батч обогащения feature-set: F.2→F.7 + completeness-loop на каждую FM, гейты на границах фаз через PA-эскалации** (DEC-DEV-0150, Epic C-i; kickoff-решения DEC-DEV-0145 г/д). `/product:complete` закаляет ОДНУ фичу — новая команда ведёт НАБОР FM (релиз) через обогащение + bounded completeness-loop, перенося человеческий approve-гейт с per-item на **границы фаз** (L1 PA-эскалации — владелец ратифицирует из ledger). **Тонкая оркестрация существующей машинерии** (vision cut 4): ENRICH-стадия исполняет процедуру существующего `commands/product/feature.md` (F.2→F.7), COMPLETE делегирует существующему `complete-feature.mjs` через `workflow()` — authoring нигде не ре-имплементирован. **Checkpoint-first + resume** (урок E1: session-limit на батчах): resumable-манифест в `.product/.batch-enrich/<slug>/` пишется ДО первого касания (слуг детерминирован, без таймстемпов; per-FM state-файлы single-writer); обрыв середины батча возобновляется со шва. FM-цикл **последовательный** (single-writer: один `.product/`-tree + аллокация PA-NNN канонического ledger). **PREPARE-ONLY:** FM-статус никогда не переводится (F.10 — ратификация владельца), `honest_unmet` каждого child-loop'а несётся verbatim — ниже-τ FM не округляется в done; skip/fail per FM surfaced, батч продолжается (degrade loud). F.8/F.9 — skip+log (вне C-i). Новый раннер `product/processes/batch-enrich-feature-set.mjs` + команда `commands/product/batch-enrich.md` (deployed `.claude/product/processes/`; inline-fallback на до-волновых инсталляциях); wiring-тест 85 ассертов в `test:product`; verify.md Step 4/4.6/9. Counts без изменений (24/44 — команда не артефакт-тип).
- **`/product:consilium <PA-NNN>` — decision-preparation jury for the completeness-loop's escalated decision-PAs** (DEC-DEV-0145, Epic D generalization). `/product:complete` escalates strategic/connected decisions to the canonical pending-actions ledger as plain PAs; this new command takes ONE already-escalated, **fork-shaped** decision-PA (≥2 mutually-exclusive options) and prepares an owner decision — a heterogeneous jury of Epic-A profile personas (`architect-advisor` + `qa-advisor`; `ux-advisor` only when the decision touches UI) scores the options **independently** from the raw artifacts, their verdicts aggregate **deterministically** through the shared `consilium-synth.cjs` (matrix + rank + hard/soft-veto, `--panel` parameterization), a surfacing-only integration pass flags cross-lens issues, and a recommendation package is written back into the same PA **in place** (PA-dedup). **PREPARE-ONLY:** the jury recommends, the **owner ratifies** — it never closes the PA, edits a spec, or fabricates a missing option (a <2-option PA is honestly refused with a routing note, never a manufactured fork). Generalizes the proven Orchestrator P2 `decide-architecture-foundation` primitive rather than re-implementing the aggregator. New runner `product/processes/consilium.mjs` + command `commands/product/consilium.md` (deployed `.claude/product/processes/`; falls back to inline prose on pre-wave installs). Counts unchanged (24/44 — a command is not an artifact type).
- **Run-ledger — количественная observability оркестратор-прогонов: durable `runs/<id>/run.json` + времянка `runs/ledger.ndjson`, авто-создание `.claude/orchestrator/runs/`** (DEC-DEV-0147; VC-087 + VC-134 из `dev/VIBE_CODING_ANALYSIS.md` — закрывает «tracked follow-up» из run.md «Run records (FB-003)»). Новая либа `orchestrator/lib/run-ledger.cjs` (CJS, builtins-only, CLI `start`/`finish` + чистые экспорты): диспетчер обрамляет Workflow — `start` ДО (создаёт `runs/<id>/run.json` status:running; упавший прогон оставляет след) и `finish` ПОСЛЕ (finished_at/duration_ms/result-сводка + `model_map` per-stage, извлекаемый из исходника процесса по label-инварианту смоука 0146; персоны через `agentType:` = `via-agent-definition`; tokens opportunistic) + одна идемпотентная по run_id строка в `ledger.ndjson`. **Таймстемпы — входы** (`--at <ISO>`, ставит диспетчер): Workflow-тело не читает wall-clock (determinism/resume-контракт). run-id детерминирован (`<дата>-<процесс>-<base36 epoch-ms>`, без Math.random); `finish` без `start` не падает (finished-unstarted). Wiring — `commands/orchestrator/run.md` (новая секция + переписанный блок Run records: транскрипт-дир harness остаётся source of truth per-agent деталей, ledger = durable greppable сводка). Тест 10/10 в `test:orchestrator`; полный verify EXIT=0. Consumer-zone (`orchestrator/` + `commands/`); аддитивно → counts без изменений (24/44).

- **MDP model-pinning во всех Workflow-процессах: каждый `agent()`-вызов несёт явный per-stage `model:` (56 пиновок) + смоук-гейт против регресса** (DEC-DEV-0146; VC-118 из анализа `dev/VIBE_CODING_ANALYSIS.md` — «задокументированный MDP §3-разрыв»: без пиновки стадии наследуют дорогую модель СЕССИИ = waste + конфаунд для judged-стадий). Раскладка по `settings.json.template._model_strategy` + MDP worst-of: **opus** — судейство/adversarial/verify (жюри P2 ×3 и панель P6 ×3 — «judge fixed across arms» одной точкой вызова), TDD-impl/`spec-fix`/`remediate`, диагностика (`debug`, `boot-smoke`, `classify-block`); **sonnet** — механика/relay (init/git/оракулы, PA-write, plan-parse, `synth` — рекомендацию вычисляет код `consilium-synth.cjs`, LOW-tier verify — зеркалит HIGH/LOW-разрез gate-risk-classifier); персоны (`architect/qa/ux-advisor`) уже запинованы frontmatter'ом определений — вызовы через `agentType:` не дублируют. Гейт: `tests/orchestrator/workflow-syntax.smoke.cjs` — каждая `label:`-строка обязана нести `model:`/`agentType:`, сканирует оба PROC_DIRS (покрывает процессы без wiring-теста и будущие файлы). smoke:orchestrator 15/15, полный verify EXIT=0. Consumer-zone (`orchestrator/` + `product/processes/`); аддитивно, поведение стадий не менялось → counts без изменений (24/44).

- **Позиционные doc-якоря из анализа Google-статьи о vibe coding SDLC: спектр vibe→agentic, рамка Agent = Model + Harness, «два режима оператора»** (DEC-DEV-0144; полный анализ канонизирован в `dev/VIBE_CODING_ANALYSIS.md` — мульти-агентный разбор 51 стр.: 140 идей / 9 тем, оценки применимости с evidence-путями, критик полноты против оглавления OK; это шаг 1 принятого порядка внедрения §8 отчёта). `docs/guide/00-concepts.md` §1 получает врезку **«Почему столько структуры»** (спектр vibe→agentic как обоснование гейтов/ревью — различитель не факт использования AI, а объём структуры/верификации/суждения вокруг вывода), §5 — врезку **«Два режима оператора»** (hands-on per-item approve vs async-делегирование с ревью вердикта GO/NO-GO/MANUAL_VERIFY; дуальность нарезана по фазе пайплайна и живёт даже внутри owned-зоны); root `README.md` — секцию **«Agent = Model + Harness»** (Ecosystem 3.0 = harness-слой поверх Claude Code — команды/гейты/хуки/оркестрация/верификация, а не «более умная модель»). Consumer-zone (`docs/` + root README); аддитивно → counts без изменений (24/44).

### Fixed

- **Doc-drift quick-wins из gaps-сверки: G18 scan-перед-update, G33 command-counts из генерируемого каталога, G35 мёртвая ссылка disable-d7-audit, G17 repair-подсказка после update, G21 баннеры пост-spec расширений** (DEC-DEV-0170; корзина №4 пост-graduation обязательств, приоритет владельца). **G18**: `commands/integrator/update.md` Pre-flight теперь явно вызывает `/integrator:scan` (refresh baseline до любой мутации — SPEC §13.1 «Always» держался только на add); строка SPEC про `replace` промаркирована Phase-7 (команды не существует, G14). **G33**: ручной per-namespace baseline команд в `commands/ecosystem/verify.md` (Step 4 «expect 9 / ~22 / 7…» + summary-блок) удалён — ожидания теперь ДЕРИВИРУЮТСЯ из генерируемого каталога `.claude/docs/guide/02-commands.md` (авто-синхронизирован `gen:catalog:check`'ом на каждом cut'е): verify сверяет deployed-файлы с каталогом, два параллельных несинхронизированных механизма схлопнуты в один; структурные минимумы runtime-каталогов (`product/processes` 3+, `charters` 1+) сохранены. **G35**: `enable-d7-audit.md` больше не ссылается на несуществующий `/ecosystem:disable-d7-audit` — ручное удаление hook-entry задокументировано как поддерживаемый путь, отсутствие команды — сознательное. **G17** (частично): `commands/ecosystem/update.md` Step 8 сёрфейсит условный next-step `[2] /integrator:update --repair` когда `active-tools.yaml` существует (adapters/ только что full-synced → instance-адаптеры могли отстать); детерминированный hook остаётся G16/Phase-7. **G21** (частично): баннеры в `docs/product-module/SPEC.md` (Epic A/B/C/D построены, но в тело SPEC не вписаны — SSOT указан) и `docs/design-module/SPEC.md` (двусторонний дрейф: ux-advisor код-впереди-SPEC / screen-generator SPEC-впереди-кода G36). Consumer-zone (`commands/` + `docs/`); поведение рантайма не менялось, counts без изменений (24/44).
- **Upstream-долг graduation-прогона Fabric: DEF-4 (P7 probe слеп к monorepo-workspace'ам) + ANOM-5 (owner-queue без dequeue) + ужесточение `--force-manual` (reason обязан ссылаться на существующую PA)** (DEC-DEV-0168; пост-graduation обязательство №3, условие судьи №2 + рекомендация №4 из `FABRIC_PHASE3_GRADE_REPORT.md`). **DEF-4** (`orchestrator/lib/runtime-readiness.cjs`): корневой `package.json` больше не единственный источник run-target — при его пустоте CLI сканирует workspace-пакеты (`pnpm-workspace.yaml` + npm-форма `workspaces`, литеральные пути и одноуровневый `/*`; `!`/`**` честно пропускаются disclosure-нотой), новая чистая `detectWorkspaceRunTargets()` детерминированно ранжирует кандидатов (приоритет источника → лексикографика по dir), выбор дисклозится (workspace-происхождение, неоднозначность >1 кандидата с подсказкой пина), новый `--app <dir>` пинует пакет явно; `run_target.cwd` + `plan.cwd` несут каталог буста. Enum вердиктов НЕТРОНУТ, всё аддитивно (soft-миграция: отсутствие новых входов == старое поведение бит-в-бит); ложный `NOT_STARTABLE` short-circuit до env-пробы (live-блокер R2/`runtime_gate_retry`, PA-056) устранён. Юниты 21→**30**. **ANOM-5** (`orchestrator/lib/fabric-engine.cjs`): owner-queue самоочищается на write-path — `dequeueOwnerEntries()` после каждого applied-перехода выкидывает записи ушедшей с гейта линии (вкл. терминал) и глобальных сирот (инстанс снесён), `appendOwnerQueue` дедупит повторный парк (instance+state+kind); `status` остаётся строго read-only (контракт SessionStart-хука) — read-only split `reconcileOwnerQueue()` показывает живые в `owner_queue`, протухшие — новым полем `owner_queue_stale` с reason (ничего не прячется молча). Ложные owner-сигналы terminal-инстансов (5 stale-записей live-прогона) больше не копятся. **`--force-manual`**: причина обязана содержать PA-id (`/PA-\d{3,}/`) И этот PA должен существовать в `pending-actions.md` — эскейп дороже честного пути (сначала фиксируешь ручное вмешательство как PA, потом обходишь guard); help/ошибки инструктивные. Юниты fabric-engine 27→**35**; полный verify EXIT=0. Consumer-zone (`orchestrator/lib/` + `docs/guide/07-fabric.md` риды); counts без изменений (24/44).
- **`fabric-engine.cjs` — DEF-3 bracket-guard: голый `tick` process-result-события больше не прошагивает автомат мимо предписанного брекета** (DEC-DEV-0163; live-дефект №3 прогона Fabric фазы 3, VM-пилот 2026-07-09). Диспетчер-сессия под операционным давлением тикнула `evt:impl.go`/`evt:runtime.ready_or_started` руками (в событиях нет run_id, ledger без новых брекетов, 0 Workflow-вызовов в транскрипте) — контракт run.md «prescription run-process = полный bracket (ledger start → процесс → finish → `ingest --run-id`)» движком не принуждался. Теперь `tick` события, ingest-мапленного процессу invoke-состояния, отказывает (exit 2, объяснение + путь эскейпа); сознательный ручной прогон — только `--force-manual "<reason>"`, причина аудируемо штампуется в `why[]` события (replay-нейтрально, state.json бит-в-бит). Resume-события (`evt:pa.resolved`/`evt:env.up`/`evt:owner.*`/`evt:line.start`) не затронуты; ingest/pa-scan/replay — вне guard'а. Юниты 23→**27** (bare-refuse / force+audit-marker+replay / empty-reason / resume-регрессия). Consumer-zone (`orchestrator/lib/`); counts без изменений (24/44).
- **`session-fabric-status.js` (SessionStart-инжектор Fabric) — payload отбрасывался харнесом целиком: в `hookSpecificOutput` отсутствовал обязательный `hookEventName: "SessionStart"`** (DEC-DEV-0162; live-дефект №1 прогона Fabric фазы 3, VM-пилот 2026-07-08). Дефект был невидим до первого старта сессии с ЖИВЫМ fabric-инстансом (без инстанса хук молчит) — на S2 прогона Claude Code ответил «Hook JSON output validation failed — hookSpecificOutput is missing required field "hookEventName"» и fabric-статус в контекст не заинжектился (линия выжила через регламент `status`/`pa-scan` — деградация мягкая, но G03-канал инжекта был мёртв с рождения). Фикс — поле добавлено; smoke-кейс `fabric-status-injected` ужесточён и теперь требует `hookEventName` в выводе (урок: smoke проверял форму JSON, но не контракт харнесса). Consumer-zone (`hooks/`); counts без изменений (24/44).
- **G19 — silent drift каталог↔раннер закрыт: V-18 и все 4 V-AM-* отсутствовали в `validation-runner.md`; появился детерминированный линтер синхронности** (DEC-DEV-0158; gap G19). Раннер получил: строку **V-18** в artifact-таблице (зеркалит inline-check `artifact-validate.js`, DEC-DEV-0064 — до этого `--rule V-18` через `/product:validate` молча не работал), секцию **V-AM-*** (inline-only: `hooks/design/design-artifact-validate.js`; semantics V-MK-класса — silent skip + graceful note) и machine-readable маркеры `<!-- catalog-sync:acknowledged … reason="…" -->` для признанных пропусков (V-MK-01..08, V-AM-*). Каталог: namespace-таблица §0 исправлена (15→16, V-01..V-18), §11 чекбокс линтера закрыт. Новый линтер `dev/meta-improvement/scripts/check-validation-sync.cjs` в `npm run verify` (`check:validation-sync`): ID-множества каталог (`#### V-…`) ↔ таблицы раннера + ack-маркеры, 6 инвариантов (непокрытые/orphan/stale-ack/ambiguous/prose-итог/namespace-таблица); exit 0/1/2. Семантика правил не парсится — runtime-исполнение остаётся hardcode (anti-pattern §1 уточнён). Consumer-zone (`skills/` + `docs/`); counts без изменений (24/44 — правила не добавлялись, закрыто отражение существующих).
- **G20 — устаревшие счётчики в `templates/project/CLAUDE.md.template` («23 artifact types» ×2, «33 validation rules» при каноне 24/44) + слепая зона реконсилятора закрыта: `check-counts.js` теперь сканирует `templates/`** (DEC-DEV-0156; gap G20 из `dev/process-fabric/audit/APPENDIX-B-gap-analysis.md` — дрейф тиражировался в КАЖДЫЙ новый пилот-проект при bootstrap). Root cause двойной: `SCAN_ROOTS` не включал `templates/`, а файловый фильтр `walk()` брал только `*.md` (файл — `.md.template`) — оба уровня починены, header обоснован (templates = consumer-zone, числа live). Enforcement замкнут автоматически: process-gate (commit-msg) гоняет check-counts → дрейф шаблона теперь блокирует коммит. Consumer-zone (`templates/`); counts без изменений (24/44).
- **LESSON-гейт (PreToolUse prong) — снят self-deadlock marker-exemption: strict-deny больше не блокирует записи, которыми протокол разрешает сам урок** (DEC-DEV-0143; армированный S-LE live-смоук, пилот-сессия `4fb6e0f2`, 2026-07-04). Смоук подтвердил риск, предсказанный header'ом хука: write-ahead-порядок протокола (`lesson-capture.md` шаги 3-4 — файл урока пишется ДО маркера) означает, что первые записи `/product:lesson --resume` происходят без `lesson-in-progress`-маркера → strict-deny отбивал и создание маркера, и Edit самого урока — **замкнутый круг** (live: 3× deny изнутри resume-протокола). Фикс — **target-carve-out** в `hooks/product/lesson-presence-gate.js` (`isLessonResolutionTarget`): мутации с целью `.product/lessons/**` или `.product/.sessions/lesson-in-progress.*` всегда разрешены (это инструменты разрешения урока); Bash остаётся под гейтом (протоколу не нужен до open→active-флипа). Riders: не-числовой LESSON-id больше не рендерится с `.md` (косметика из смоука); смоук-раннер `smoke-hooks.js` обучен `payloadExtra` (hook_event_name) + `expectStdoutIncludes/Absent` — blocking-хуки отвечают stdout-JSON'ом, и раннер теперь это тестирует; **+6 кейсов** (deny/carve-out×2/fresh-marker-exemption/UPS-reminder/warn-no-deny), хук-смоук 28→**34/34**. Итоги S-LE: S-LE.2/4/5/6 PASS · S-LE.3 deny PASS + exemption FAIL→FIXED · S-LE.1 PARTIAL (stderr-фидбек Stop-гейта доходит до модели, но `preventedContinuation:false` на CC 2.1.200 — hard-block не продемонстрирован). **Дефолт остаётся `warn` — флип warn→strict заблокирован до PASS live-ре-прогона S-LE.1/S-LE.3 против фикса** (`dev/gates/S_LE_LESSON_GATE_SMOKE.md`). Consumer-zone (`hooks/`); поведенческий фикс, counts без изменений (24/44).

---

## [1.7.0] — 2026-07-03

**Minor release — accumulated since 1.6.0:** the **Orchestrator Module is COMPLETE — P1–P7 + the two-way §6 capability channel** (P2 `decide-architecture-foundation` consilium 0127/0129 + its blind-spot fixes 0135; P7 `runtime-smoke-readiness` 0120; §6 detect-leg 0117; removes the PILOT POINT blocker) plus the N+2 **gate-contract hardening** distilled from three live-run batches (0081/0084/0085/0087/0089/0091–0094/0095/0096/0101–0104/0106/0112/0113/0114 — CONCERNS propagation, verify-finding-before-act + order-aware disposition, defect-enum polarity gate, remediation discretion, PA-id worktree safety, readiness DEGRADED-not-silent, LF pinning); the **Autonomous Pipeline Vision Increment 1 + Wave B build** (Epic A personas + zone-routing and the B1 completeness-oracle + bounded loop 0098, pilot-shape fixes 0099/0103, worktree hardening B-a 0140, and the **durable wave-runner B-b/B-c 0142** — the new top-level `product/processes/` runtime dir + the deterministic `gap-classifier.cjs` stop-verdict); **Guided Research Wave 1** (0138 — research-intake + anti-hype-filter skills + thin `/ecosystem:research`); the **doc-UX waves** (interactive generated maps: command-map 0118 + the BPMN process map 0121–0125; the shared map-shell with the in-app UTF-8 doc panel 0128/0133/0134; the layered guide entry + gates explainer 0130; the generated glossary + naming-axes legend 0131/0139; Diátaxis `doc_type` + drift check 0137; the user-guide layer 0105); the **feedback contour split phase 2** (0090/0096/0097 — `/product:validation-tune` + `/ecosystem:meta-feedback` + the dev-side `feedback-intake` consolidator); and the D7/infra hardening riders (0082/0083/0088/0107). Counts: **unchanged 24/44** (everything additive). **Deferred (carried, not lost):** B-d real-resolve pilot calibration of the wave-runner; the pilot-side `.gitattributes` delivery + one-time LF normalize (FB-LR-27); runtime smokes `S-LE` + `PATCH_1.3.3 S1–S5` + `PHASE_6 S1–S7` (next pilot session); the keyed answer-engine bake-off (gated on Wave-2-tooling).

### Added

- **Completeness-loop — durable wave-runner: `/product:complete` диспетчит Workflow `product/processes/complete-feature.mjs` + детерминированный `gap-classifier.cjs` (classify + stop-вердикт)** (DEC-DEV-0142; Wave B / B-b, work-order `dev/ECOSYSTEM_VISION_BATCH_2.md`, проектировка `dev/CONSOLIDATED_EXECUTION_PLAN.md` §4.1). Волна completeness-loop (skill DEC-DEV-0098, закалённый B-a/0140) была harness-driven прозой — теперь это **исполняемый Workflow-скрипт** в новом top-level runtime-каталоге **`product/processes/`** (зеркально `orchestrator/`; bootstrap подхватывает bulk-copy автоматически, `/ecosystem:update` синкает namespace-aware — wiring по прецеденту DEC-DEV-0078). Волна: **SCORE** (completeness-oracle — внешний детерминированный стоп-сигнал, verbatim-relay) → **SURFACE** (канонические персоны `architect`/`qa`(+`ux` при `has_ui`) с одним anchored-root на ран (FB-LR-28) и raw-source брифами (FB-LR-31 — персоны читают сырые артефакты, бриф не пересказывает); crash-safe слоты + bounded re-spawn, **никогда** general-purpose fallback; персистенция findings выделенным writer-шагом — advisors read-only by design, находка: контракт 0140 «persona WRITES» был невыполним без Write-grant) → **CLASSIFY** (новая dual-use либа **`hooks/product/lib/gap-classifier.cjs`**: диспозиции по разметке `dev/LOOP_READINESS_AUDIT.md` — 🟢 VC/LC/RPM-деривации → auto, 🔴 + unknown → decision, консервативно; **стоп-вердикт `shouldStop`** = тестируемый SSOT формулы `cap ∧ (met ∨ Δ<ε ∨ decisions-only)`; агент — только транспорт, `.mjs` держит in-code зеркала: hard cap, met-check, Δ-mirror — терминация гарантирована) → **RESOLVE** (conservative whitelist + verify-before-act DEC-DEV-0093, sequential single-writer, идемпотентно keyed) → **ESCALATE** (канонический pending-actions ledger, PA_CANON verbatim + PA-dedup DEC-DEV-0089) → **CloseOut (B-c)**: advisory-прогон делегатов B5/B6/B8 (`bg-review`+`validate`) на стопе — в отчёт, `met` не флипает. Completion-report — rail 5: `honest_unmet` прямым текстом при стопе ниже τ. `/product:complete` переписан на диспетч `Workflow({scriptPath})` с честным inline-fallback (до-1.7.0 инсталляции); skill остаётся SSOT семантики волны (+фикс несостыковки: `advisor-pending.yaml` → реальный `hooks/product/zone-routing.yaml`). `verify.md`: Step 4 счётчик `product/processes/*.mjs` + новый **Step 4.6** delivery-маркеры (`delegated_unverified`, `gap-classifier.cjs`) + Step 9 summary-группа. Generic workflow-смоук обобщён на оба каталога процессов. Тесты: `gap-classifier.test.cjs` 20 ассертов в `test:product`; `npm run verify` EXIT=0. Consumer-zone (`commands/`, `skills/`, `hooks/`, `product/`); аддитивно, без новых типов/правил → counts без изменений (24/44). _(Live-калибровка широты авто-RESOLVE — B-d на пилоте, E4.)_
- **Руководство оператора — единая легенда «Оси именования» в глоссарии + фикс дрейфнувшей расшифровки `P1–P5`** (DEC-DEV-0139; doc-UX Волна 2, E2, `dev/DOCS_UX_BATCH_DESIGN.md`). Новая секция **«Оси именования»** в генерируемом `docs/guide/03-glossary.md` (15 строк `Ось · Что нумерует · Значения · Не путать с`) разводит перегруженные метки экосистемы: `P#` (принципы `P1–P7` ≠ процессы Product `P1–P5` ≠ Orchestrator `P1–P7`, суффиксы `p`/`o` в картах), семейство `D` (`D1–D6` домены ≠ `D.1–D.6` шаги Design ≠ `D1.1–D1.9` шаги Discovery ≠ `D2-B01…` обязанности), тройной «tier» (review / `validation_tier` / `product_tier`), тройной `MVP`, двойной `L0/L1` (лестница понимания vs зум App Map). Editorial-данные — в `ecosystem-map.overlay.json::namingAxes[]`, эмит первым разделом в `gen-glossary.cjs` + selftest-инварианты (анти-дрейф-контур, как `glossary[]`). Заодно **починена дрейфнувшая строка глоссария** `P1–P5` (была «Discovery, Planning, Feature, Handoff, Maintenance» — Handoff/Maintenance не P-процессы; стала SSOT-верной по `docs/pmo/processes.md`: «P1 Init · P2 Feature · P3 Feedback(stub) · P4 Cascade · P5 Periodic», переименована в `P1–P5 (Product)`). Карта команд встраивает `glossary[]` → `ecosystem-map.html` перегенерирована; `namingAxes` добавлен в allow-list второго генератора. Инвентаризация осей — вынесенным read-only аудит-субагентом (~20 осей + `file:line`); `P1–P5`-фикс доп. верифицирован по SSOT перед правкой. `npm run verify` EXIT=0. **Правьте overlay/шаблон, не сгенерированный HTML/MD.** Consumer-zone (`docs/`); аддитивно → counts без изменений (24/44). _(Аудит surfaced и отложил: Orchestrator `P1–P7` vs `P3–P6` в гайде · node-id `P4`/`P4p` кодировка карт · membership-дрейф `NFR`=🟠/`AM`=🟢 в 00-concepts/06-gates — отдельными фиксами.)_
- **Guided Research — лёгкий скилл-слой: интейк с контрактом метрик полезности + анти-хайп фильтр + тонкая команда `/ecosystem:research`** (DEC-DEV-0138; Wave 1 капабилити «Guided Research», дизайн `dev/RESEARCH_CAPABILITY_BLUEPRINT.md` + Wave 0.5 `dev/RESEARCH_CAPABILITY_WAVE0.5_BAKEOFF.md`). Поиск/сбор/обработка информации в экосистеме были размазаны по трём силосам (`integrator/research-protocol`, `product/market-research-protocol-quick`, harness `deep-research`): retrieval/триангуляция/анти-хайп есть кусками, но **никто не со-формировал точный бриф до поиска и не скорил decision-usefulness** (в отличие от credibility), гейтя финальную сводку. Wave 1 добавляет **два net-new скилла + тонкую команду-оркестратор**, переиспользуя существующие loop'ы (не дублируя): **(1) `skills/ecosystem/research-intake.md`** — Pillar A (reference-interview + XY-drill + batch ≤3 вопросов по information-gain → замороженный **Research Brief** с явным markdown-шаблоном; режим ИИ-заказчика с `input-required`) + Pillar B (две ортогональные оси **Relevance ⟂ Decision-Utility**, Tier-1 hard gates faithfulness/citation/freshness/corroboration, Tier-2 weighted, вердикт **PASS/PARTIAL/SHORTFALL** с per-dimension subscores + явный шаблон Metrics Contract); **(2) `skills/ecosystem/anti-hype-filter.md`** — Pillar D (atomize → SIFT/lateral-read → provenance-tier → hype-scan H1–H8 → триангуляция по независимости → faithfulness (LLM-judge = 1 сигнал) → **keep/DEMOTE/drop с audit-trail** — down-weight, не тихий дроп; консолидирует анти-паттерны `research-protocol` + триангуляцию/`[оценочно]` `market-research`); **(3) `commands/ecosystem/research.md`** — тонкий `/ecosystem:research`, оркеструющий A→B→C(reuse `deep-research`/`market-research`/`research-protocol` + MCP-стек, fallback WebSearch/WebFetch)→D→scored-синтез + approve-gate («silence is NOT consent») + research-cache. **Слоями (§7.1 дефолты):** бриф+метрики — **markdown, НЕ формальный PMO-артефакт** (формализация в тип RB отложена до Wave 3 за триггером reuse≥N + form-drift); дом = `skills/ecosystem/`; опц. warn-хук отложен. Wave-2 answer-engine (Perplexity Sonar, provisional по Wave 0.5) **пока не подключён** — команда явно помечает как future. `verify.md` Step 4 + summary: ecosystem-команды **6→7** (+research). Скиллы self-contained (без ссылок на `dev/`, т.к. он не ставится потребителю). check-counts ✓ 24/44, hook-smoke 28/0. Consumer-zone (`commands/` + `skills/`); аддитивно, без новых типов/правил → counts без изменений (24/44).
- **Руководство оператора — Diátaxis-роль `doc_type` у каждой `guide/*.md` + анти-дрейф-чек против таблицы хаба** (DEC-DEV-0137; doc-UX Волна 1, A3, `dev/DOCS_UX_BATCH_DESIGN.md`). Каждая из 8 `docs/guide/*.md` получила YAML-фронтматтер `doc_type` (Diátaxis-роль, доминирующий тип + опц. `doc_type_secondary`): 6 рукописных — прямыми правками (00-concepts=explanation · 01-first-session=tutorial · 04-ui-design/05-implementation=how-to · 06-gates=explanation·how-to · README=navigation), 2 генерируемых (02-commands · 03-glossary=reference) — **через генераторы** (`gen-command-catalog`/`gen-glossary` эмитят фронтматтер; сгенерированное не правим руками). Новый `check:doctype` (`dev/meta-improvement/scripts/check-guide-doctype.cjs`, в `npm run verify`): валидирует enum {tutorial, how-to, reference, explanation, navigation}, **сверяет ручную Diátaxis-таблицу в `README.md` («Что здесь — файлы и их роль») с фронтматтером** (метка строки ⇔ doc_type файла) + anti-orphan в обе стороны — рукописная таблица хаба больше не может разойтись с ролью доки (это и есть потребитель метки, снявший прежнюю отсрочку A3 из 0131). In-app doc-панель карт (`vendor/map-shell.js` `renderMd`) теперь **срезает ведущий фронтматтер и HTML-комменты** — заодно почищен старый шум (02/03 показывали сырой `<!-- GENERATED -->`). `npm run verify` EXIT=0. Consumer-zone (`docs/`); аддитивно → counts без изменений (24/44). **Закрывает последний критерий приёмки Волны 1** (§8: «каждая `guide/*.md` имеет `doc_type`»).
- **Интерактивные карты — cross-map deep-link: общий `#focus=kind:id`, который чтят обе карты + навбар переносит фокус при переключении вида** (DEC-DEV-0134; doc-UX Волна 1, PR#3b / C3, `dev/DOCS_UX_BATCH_DESIGN.md`). Завершает интеграцию двух карт (`ecosystem-map.html` ⇆ `ecosystem-processes.html`): раньше переключение вида **теряло контекст**. Теперь общий hash-параметр **`#focus=kind:id`** чтят обе карты (via adapter-паттерн в шелле — каждая карта регистрирует `MapShell.registerView({getFocus, applyFocus})`), а навбар **«⇄ переключить вид»** на клике **переносит текущий фокус** в другую карту (`href#focus=<token>`) → переключение сохраняет контекст. `art:*` (артефакт) — общий узел обеих карт, фокусируется в обеих (карта команд подсвечивает + скроллит карточку; карта процессов разворачивает дорожку артефактов, выделяет node + открывает side-panel + центрирует); `cmd:*` — только карта команд (в процессах грациозно no-op). Две пойманные ловушки: (a) **expand-collapse удаляет детей свёрнутого компаунда** — артефакты в свёрнутой по дефолту дорожке были не резолвимы, `applyFocus` сперва разворачивает её; (b) **`fit` во время асинхронной ELK-раскладки → пустой холст** — фокус ждёт `layoutstop`, затем переразложить + центрировать zoom 1.1. `view=`-параметр не реализован (файл и есть вид, навбар — переключатель). Само-проверено в реальном Chrome (puppeteer): обе карты по `#focus=art:FM` фокусируют FM, 0 JS-ошибок. Смоук: `mapshell.smoke.mjs` 7→**8**, `procmap.smoke.mjs` 20→**21** (deep-link в обе стороны). **Правьте шаблон/шелл, не сгенерированный HTML.** Consumer-zone (`docs/`); аддитивно → counts без изменений (24/44). _(Тултипы акронимов из `glossary.json` осознанно отложены как low-ROI — карты уже показывают имена артефактов; machinery без явного потребителя, урок DEC-DEV-0131.)_
- **Интерактивные карты — общий шелл `map-shell.{js,css}`: навбар «переключить вид» + просмотр `.md` прямо в карте (чинит «кракозябры») теперь в ОБЕИХ картах** (DEC-DEV-0133; doc-UX Волна 1, PR#3a / C3, `dev/DOCS_UX_BATCH_DESIGN.md`). Две карты (`ecosystem-map.html` — команды/артефакты; `ecosystem-processes.html` — BPMN-процессы) были двумя отдельными приложениями; in-app UTF-8 doc-панель (DEC-DEV-0128) жила **только** в карте процессов → `.md`-ссылки карты команд (исходники команд, SPEC, футер) всё ещё уводили в сырьё, отданное IDE-сервером без `charset=utf-8` → GUI-Chrome на русской локали читал `windows-1251` = «кракозябры» (недочинённый остаток). Новый вендоренный офлайн-шелл `docs/guide/vendor/map-shell.{js,css}` **инклюдят оба шаблона**: **(1)** doc-панель мигрирует в шелл (dedup — карта процессов отдаёт свою инлайн-панель туда, оставив тонкие шимы, так что side-panel и headless-хук работают без правок), а карта команд её **получает** — клик по `.md`-ссылке (помечена `class="doclink"`) открывает файл в панели, декодируя байты как UTF-8 самим (`fetch` + `TextDecoder('utf-8')` + минимальный markdown-рендер) → читаемо в **обеих** картах; **(2)** навбар **«⇄ переключить вид»** (команды ⇆ процессы) с подсветкой текущего вида; **(3)** перехват doc-ссылок в **capture-фазе** — бьёт `stopPropagation` на SPEC-ссылках внутри `<summary>`; панель `position:fixed` — работает и в скролящемся документе карты команд, и в overflow:hidden флексе карты процессов. Graceful fallback: `fetch` недоступен (`file://` без флага) → «открыть ↗» + подсказка про IDE-сервер. Само-проверено в реальном Chrome (puppeteer): обе карты — навбар подсвечивает текущий вид, клик по `.md` открывает UTF-8-панель с целой кириллицей + markdown, 0 JS-ошибок, раскладка не поехала. Смоук: `procmap.smoke.mjs` 17→**20** (+3 шелл-ассерта; doc-панель по-прежнему зелёная — через шелл), новый **`mapshell.smoke.mjs` 7/7** (карта команд: шелл · навбар · клик footer-doclink → панель с целой кириллицей), `smoke:mapshell` в `npm run verify` (EXIT=0). **Правьте шаблон/шелл, не сгенерированный HTML.** Consumer-zone (`docs/`); аддитивно → counts без изменений (24/44). _(Отложено в PR#3b: единый hash-deeplink `view=`/`focus=` для cross-map фокуса узла + тултипы акронимов из `glossary.json`.)_
- **Руководство оператора — генерируемый глоссарий `03-glossary.md` из SSOT** (DEC-DEV-0131; doc-UX Волна 1, PR#2 / E1, `dev/DOCS_UX_BATCH_DESIGN.md`). Новый Tier-2 анти-дрейф-генератор `dev/meta-improvement/scripts/gen-glossary.cjs` собирает **единый канонический словарь** из SSOT — канон-имена артефактов из H1 спеков (`docs/pmo/artifacts/*.md`, тот же харвест, что `gen-ecosystem-map`) + tier/родословная/группы/сквозные термины из `ecosystem-map.overlay.json` — в `docs/guide/03-glossary.md`: 24 артефакта по доменам (D1 / мост / D2-B / D2-UI / cross) с ID · названием · ревью-уровнем · «что питает» + сквозные термины и вердикты. Убирает дрейф двух рукописных копий: **`00-concepts §8` ужат до указателя** на канон + 10-строчного cheat несущих. Паттерн `gen-command-catalog` (markdown, header-баннер, `--check` = EOL-сравнение без date/sha → детерминир., `--selftest` = парити 24/24 + группы + уникальность терминов, fail-hard). Скрипты `gen:glossary`/`:check`/`:selftest`; **`gen:glossary:check` вплетён в `npm run verify`**. `guide/README`: строка в таблице файлов (Reference/gen) + роутер «вспомнить артефакт» → 03-glossary + провенанс. `npm run verify` EXIT=0. Consumer-zone (`docs/`); аддитивно → counts без изменений (24/44). _(A3 doc_type-frontmatter/check отложен — нет потребителя: хаб рукописный, генератора индекса нет; поднять с ним. Волна 1 PR#3 = общий шелл C3.)_
- **Руководство оператора — единый слоистый вход «Начни здесь» + «зачем гейты»** (DEC-DEV-0130; doc-UX Волна 1, PR#1, `dev/DOCS_UX_BATCH_DESIGN.md`). `docs/guide/README.md` пересобран в **единый вход**: L0 «за 60 секунд», **лестница понимания L0→L5** (что это → пайплайн → из чего собрано → сессия руками → handoff к коду → глубокий reference; каждый уровень линкует нужную доку/карту), **роутер «Я хочу…»** (задача → дверь), обе интерактивные карты на видном месте, файл-таблица с **Diátaxis-ролью** (Tutorial/How-to/Reference/Explanation). Прочие входы (root `README.md`, `HOME.md`, `docs/MAP.md`) получили однострочный указатель на хаб → **5 разрозненных хабов свёрнуты в 1 canonical операторский вход** (закрывает фрагментацию входа + «карта процессов не залинкована из guide/README»). Новый **`docs/guide/06-gates.md`** — «почему тебя останавливают»: approve-уровни 🔴🟠🟡🟢 / DA-ревью / refusal-DoR / Orchestrator-вердикт `GO/NO-GO × readiness × conflicts` поданы как **страховка** (класс дорогих ошибок, пойманный до вложения) + таблица «сигнал → действие»; сознательно ограничен user-facing гейтами (dev-side process-gate/lesson-gate → указатель на `CLAUDE.md`). `npm run verify` EXIT=0. Consumer-zone (`docs/` + root README/HOME); аддитивно → counts без изменений (24/44). _(doc_type-frontmatter + анти-дрейф-check перенесены в PR#2 с глоссарием-генератором — там их потребитель.)_
- **BPMN-карта — просмотр `.md` прямо в карте (чинит «кракозябры») + фикс битого пути + дешум** (DEC-DEV-0128; Волна 0 doc-UX батча, `dev/DOCS_UX_BATCH_DESIGN.md`). Клик по doc-ссылке (footer + «источник» процесса в side-panel) больше не уводит в сырой `.md`, который встроенный веб-сервер IDE отдаёт **без `charset=utf-8`** (→ GUI-Chrome на русской локали читал `windows-1251` = «кракозябры»), а открывает файл **во встроенной панели карты, декодируя байты как UTF-8 самим** (`fetch` + `TextDecoder('utf-8')` + минимальный markdown-рендер) — обход charset-рулетки целиком, независимо от сервера (проверено: байты `.md` валидны, headless-Chrome по `file://` рендерит их корректно — корень был на выдаче, не в файлах/HTML). Заодно исправлен **реальный битый путь** side-panel-«источников» (`d.doc` строился без `../../` → `docs/guide/docs/…` = 404; footer-ссылки были корректны). **Дешум:** длинные кросс-лейн `delegate`-рёбра притушены по умолчанию (ярче на выборе узла через `.ehl`), дефолт-вид сдвинут так, что легенда не перекрывает подпись дорожки «Артефакты» (overlay-слои produces/… уже были off). Ссылка на **карту процессов добавлена в `docs/guide/README.md`** (была не залинкована). Graceful fallback: если `fetch` недоступен (`file://` без флага доступа к файлам) — «открыть ↗» + подсказка про IDE-сервер. Само-проверено в Chrome (headless: панель рендерит `processes.md` без кракозябр, кириллица цела); смоук `procmap.smoke.mjs` +2 ассерта (UTF-8-декод/кириллица + легенда не перекрывает) → **17 проверок**, `npm run verify` EXIT=0. **Правьте шаблон, не сгенерированный HTML.** Consumer-zone (`docs/`); аддитивно → counts без изменений (24/44).
- **Orchestrator P2 `decide-architecture-foundation` — decision SUPPORT on an undecided architecture fork: a heterogeneous 3-prior jury → a scored recommendation + a DRAFT DEC for the owner** (DEC-DEV-0129; Phase 5C of the "module complete" plan; design ratified in DEC-DEV-0127). P2 was cut from the first increment (optional; run by hand in RUN 01 E1). It consumes a **declared** fork — best a cross-spec-conflict pending-action that already enumerates the options (P6/P4 escalate these; the S7 fork `PA-040/042` is the live example) — and compresses it to a decidable shape *(options × lenses × risks × recommendation + the surfaced split)* the **owner ratifies**. It does **not** pick the architecture: the whole system routes architecture decisions to the owner (FB-LR-07), and Vision Epic D's research is that a consilium only pays off on a single connected decision as a heterogeneous **jury that prepares** the decision, not a debate that takes it (a debate is groupthink at ~15× cost). Three pieces. **(1) `orchestrator/lib/consilium-synth.cjs`** — a deterministic, dependency-free, dual-use synthesis core (require exports + a `require.main` CLI an agent runs via Bash and relays as JSON, the orchestrator-lib convention, DEC-DEV-0073 §D.1): `buildMatrix` (option × prior scores + sum/min + veto attribution), `rankSurvivors` (sum desc → worst-of min-floor tie-break → id), and `synthesize` → `{recommended, strength ∈ strong|split|none, matrix, ranked, survivors, vetoed, panel_complete, …}`. **The rule (DEC-DEV-0127 §5/§9.1): veto is worst-of** — an option ANY prior marks in `blocking_concerns` is vetoed and can never be recommended (mirrors `remediation-guard`'s "a conflict beats a preference"); rank among survivors is by summed score; `strong` requires the **full 3-prior panel** unanimous on a surviving option (a 2-of-3 with a died architect is **never** strong — panel honesty, `panel_complete:false` is disclosed); a `split` surfaces `the_real_tradeoff` (the divergence IS the product, not a bug to smooth); `none` (every option vetoed) escalates a re-pose. **(2) `orchestrator/processes/decide-architecture-foundation.mjs`** — the P2 Workflow (Brief→Consilium→Synthesize→Recommend): assemble/**lift** the ForkBrief from the fork's PA (never invent an option; a fork with <2 options is under-specified → surfaced, not fabricated) → `parallel()` the 3 architects (priors velocity/fidelity/integrity, each seeing only the brief, **no cross-talk, no consensus round**) → relay the synth lib's matrix/rank/veto (CODE decides, not eyeballed) → formulate `the_real_tradeoff`+`rationale`+a **DRAFT** `dec_draft`+`applies_to` ON TOP of the fixed matrix (the prompt may explain the recommendation, not change it) → write the recommendation into the fork's **canonical** pending-action (`PA_CANON`, FB-LR-23) as a `**Resolution (proposed by P2 consilium)**` proposal. **P2 never flips the PA, edits a spec/design/tasks file, or finalizes a DEC** — the owner ratifies → commits the DEC → edits the specs (or orders P3/P5 to implement the chosen option). **(3) `skills/orchestrator/architecture-consilium.md`** — the prior methodology (each lens + its blind spot), the jury-not-debate rule, the `ArchVerdict` structured schema with explicit anti-pattern field-name warnings (a drifted field silently loses a veto), and the synthesis + recommendation-not-auto-decide boundaries. Registered in `commands/orchestrator/run.md` (table + pre-flight — P2 is the one process that does NOT need cc-sdd — + launch + after-run return contract; the "P2 still deferred" note removed), `orchestrator/README.md` (structure + status), and `docs/orchestrator-module/SPEC.md` (P2 marked built), and fixes a stale `verify.md` line ("P2/P4/P6/P7 processes deferred" → "dispatches P2–P7"). Tests: new `consilium-synth` 15/15 (matrix/veto worst-of/rank tie-break/STRONG·SPLIT·NONE/panel honesty/unknown-prior drop/an end-to-end CLI seam over a temp verdicts fixture) + new `decide-architecture-foundation-wiring` 9/9 (FSM order, lib-is-backbone, jury-parallel-no-consensus, the recommend-not-decide boundary, PA_CANON, the ≥2-options guard, the envelope) + auto-covered by the generic `args-parsing` (FB-001 guard) and `workflow-syntax` smoke; wired into `npm run test:orchestrator`; `npm run verify` EXIT=0. **Additive** — net-new lib + process + skill, no artifact-type/validation-rule change → counts unchanged (24/44); the process map is generated from an editorial overlay (not a `.mjs` scan) so it is untouched. **This completes the Orchestrator process chain P1–P7 + the two-way §6 capability channel — the module is COMPLETE, which removes the PILOT POINT blocker.** Deferred: the live dogfood grade on the S7 fork `PA-040/042` (a different fork than RUN 01's stack-choice → closes open-Q#7). Design contract: `dev/ORCHESTRATOR_P2_KICKOFF.md`; RUN 01 harvest: `dev/ORCHESTRATOR_DOGFOOD_RUN_01.md` §1 E1; concept: `dev/ECOSYSTEM_VISION.md` Epic D.
- **Интерактивная BPMN-карта всех процессов — `docs/guide/ecosystem-processes.html`** (DEC-DEV-0121). Граф «реально всех» процессов экосистемы с drill-down, зумом и сворачиванием узлов **вместе с их связями** (то, чего не было в card/list-карте `ecosystem-map.html`). Иерархия **lane (модуль) → процесс → шаг/гейт**: 5 lane · 20 процессов · 108 шагов · 166 узлов · 277 рёбер — P1.A Discovery (D1.1→G1→…→G5→D1.5z), P1.B Planning, P2 Feature (F.1→F.10 + NFR Ask/Define), P2.5 Design (D.1→D.6), approve-механика (стандартный/auto путь), BG-extraction (Phase 1–4 + mass-rename), Cascade BFS, Product DA (classify→cosmetic/6-lens→reception), Orchestrator P3o→P6o (+P7), Integrator add 6-стадий + maintenance, Ecosystem setup/feedback. **Движок — Cytoscape.js** (вендоренные либы в `docs/guide/vendor/`, полностью офлайн, как остальная карта): нативный zoom/pan колесом, расширение expand-collapse сворачивает любой compound-узел И пучкует его рёбра в meta-edge, переключаемые **слои связей** (поток / делегация / produces / consumes / каскад / lineage), поиск с авто-раскрытием предков, side-panel по клику (для шага — хуки с event/blocking/source + правила валидации с severity/scope; для артефакта — tier/cardinality/lineage; навигация по артефактам), фильтр «только shipped», легенда, zoom-контролы. **Хуки (18) и правила (44) — метаданные в панели, не отдельные узлы** (чище визуально, «досконально» через drill-down). **Генерируется из SSOT, не правится руками** (Tier-2 anti-drift, как `gen-ecosystem-map`/`gen-command-catalog`): редакторский overlay `docs/guide/process-graph.overlay.json` + sibling-генератор `dev/meta-improvement/scripts/gen-process-map.cjs` (переиспользует `ecosystem-map.overlay.json` для гейтов/правил/lineage — единый источник), `--selftest` (10 классов парити ссылок) + `--check` (в `npm run verify`). Линкуется из футера `ecosystem-map.html` и `docs/MAP.md`. **Правьте overlay/шаблон, не сгенерированный HTML.** Consumer-zone (`docs/`); аддитивно, без новых типов/правил → counts без изменений (24/44).
- **BPMN-карта — ad-hoc группировка узлов «в моменте» (шорткат `G`/`U` + кнопки)** (DEC-DEV-0122). На карте процессов `ecosystem-processes.html` теперь можно **выделить произвольный набор узлов** (Shift+рамка по холсту или Shift+клик) и **обернуть их в свёртываемую ad-hoc-группу** на лету — клавишей **`G`** или кнопкой «⊞ Сгруппировать»; **`U`** / «⊟ Разгруппировать» возвращает узлы в исходные дорожки и удаляет группу; «Сброс» очищает все ad-hoc-группы. Группа визуально отличается (пунктирный голубой контур) от SSOT-компаундов (lane/процесс), создаётся развёрнутой и сворачивается стандартным cue ⊖ (свёртка пучкует её рёбра в meta-edge, как у любого compound). Инструмент **декластеризации «в моменте»** — session-only (refresh очищает), не пишется в overlay. **Это runtime-аффорданс шаблона** (`ecosystem-processes.template.html`), не данные: overlay/генератор/selftest не тронуты, парити не изменилось. Само-проверено в реальном Chrome (puppeteer-core headless → 12/12 ассертов, 0 JS-ошибок). Consumer-zone (`docs/`); counts без изменений (24/44).
- **BPMN-карта — детерминированный self-test смоук в `npm run verify` + раскладка dagre→fcose** (DEC-DEV-0123). **(1) Self-test инфра:** новый `tests/guide/procmap.smoke.mjs` рендерит сгенерированную карту в **уже установленном** Chrome/Edge через `puppeteer-core` (executablePath — **без скачивания браузера**) и детерминированно ассертит: 0 pageerror/console.error, число узлов/рёбер против data-island после `expandAll`, отсутствие NaN-позиций, ненулевые extents, 0 висячих рёбер, раунд-трип ad-hoc группировки и **lane-overlap** (sibling-дорожки не должны грубо пересекаться). Скриншот = артефакт для глаза, **не гейт** (vision-на-скриншоте как единственный оракул ненадёжен). **Грейс-скип** (exit 0) при отсутствии браузера/`puppeteer-core` — `verify` не падает на машинах без браузера. npm-скрипт `smoke:procmap`, вплетён в `verify`; `puppeteer-core` — devDependency (он браузер **не** качает). **(2) Раскладка dagre→fcose:** dagre игнорировал compound-вложенность (5 дорожек налезали друг на друга, дети вываливались, пул артефактов разреженный) — заменён на **fcose** (compound-aware force, вендорнут офлайн `layout-base`+`cose-base`+`cytoscape-fcose`; dagre-вендоры удалены): module-лейны раздельные (overlap 0–4% против ~100% у dagre, под регрессионным гардом смоука), дети вложены, артефакты компактны. `LAYOUT_FULL` (initial/relayout/группировка) + `LAYOUT_INC` (drill через expand-collapse — открытие процесса уточняет, а не перетряхивает карту), кнопка «Перераскладка». **(3) Косметика:** `line-color:'var(--seq)'` → литеральный `#8b949e` (CSS-переменные не резолвятся в cytoscape `style`); регистрируется только fcose (снят warning повторной регистрации expand-collapse). Опционально (документировано, не включено): Playwright MCP `--headless --browser msedge` как standing browser-capability. Само-проверено в Chrome (`smoke:procmap`, 0 JS-ошибок); `npm run verify` EXIT=0. Consumer-zone (`docs/`) + dev-тест; counts без изменений (24/44). _(Раскладка fcose в этом же `[Unreleased]` далее заменена на ELK ради таймлайн-порядка — см. DEC-DEV-0124 ниже.)_
- **BPMN-карта — раскладка fcose→ELK: процессы по таймлайну + шаги по порядку прохождения** (DEC-DEV-0124). По дефолту процессы теперь расположены **в порядке таймлайна использования** (Discovery → Planning → Feature → Design → Orchestrator P3→P6 → …), а при раскрытии процесса его шаги открываются **в порядке прохождения** (слева направо по `next[]`), а не «где попало». Движок раскладки заменён с force-directed **fcose** на **ELK layered** (directed + compound-aware + **детерминированный**; вендорнут офлайн `elk.bundled.js` + `cytoscape-elk`, fcose-вендоры удалены): `elk.direction=RIGHT` + `considerModelOrder` поднимают таймлайн из авторского порядка процессов и crossEdges в раскладку, sequence-рёбра дают traversal-порядок шагов; детерминизм (нет randomize) → раскрытие всегда одинаковое, не «где попало». ELK асинхронен (elkjs promise-based) → фит по `layoutstop`, смоук ждёт через флаг `__layoutRunning`. Смоук `procmap.smoke.mjs` дополнен ассертами порядка (timeline-монотонность процессов + traversal-forward шагов раскрытого процесса) — **13 проверок**. Раскрытый процесс ложится **ровной горизонтальной лентой** — близко и без зигзага (DEC-DEV-0125): раскладка считает только флоу-рёбра (`sequence`/`delegate`), исключая скрытые overlay-связи к артефактам, которые тянули шаги в диагональную лесенку (P1A 14 шагов: 8 рядов→1, P2A 13: 12→3); node-placement `BRANDES_KOEPF` выравнивает в прямые линии. Все манипуляции (раскрытие/свёртка/группировка/тулбар) идут через единый фильтрованный `runLayout` → ровно «при любых манипуляциях». Само-проверено в Chrome: процессы монотонны по таймлайну, раскрытый P1A 13/13 sequence-рёбер вперёд + 1 ряд (без лесенки), lane-overlap <5%, extents компактнее (3996×891), 0 JS-ошибок; смоук **14 проверок** (+гард ровности); `npm run verify` EXIT=0. Consumer-zone (`docs/`); counts без изменений (24/44).
- **Orchestrator P7 `runtime-smoke-readiness` — the runtime gate after a P6 GO: "tests pass AND does the app actually start?"** (DEC-DEV-0120; Phase 5B of the "module complete" plan). RUN 01's runtime leg (E6 / P2-1) booted the dev server with **223 GREEN tests and got a 500** — the process never loaded `.env` (+5 infra gaps). The lesson: **"223 tests green ≠ application starts."** P7 is the gate that closes that gap — the last check before the "до прода" segment (Vision Epic E). Built as the **same two-leg split as 5A**: a deterministic **READINESS leg** now, a substrate-gated **EXECUTION leg** deferred. **(1) `orchestrator/lib/runtime-readiness.cjs`** — a deterministic, dependency-free, dual-use core (require exports + a `require.main` CLI an agent runs via Bash and relays as JSON, the orchestrator-lib convention, DEC-DEV-0073 §D.1): `detectRunTarget` (a boot command from `package.json` scripts.dev|start|serve or a declared runtime command), `bootBlockingCaps` (a §6 `BLOCK` capability is a hard boot blocker; a `DEFERRED` one — a dev stand-in covers it — is **not**, it is disclosed), and `assessReadiness` → `verdict ∈ {READY_TO_SMOKE, BLOCKED_ON_CAPABILITY, ENV_NOT_READY, NOT_STARTABLE}` (precedence NOT_STARTABLE > BLOCKED_ON_CAPABILITY > ENV_NOT_READY > READY) + `disclosures[]` (mock-only boot / non-GO P6 / DEGRADED env — "a green boot is indicative, not proof") + `smokePlan` (success-signals + the RUN 01 failure-class taxonomy: env-not-loaded 500, missing-migration, port-in-use, missing-runtime-secret, dependency-down). It **composes the §6 manifest, not duplicates it** — the boot-blocking capabilities are the SAME `external_capabilities` the 5A `capability-probe` dispositions; the process passes `--feature` and the lib reuses the probe, so disposition is computed once, deterministically. **(2) `orchestrator/processes/runtime-smoke-readiness.mjs`** — the P7 Workflow (Assess→Smoke→Report): pre-flight env-readiness → relay the runtime-readiness verdict → branch — `BLOCKED_ON_CAPABILITY` emits a §6 capability-request into the **canonical** `pending-actions.md` (reuses `PA_CANON`; route Integrator/Product, OD7 await) and **does NOT boot or mock**; `NOT_STARTABLE` records a tracking item routed to Product (a scaffold gap — never invents a boot command); `ENV_NOT_READY` is transient (bring the env up, re-run); `READY_TO_SMOKE` runs a live boot + diagnoses a failed start **capture-don't-fix** (surface for a P5/P6 re-run, never auto-remediate) → Report discloses the caveats. The two-axis contract `verdict × readiness` is consistent with the P5/P6 gate. **The live boot is substrate-gated** — a real grade needs a pilot dev env + a GO'd feature, and the full Epic E deploy/rollback chain needs Integrator D3-runtime tooling that does not exist yet; the `bootSmoke:false` knob runs the readiness leg only (assess + disclose, no boot) — the explicit bridge while execution is gated. Registered in `commands/orchestrator/run.md` (table + pre-flight + launch + after-run return contract), `orchestrator/README.md` (structure + P7-flow), and `docs/orchestrator-module/SPEC.md` (P7 ❌→⚠: readiness leg built, execution gated). **Fail-loud, not fail-open** (independent adversarial review): the `--feature` §6 probe distinguishes a genuine failure (probe unloadable / feature unresolvable / read throws → `capabilities_unknown` + a loud disclosure) from a feature that legitimately declares no caps (`[]`), so an unprobeable feature never silently reads as a clean `READY_TO_SMOKE` (it discloses the gap; the verdict is not false-blocked — the live boot's `missing-runtime-secret` class is the ground-truth backstop, per the DEC-DEV-0112 doctrine). Tests: new `runtime-readiness` 21/21 (run-target detection incl. whitespace trim, boot-blocking lens, verdict precedence, disclosures, smoke plan, `capabilities_unknown` fail-loud, **an end-to-end CLI seam over a temp fixture** — a BLOCK manifest flows to `BLOCKED_ON_CAPABILITY`, env presence ⇒ READY, a missing feature ⇒ `capabilities_unknown`) + new `runtime-smoke-readiness-wiring` 12/12 (assess→branch→report order, all four verdicts, BLOCK→§6-request with no auto-provision, capture-don't-fix, the bootSmoke knob, PA_CANON, the envelope incl. `capabilities_unknown`) + `smoke:orchestrator` parses the new process (6/6); wired into `npm run test:orchestrator`; `npm run verify` EXIT=0. **Additive** — net-new lib + process + two tests, no artifact-type/validation-rule change → counts unchanged (24/44). Deferred: the live boot grade + OD7 execution + the full Epic E deploy chain (all need Integrator D3-runtime). RUN 01 harvest: `dev/ORCHESTRATOR_DOGFOOD_RUN_01.md` §1 E6; concept: `dev/ECOSYSTEM_VISION.md` Epic E.
- **Карта экосистемы `ecosystem-map.html` — теперь генерируется из SSOT + обогащена** (DEC-DEV-0118). Карта больше не правится руками: `dev/meta-improvement/scripts/gen-ecosystem-map.cjs` собирает её из **SSOT** (набор команд из `commands/**` frontmatter, каталог 24 артефактов, счётчик 44 правил, существование hook-файлов гейтов) + **редакторского overlay** `docs/guide/ecosystem-map.overlay.json`, инжектя данные в шаблон `docs/guide/ecosystem-map.template.html`. `--check` (regenerate-and-diff, CI/commit-гейт) и `--selftest` (целостность ссылок) — антидрейф по образцу `gen-command-catalog.cjs` (DEC-DEV-0105); ручная `META_DATE` заменена штампом `date + git SHA + счётчики`. **Правьте overlay/шаблон, не сгенерированный HTML.** Обогащение содержимого: слой **гейтов и хуков** «что меня заблокирует и почему» (блокирующие/warn + обход + исходник), **процессы P1–P6** дорожками по владельцу, **родословная артефактов** + кто создаёт / кто потребляет, **поверхность валидации** (44 правила, развёртываемые таблицы по severity), **горизонт** planned-пунктов (намеренно отделён от shipped-фильтра). UX: **URL deep-linking** (шарабельные фильтры + back-button), фильтры по модулю и статусу, двунаправленная подсветка артефакт↔команда, ссылки на исходники команд (+ SPEC), клавиатура (`/` поиск, `Esc` сброс, `←/→` стадии пайплайна), реверс-индекс «я хочу…», доступность (`:focus-visible`, `prefers-reduced-motion`, aria-labels) и print-стиль для офлайн-обзора. Consumer-zone (`docs/`, `dev/`-script); counts без изменений (24/44 — карта их отображает, не добавляет типов).
- **Orchestrator §6 capability detect-leg — the channel now DETECTS a deferred provider gap, not just handles a BLOCK** (DEC-DEV-0117; closes the #3/#4 remainder of DEC-DEV-0081 / S6; Phase 5A). S6 root cause: the §6 capability-channel was a **block-handler, not a gap-detector** — it fired only when an implementer emitted a blocking signal, so a spec-mandated Mock that made a deferred provider (DeepL/ElevenLabs/Whisper) non-blocking left the channel silent and a GO shipped with the real provider seam hidden. Fixes #1/#2/#5 (MERGED) fixed the *plumbing* (a reported CONCERN is propagated, GO discloses it). This adds the missing **DETECT-leg**: the orchestrator itself enumerates a feature's declared external capabilities at **pre-flight** and dispositions the absent ones — it no longer waits for the implementer to notice. Three pieces. **(1) `external_capabilities` manifest** — an OPTIONAL `FM-*` frontmatter list (`{capability, secret_env, provider, tier, dev_stand_in}`) documented in `docs/pmo/artifacts/FM.md`; **absent == [] == old behaviour 1:1** (backward-compat). Because `tier` + `dev_stand_in` are **declared**, the block-vs-deferred disposition is **deterministic** (read it), not a heuristic — defusing the dead-rule / noisy-rule risk the S7 brief warned of (a heuristic would fire on every mocked seam). **(2) `orchestrator/lib/capability-probe.cjs`** — a deterministic, dependency-free, dual-use probe (require exports + a `require.main` CLI an agent runs via Bash and relays as JSON, the orchestrator-lib convention, DEC-DEV-0073 §D.1): reads the manifest + checks env presence → per capability `{present, disposition ∈ SATISFIED|EXPECTED_ABSENT_BUT_DEFERRED|BLOCK, provider_choice_pending, routes, surface, rationale}` + a summary. Access routes **Integrator**; an undecided provider (`TBD`) routes **Product** (OD8); the two axes are orthogonal. **(3) wiring in `feature-to-tdd-impl.mjs`** — a **pre-flight** detect-leg (CODE, not LLM initiative: an agent only relays the lib JSON, like the env probe) that runs the probe BEFORE the task loop, proactively surfaces each absent capability as a tracking item in the **canonical** `pending-actions.md` (reuses the FB-LR-23 `PA_CANON` resolver — also closes the §6-writer leg of FB-LR-29), forwards them into the P6 gate + the fallback gate for **GO-disclosure**, and returns a new `capabilities` envelope key. **A BLOCK (no dev stand-in) is DISCLOSED + flagged for the OD7 escalate→await leg — never auto-provisioned or mocked**; the OD7 async `request→await→resume` *execution* and the live S7 grade are the **substrate-gated remainder** (they need a real, in-scope blocking provider gap = RL-002 vendor wiring) and stay tracked as the deferred S7 leg. Tests: new `capability-probe` 12/12 (manifest parse, backward-compat absent⇒[], deterministic disposition, routing, summary) + `feature-to-tdd-impl-wiring` 13→16 (detect-leg pre-flight order, PA_CANON surface + no-auto-provision, envelope/disclosure flow); wired into `npm run test:orchestrator`; `npm run verify` green. **Additive** — net-new lib + optional frontmatter field + an optional `capabilities` return key (absent == prior behavior), no artifact-type/validation-rule change → counts unchanged (24/44). Follow-ups: propagate the manifest into the product-authoring skill + backfill real specs (only a fixture carries it today); the OD7 execution + the S7 detect-leg live grade. Brief: `dev/ORCHESTRATOR_S7_BRIEF.md`; root cause: DEC-DEV-0081.
- **`/ecosystem:verify` — Orchestrator gate-contract delivery spot-check** (DEC-DEV-0107). A new Step 4.5 greps the installed `.claude/orchestrator/processes/*.mjs` for the current return-contract markers (`validators_incomplete` + `committed_under_non_ready` in `validate-feature-impl.mjs`, `conflicts.concat` in `feature-to-tdd-impl.mjs`) and reports ✓/❌ per marker, with a matching line in the summary block. **Why:** the version-drift check (Step 5) catches *"you never updated"*, but not *"the update ran yet a file didn't actually land"* — a **partial/aborted sync** (the DEC-DEV-0088 class: an aborted `/ecosystem:update` left later namespaces stale even though `ecosystem_version` re-stamped). Counting command *files* (Step 4) can't see it — the file is present but its **contents** are old. This is a **delivery presence** check, NOT a behavioral one (that the gate *behaves* correctly is validated by a live Orchestrator run, graded post-hoc — never here). The marker list carries the same explicit baseline-snapshot caveat as Step 4 (an absent marker is most likely a stale/partial update, but could be list-drift if the contract evolved and the list wasn't refreshed — DEC-DEV-0082 discipline); the non-drifting version (markers generated from the `.mjs` return objects) is a deferred follow-up. Consumer-zone (`commands/ecosystem`), no artifact-type/validation-rule change → counts unchanged (24/44).
- **Guide layer finalized — generated command catalog + UI-design node** (DEC-DEV-0105). Adds **`docs/guide/02-commands.md`** — a human "which command for X" catalog of all 43 slash-commands, **generated from `commands/**/*.md` frontmatter** by **`dev/meta-improvement/scripts/gen-command-catalog.cjs`** (`npm run gen:catalog`; `--check` is an idempotence/CI gate) — the Tier-2 anti-drift mechanism so the catalog never hand-rots (per the `docs/MAP.md` "generate, don't hand-maintain" decision). Adds **`docs/guide/04-ui-design.md`** — the Design Module (`has_ui`) node: when it activates (the `F.8` auto-trigger from `/product:feature`, or `/design:start`), the `D.1→D.6` flow, the seven `/design:*` commands, and what it produces (`MK/DS/NM/AM` → handoff §10). Completes the USER-layer guide (map + concepts + first-session + ui-design + implementation + generated catalog).
- **User-facing guide layer (`docs/guide/`) + interactive system map** (DEC-DEV-0105) — closes the doc-review finding that the ecosystem had a strong install guide and a deep reference/spec library but **no task-oriented operator guide** (a know-nothing could install + orient, but could not learn to *drive* the product loop, which was reconstructable only by chasing command footers and hitting refusal gates at runtime). Introduces an explicit **3-layer documentation model** — **USER** (`docs/guide/`, "how to do the work") · **REFERENCE** (`docs/`, "what it is") · **DEV** (`dev/` + `CLAUDE.md`, "how the ecosystem is built"). First artifact: **`docs/guide/ecosystem-map.html`** — a self-contained, dependency-free interactive map ("everything in one place"): a clickable **D1→D6 pipeline** (stage → command filter), all **43 slash-commands** as cards (when-to-use + `argument-hint` + produced-artifacts + an honest `shipped`/`partial`/`conditional` status badge — so the Orchestrator's built-and-live-validated P3→P6 is shown truthfully, and dead-end commands are excluded), the **24 artifact types** with review levels, a **glossary**, live search, and a "works today" filter. Statuses + descriptions are authored from the specs (v1, hand-maintained); frontmatter-generation is deferred to a later increment (anti-drift, per the `docs/MAP.md` "generate, don't hand-maintain" decision). Plus `docs/guide/README.md` (section index) and the **First Product Session walkthrough** (`docs/guide/01-first-session.md`) — a narrative from a fresh bootstrap through `init → plan → feature → handoff` on a worked example, with the real approve-gates (G1/G4/DRC/G5, per-artifact approve), the implement path (`integrator:add cc-sdd → orchestrator:run`), and refusal-gates framed as guardrails; extracted from the buried `processes.md §8/§9` transcripts. Adds the **mental-model + glossary primer** (`docs/guide/00-concepts.md`) — the D1-D6 pipeline, the draft→iterate→approve loop, review levels, the artifact-derivation chain, and the load-bearing acronyms, in plain language to read before the first session. Adds **`docs/guide/05-implementation.md`** — the delegated half (`integrator:add cc-sdd → orchestrator:run` P3→P4→P5→P6) with a decision table for reading the gate verdict on its two axes (`result` GO/NO-GO/MANUAL_VERIFY × `readiness` READY/DEGRADED/ENV_NOT_READY) plus `conflicts[]`, and the substrate-disclosure caveat (a GO over mocked real seams is GO-with-caveats). With these the **USER-layer guide is functionally complete** (map + concepts + first-session + implementation); the remaining **Tier-0 drift fixes** (stale README counts, dead-end command refs, contradictory Orchestrator status, frozen version-headers) and the frontmatter-generated command catalog (Tier-2) are separate follow-ups.
- **Bounded completeness-loop (core) — drive a feature's D1-D2B spec to handoff-DoR-sufficiency with a deterministic external stop-signal** (DEC-DEV-0098; Autonomous Pipeline Vision, Epic B / Increment 1, B1 core). The vision's highest-leverage lever (input quality compounds downstream), built with the research rails baked in from day one (vision §4 cluster 1 — self-grading-as-sole-stop is the documented failure mode). Three pieces: **(1) `hooks/product/lib/completeness-oracle.cjs`** — a deterministic, dual-use scorer (require exports + a `require.main` CLI an agent runs via Bash for the loop's stop-check) that indexes `.product/` and scores a feature against the handoff **Definition of Ready** (handoff-spec.md §7) — the τ anchor for "sufficient, not ideal" (B2): it computes B1 (FM in-progress), B2 (≥1 active SC), B3 (referenced BR active), B4 (every active SC has an active VC), B7 (has_ui ⇒ active MK), returns `{score, tau, met, gaps[], ambiguities[], delegated_unverified[]}`, and is **honest about what it does not compute** — B5/B6/B8 (BG coverage / V-01..11 / RPM-actors) are reported in `delegated_unverified`, never silently dropped, and a missing feature returns an explicit error, never a false 1.0; **(2) `skills/product/completeness-loop.md`** — the bounded wave (SCORE→SURFACE→CLASSIFY→RESOLVE→ESCALATE→RE-SCORE) with the hard rails: the stop authority is the **external** oracle + an iteration cap (never the generator grading itself), stop = `max_waves(3) ∧ (score≥τ ∨ Δscore<ε ∨ info-gain→0)`, decisions **escalate** (only resolvable derivations auto-fix, with verify-before-act + idempotent update-in-place per `dev/LOOP_READINESS_AUDIT.md` §5.3), and **no silent truncation** (a partial spec is never rounded up to "done"); **(3) `/product:complete <FM-NNN>`** command that loads the skill and runs the loop (the 20th product command). It fires the A1 personas as their canonical `subagent_type` and consumes the A2 router's zone routing — closing the Epic A→B chain. **v1 is core/skeleton**: the oracle + stop contract are enforced, the auto-fix step is intentionally conservative (surface + escalate by default) until pilot data calibrates what is safe to auto-resolve; the Epic D consilium escalation channel comes later. Tests: new `tests/product/completeness-oracle.test.cjs` (7 assertions — full-ready 1.0/met, each blocker's failure path, draft-SC ambiguity surfacing, missing-feature error, delegated-list completeness); `verify.md` product command count `19 → 20`; `npm run verify` + `check-counts` green. **Additive** — net-new lib + skill + command, no artifact-type/validation-rule → counts unchanged (24/44).
- **Zone→persona router — a deterministic hook that fires the right profile reviewer when a `.product/` zone is touched** (DEC-DEV-0098; Autonomous Pipeline Vision, Epic A / Increment 1, A2). Completes Epic A: the personas exist (A1), this wires them to *fire automatically* in their zone (the vision's "guarantee a profile agent is called when a step touches its area"). Three parts, all consumer-deployable: **(1) manifest `hooks/product/zone-routing.yaml`** — the agent-side SSOT mapping each PMO zone (`.product/business-rules`, `invariants`, `scenarios`, `features`, `nfr`, `verification`, `lifecycles`, `mockups`) to the persona(s) whose distinct prior adds value there (heterogeneous, never the whole panel; `mockups` covers MK **and** NM since both live there); **(2) router `hooks/product/zone-router.cjs`** — a pure, dependency-free, dual-use lib (`require` exports + a `require.main` CLI an agent can run via Bash and relay as JSON, the orchestrator-lib convention) that does `matchZone` (glob→suffix-regex, single-segment `*`), `classifyMagnitude` (deterministic cosmetic-vs-significant pre-filter, conservative — creation and any substantive body line ⇒ significant; only frontmatter-metadata / reference-list / whitespace ⇒ cosmetic), and `route` → `{zone, personas, magnitude, fire}`; **(3) hook `hooks/product/zone-change-trigger.js`** — PostToolUse Write/Edit, filters to `.product/`, matches a zone, computes the diff→magnitude, and on a fire appends/updates `.product/.pending/advisor-pending.yaml` (flat entries, **dedup by artifact id** so a loop-wave re-run UPDATES in place — idempotency per `dev/LOOP_READINESS_AUDIT.md` §5.3 / the DEC-DEV-0089 PA-dedup lesson; flat scalars sidestep the diff-yaml whitespace-ladder fragility of DEC-DEV-0023) + a stderr signal naming each persona's **canonical subagent_type** (not-found = STOP, never `general-purpose`). **The firing decision is CODE, not LLM judgment** — a persona fires iff its zone matched AND magnitude ≥ the zone's threshold, so the panel never burns tokens on a trivial edit (Epic A anti-over-engineering rail). The hook only **signals** (never spawns a subagent itself — same contract as `br`/`ic-change-trigger`). Registered in `hooks/product/manifest.yaml` (auto-deployed by bootstrap/update). Tests: new `tests/product/zone-router.test.cjs` (17 assertions — manifest parse, zone match incl. Windows separators + no-traversal, magnitude polarity, fire gate) + 2 new `smoke-hooks` cases (fires-on-significant, silent-on-non-zone); `npm run verify` + `check-counts` green. **Additive** — net-new hook + lib + data, no artifact-type/validation-rule → counts unchanged (24/44); `verify.md` tracks hooks only via the LESSON-gate self-check (no general hook count to drift). The bounded completeness-loop that consumes `advisor-pending.yaml` is B1.
- **Three profile-persona reviewers — `architect-advisor`, `qa-advisor`, `ux-advisor` — heterogeneous critic agents for the product completeness-loop** (DEC-DEV-0098; Autonomous Pipeline Vision, Epic A / Increment 1, A1+A3). The ecosystem shipped exactly one critic agent (`product-devils-advocate`, a *business* lens); the vision's completeness-loop (Epic B) and consilium (Epic D) both need a **registry of heterogeneous personas** so a profile reviewer fires when a process touches its zone. Adds three, each cloning the DA convention (frontmatter `name/description/tools/model` + Role "You ARE / You are NOT" + adaptive-depth cosmetic/significant + canonical structured-verdict frontmatter + explicit anti-pattern field-name list + Builder/Critic isolated context) but carrying a **distinct prior** (heterogeneity is mandatory — a homogeneous panel is groupthink, vision §4 cluster 2): **`architect-advisor`** (`agents/product/`) — feasibility-as-specified / structural decomposition / technical-risk / data-state modeling / integration seams; **`qa-advisor`** (`agents/product/`) — testability / acceptance-VC coverage / edge-cases / failure-recovery / observability / regression; **`ux-advisor`** (`agents/design/`, new directory) — usability / flow-completeness (happy+alt+error) / component-state coverage / accessibility / content clarity / consistency (UI-bearing scope only). Two design choices ported from sibling tracks: **findings are a DEFECT/GAP enum only** — a satisfied dimension is reported `clean: true`, never as a positive "finding" that could survive as an unresolved item (lesson DEC-DEV-0094); and **output is idempotent** — each persona writes to `.product/.advisor-findings/<persona>-<ARTIFACT-ID>.md` keyed on persona+artifact, **not** a timestamped filename, so a completeness-loop wave UPDATES in place instead of appending near-duplicates (lesson DEC-DEV-0089 + `dev/LOOP_READINESS_AUDIT.md` §5.3). **A3 — subagent-type contract** (`da-subagent-type-contract` pattern) applied to all three: each is spawned as its canonical `subagent_type`, an «Agent type not found» reply is a **loud blocking setup error** (STOP + surface), **never** a silent fallback to `general-purpose` + role-adoption — registration root-cause stays a separate live-harness fix, not a speculative bootstrap patch. Agents deploy via the existing namespace-aware `agents/` directory mirror (bootstrap/update) — no enumeration to update. **Additive** — net-new agent files, no artifact-type/validation-rule change → counts unchanged (24/44, `check-counts` green); `verify.md`/overview templates track agents only as a directory (no per-agent count to drift). The zone→agent router that fires these personas is the next increment (A2). Concept: `dev/ECOSYSTEM_VISION.md` (Epic A); kickoff: DEC-DEV-0098.
- **Upstream feedback — phase-2 receiving side: a consolidator that picks up the `/ecosystem:meta-feedback` outbox, unifies it with the FB-ledger + Session Audit, and dedupes against decisions already made** (DEC-DEV-0097; phase 2 of DEC-DEV-0090). DEC-DEV-0090 split feedback into a project-local contour (`/product:validation-tune`) and an upstream one (`/ecosystem:meta-feedback`, which captures **systemic** ecosystem defects into a committed pilot outbox `.product/.upstream/feedback-outbox.md` as `UF-NNN`); v1 shipped the **capture** side and deferred the **receiving** side. This builds it as an **ecosystem-repo / dev-only** tool (`dev/meta-improvement/scripts/feedback-intake.js`, like `audit-journal.js` — **not** shipped to pilots) doing the three phase-2 jobs: **(1) auto-pickup** — parse a pilot's `UF-NNN` outbox (and, optionally, the Orchestrator FB-ledger + the Session Audit `audit-journal.ndjson`) by code; **(2) unified finding contract** — normalize all three sources onto one shape (`uid / source / source_ref / severity / signature / status`), reusing `audit-journal.js`'s signature space so it consolidates rather than duplicates; **(3) dedupe against `DEV_JOURNAL.md`** — each finding gets a disposition: `ported` (self-declares a `DEC-DEV-NNNN` that exists in the journal), `likely-ported` (a `DEC-DEV` entry names its ref, or it cites a `DEC-DEV` the journal lacks → flagged for a human check, no silent dedupe loss), or `open` (needs upstream triage). **Capture-don't-fix invariant holds:** it reads + reconciles + reports only — never edits an artifact, never writes the outbox, never auto-applies; acceptance (turning `open` into a `DEC-DEV-*` patch) stays a human act. The `/ecosystem:meta-feedback` skill + command now point to it (their "phase 2" forward-notes are fulfilled); `SESSION_AUDIT_GUIDE.md` documents it as the upstream-feedback receiving side. Validated on the repo's real FB-ledger + `audit-journal.ndjson` (108 findings reconciled; correctly surfaced a stale ledger route whose finding was resolved but never DEC-DEV-stamped). Tests: new `feedback-intake` 11/11 (3 sources, all dispositions, severity mapping, end-to-end); `npm run verify` + `check-counts` green. Dev-only tooling → counts unchanged (24/44), no consumer artifact-type/rule. Cross-pilot aggregation of multiple outboxes is still deferred. Plan: `dev/FEEDBACK_CONTOUR_SPLIT_PLAN.md`.
- **Orchestrator remediation-discretion guardrails — a remediation can no longer mask a cross-spec/design conflict by picking a side, and a transient impl-block auto-retries** (DEC-DEV-0096; N+2 queue P5 / T5, FB-LR-07 / FB-LR-08). The riskiest queue item — agent **concurrency**. Live-run B/C surfaced two failure modes the remediation loops had no discretion against: **(FB-LR-07)** when a remediation hit a contradiction *between* specs/requirements (the `had_trial` trial-seam: two requirements disagree) or a *design self-contradiction* (`card_last4`), one agent could **resolve it unilaterally and commit** — "winning" over siblings that correctly blocked, and **masking an upstream conflict** that should have escalated to a product decision; and **(FB-LR-08)** a *transient* impl-block (a locked git index / a flaky install / a momentarily-down substrate) was treated like a content gap — one debug round then skip, needing a manual re-drive. **New deterministic lib `orchestrator/lib/remediation-guard.cjs`** (run by an agent via Bash, relayed as JSON — like `env-readiness`/`coverage-oracle`, DEC-DEV-0073 §D.1): `classifyBlock(text)` → a discretion verdict (`transient` ⇒ bounded auto-retry · `cross-spec-conflict`/`design-contradiction` ⇒ escalate, never self-resolve · `capability` ⇒ Integrator/Product request · `content`/unmatched ⇒ debug-block, the conservative default — uncertainty is **not** a retry, and a **conflict signal always outranks a transient one** so a real contradiction is never papered over), plus `detectsUnilateralResolution(note)` — the anti-mask backstop that flags a "fix" note admitting it resolved a contradiction by picking a side (fires only on the conjunction of a resolution verb AND a contradiction context, so a routine fix is not flagged). **P6 (`validate-feature-impl`):** remediation is documented + asserted **single-writer** (strictly sequential, one commit at a time — extends FB-004's cross-run rule to within-run); the remediation agent classifies a block it cannot fix, re-confirms the defect is still present before committing (a sibling fix ⇒ `resolved-by-concurrent-commit`, no double-commit), and self-checks its fix note; a cross-spec/design conflict **escalates** to a `pending-actions` CONCERN (new `escalateConflict`) and is tracked in a new **`conflicts`** return field — a non-empty `conflicts` **degrades a would-be GO to `MANUAL_VERIFY_REQUIRED`** (never NO-GO — the code may be fine pending the decision). **P5 (`feature-to-tdd-impl`):** a BLOCK is classified before a debug round is spent — `transient` gets a **bounded auto-retry** (re-probe env, retry, `maxTransientRetries` default 2, no debug round consumed); a cross-spec/design contradiction is **escalated** (recorded with the upstream route, tracked in `conflicts`, counts as `blocked` ⇒ the gate runs advisory, never a clean GO); the implementer prompt forbids a unilateral resolution. `commands/orchestrator/run.md` updated (preflight, the `remediationGuard` launch arg, the new `conflicts` return key on P5+P6, "After the run" discretion guidance, the FB-004 in-run single-writer note). Tests: new `remediation-guard` 12/12 (adversarial — a transient-flavoured reason that is actually a conflict ⇒ escalate; a clean-sounding fix note that buried a contradiction ⇒ flagged; false-positive guards) + new `feature-to-tdd-impl-wiring` 8/8 (fills a prior P5 wiring-coverage gap) + `validate-feature-impl-wiring` 19→24; `npm run verify` green. **Additive** — `conflicts` is a new optional return field, the schema fields are additive optional (absent == prior behavior), no new artifact-type/validation-rule → counts unchanged (24/44). Findings ledger: `dev/ORCHESTRATOR_LIVE_RUN_FB_LEDGER.md`; work-order: `dev/ORCHESTRATOR_N2_GATE_CONTRACT_WORKORDER.md`.
- **Orchestrator P4 design→tasks structural-coverage gate — catches a design module no task builds (the unmounted-API gap), pre-impl** (DEC-DEV-0095; N+2 queue P4 / T4, FB-LR-05). `design.FileStructure ⊆ ⋃ tasks.boundary` was a blind spot of every existing oracle (coverage-oracle = requirement→presence, fidelity-oracle = spec→`.product`, RA-10 = cross-task seams *post-impl*) — in the pilot a missing `admin.module.ts` assembly task shipped a fully-unmounted API while all gates stayed green. New deterministic lib **`orchestrator/lib/design-coverage-oracle.cjs`** (run by an agent via Bash, like the other oracles): extracts the design's File-Structure file list + scans `tasks.md` from ground-truth text → **`uncovered_design_files`** (a design file whose basename appears in no task) + a **T4-lite dangling-forward-reference linter** (flags vague "wired up in a later task" deferrals — partial, candidate-only). P4 (`audit-spec-fidelity`) runs it per feature as a **hybrid** layer (deterministic candidates → a semantic confirm for naming/path variance → only confirmed gaps kept) orthogonal to the fidelity audit; a confirmed gap **excludes the feature from `impl_ready`** and is routed `spec` (a spec-completion pending-action recommending the missing assembly/wiring task). **Conservative:** lenient toward "covered" (only a file mentioned *nowhere* is flagged); **surfaces + routes, does NOT auto-add tasks** (a missing task is for the spec author / a P3 re-run). `commands/orchestrator/run.md` updated (preflight, launch arg `coverageOracle`, the new `coverage_gaps` return key, "After the run" guidance, and "run P4 between P3 and P5"). Tests: new `design-coverage-oracle` 6/6 + `audit-fidelity-wiring` 9→10; `npm run verify` green. Additive return field (`coverage_gaps`) — counts unchanged.
- **Orchestrator gate-outcome contract — `verdict × readiness` two axes + shared `env-readiness` probe** (DEC-DEV-0092; Phase N+2, work-order `dev/ORCHESTRATOR_N2_GATE_CONTRACT_WORKORDER.md`). Closes the live-run-B false-NO-GO (FB-LR-02/04/09): a **down substrate** (Docker/Postgres/Redis off → build GREEN but the suite RED on `PrismaClientInitializationError` / `ECONNREFUSED :5432|:6379`) made P6 synthesise `NO-GO` — the gate was scored as "the code failed" when it never got to judge. The fix models a gate outcome on **two orthogonal machine-readable axes**: `verdict ∈ {GO, NO-GO, MANUAL_VERIFY}` ("is the code good?") × `readiness ∈ {READY, DEGRADED, ENV_NOT_READY}` ("did the gate get to judge?"), with a code-invariant **`ENV_NOT_READY ⇒ MANUAL_VERIFY_REQUIRED` (never NO-GO)** decided *before* the `!mechPassed → NO-GO` branch; only `READY`/`DEGRADED` may pair with `GO`. New deterministic lib **`orchestrator/lib/env-readiness.cjs`** (run by an agent via Bash, like `coverage-oracle`/`fidelity-oracle`): a `probe` mode (docker / `pg_isready` / redis / `prisma migrate status` — migration-history integrity closes FB-LR-09) + a `classify-failures` mode (the substrate-error allowlist — a RED suite is an env artifact only if **every** failure matches, a single real failure defeats it). **P6 (`validate-feature-impl`)** runs the probe BEFORE the suite, classifies failures, and carries `readiness` in `MECH_SCHEMA` + its return; **P5 (`feature-to-tdd-impl`)** runs a pre-flight probe, forwards the hint to P6 (worst-of with P6's own probe), maps blocked-tasks `degraded → DEGRADED`, and returns `readiness`. `commands/orchestrator/run.md` updated (preflight, launch args, return keys, "After the run" `readiness × verdict` guidance — same increment, so the new field is not silently ignored). Tests: `env-readiness` 8/8 + `validate-feature-impl-wiring` 13→17 (probe-before-suite, the invariant order, P5 forwarding); `npm run verify` green. **Soft-migration (backwards-compat-narrow):** both axes are **additive optional** return fields — an absent `readiness` == `READY` == today's exact behavior; existing `result`/`go_gate` tokens stay valid, so pilot state and any `result`-keyed caller are untouched. Pilot re-validation (re-run C nested + re-run B with Docker down) is a separate live session.
- **Orchestrator P4 `audit-spec-fidelity` — pre-impl spec-vs-`.product` fidelity gate** (DEC-DEV-0084; Phase N+1a). New process (`/orchestrator:run audit-spec-fidelity --feature <slug>`) auditing generated cc-sdd specs against the `.product` source for fidelity drift BEFORE impl — a distinct axis from C-07 (handoff→brief mapping), coverage-oracle (presence), and cc-sdd cross-spec review (specs agreeing with each other). **Two layers:** a deterministic trace-integrity oracle (`orchestrator/lib/fidelity-oracle.cjs` — every id the spec references must EXIST in the `.product` ground truth; catches fabricated traces like the RUN-01 fictitious `IC-013`, by code not judgment; reuses coverage-oracle's id-extraction) + an inline `fidelity-auditor` role for semantic drift (value mismatches, contradicted/re-scoped rules, stale entities, weakened acceptance). **Triage:** `spec-defect` → fix the spec + auto-re-audit (P1-2, bounded — remediation can introduce new drift); `product-defect` → routed to Product via `.claude/pending-actions.md` (the **OD8 reverse channel** — the spec is NOT patched around a defective canon and `.product/` is NOT edited). Ships `skills/orchestrator/audit-spec-fidelity.md` + `commands/orchestrator/run.md` wiring + tests (`fidelity-oracle` 7/7, `audit-fidelity-wiring` 7/7; `npm run verify` green). Full P6 `validate-feature-impl` (N+1b) ships alongside (next entry).
- **Orchestrator P6 `validate-feature-impl` — feature-level GO/NO-GO gate after impl** (DEC-DEV-0085; Phase N+1b). New process (`/orchestrator:run validate-feature-impl --feature <slug>`) that replaces the thin inline `kiro-validate-impl` lift P5 used. It runs a **mechanical layer** (the full test suite + build must be green — not "all tasks `[x]`") + **three parallel validators**, each a distinct lens per-task review cannot give: `requirements-coverage` (RA-8; every requirement has BOTH impl and test — reuses `coverage-oracle` as the anti-self-report backbone), `design-alignment` (RA-9; impl honours design.md, no silent re-design), `integration-boundary` (RA-10; cross-task seams wired — orphan export FB-010, emitted-but-unhandled events, a caller using a route the producer never registered like RUN 01's `/reset` vs `/reset-password`). Its core value is **verify-finding-before-act**: a validator finding is confirmed by grepping ground truth BEFORE any remediation — refuted findings are dropped, never chased; confirmed ones are remediated bounded (≤3 rounds) and re-verified. The verdict is **synthesized deterministically** (`GO` only if mechanical-green AND no unresolved confirmed finding AND no upstream block; a high-severity residual forces `NO-GO`, else `MANUAL_VERIFY_REQUIRED`). Deferred-capability CONCERNS forwarded from P5 are disclosed in the verdict (FB-013). **P5 (`feature-to-tdd-impl`) now delegates its feature-level gate to P6 via `workflow()`**, with a fallback to the inline `kiro-validate-impl` lift if nested-workflow execution is unavailable. Ships `commands/orchestrator/run.md` wiring + tests (`validate-feature-impl-wiring` 13/13; `npm run verify` green). Live run (a pilot with an implemented feature) is a separate session. **Live-run outcome (DEC-DEV-0091): P6 as a standalone gate is now live-validated** (pilot `my-first-test` — B `billing` + C `admin`); it caught a real cross-feature regression an advisory gate had GO'd over. Two gaps surfaced and are queued: the P5→P6 *delegation* path was broken (fixed below) and a **down datastore yields a false NO-GO** (the next increment introduces a `verdict × readiness` outcome contract — see `dev/ORCHESTRATOR_N2_GATE_CONTRACT_WORKORDER.md`).

- **Feedback contour split — `/product:meta-feedback` → `/product:validation-tune` (local) + new `/ecosystem:meta-feedback` (upstream)** (DEC-DEV-0090). The single `/product:meta-feedback` command conflated two jobs and its name promised a third: it *did* project-local validation tuning (writes the project's `validation-config.yaml`) but its name implied feedback **upstream to the ecosystem repo**, and it collided semantically with the Orchestrator `FEEDBACK-JOURNAL`. Split into two honest contours joined by a classification bridge. **Local (`/product:validation-tune` + `skills/product/validation-tune.md`, renamed):** project-local rule/severity tuning, upstream language scrubbed, journal tag `DEC-TUNE-*`. **Upstream (`/ecosystem:meta-feedback` + `skills/ecosystem/meta-feedback.md`, new):** captures **systemic** defects (a rule/process that misfires regardless of project) into a committable outbox `.product/.upstream/feedback-outbox.md` (`UF-NNN`, one counter = SSOT — the anti-fragmentation lesson from the per-run FEEDBACK-JOURNAL split), with **hybrid delivery** — local pickup (co-located, the `audit-watch.js` read model) or `--push`/`--issue` for a remote pilot; **capture-don't-fix** (acceptance into a `DEC-DEV-*` happens in the ecosystem repo). Receiving-side automation (auto-pickup, dedupe, unified contract with FEEDBACK-JOURNAL + Session Audit) is **phase 2**. **Bridge:** the oracle question «would this rule misfire the same way in *another* project?» — no → tune locally, yes → escalate. Reference sweep: live `product:meta-feedback` mentions repointed to `validation-tune`; `skill bug / A1 misfire` sites repointed to `/ecosystem:meta-feedback` (a skill defect is systemic, not project-local); `verify.md` ecosystem command count `5 → 6`; `CLAUDE.md.template`, README, ROADMAP, product/integrator SPECs, pmo `validation.md`/`processes.md` updated. **Migration (existing projects):** after `/ecosystem:update`, old `commands/product/meta-feedback.md` is purged and the two new commands appear; `.product/.pending/meta-feedback.yaml` → `validation-tune.yaml` (absent == prior behavior 1:1). Plan: `dev/FEEDBACK_CONTOUR_SPLIT_PLAN.md`.

### Changed

- **Orchestrator P4 `audit-spec-fidelity` — verify-finding-before-act before the spec-fix** (DEC-DEV-0087; parity with P6). P4 previously edited + committed the spec directly on a `fidelity-auditor` finding (the auto-re-audit caught only fixes that failed to converge, not a fix made on a hallucinated drift). Now each **semantic** spec-route drift is first **confirmed against ground truth** (grep the spec + the cited `.product` source); a refuted drift is **dropped, not fixed** — P4 no longer rewrites a spec around a drift that does not actually exist. The deterministic trace-integrity finding (`fabricated-trace`, from `fidelity-oracle`) stays exempt — it is already confirmed by code. Closes the asymmetry where P4 was the only orchestrator gate that remediated without up-front finding-confirmation.

### Fixed

- **Completeness-loop (Epic B) hardened for worktree / parallel safety — SURFACE anchors every persona brief's paths to one resolved root, ESCALATE writes the SINGLE canonical pending-actions ledger, and the advisor-findings write is now an explicit keyed step** (DEC-DEV-0140; Autonomous Pipeline Vision, Wave B / B-a; FB-LR-28/29 from the V-2 completeness-loop live-run `a2aaf44a`). The `skills/product/completeness-loop.md` skill was `core/skeleton` prose that never anchored worktree-safety into its wave steps, so a run inside a git worktree could mis-read a file or split its pending-actions — latent risks B-a closes **before** the durable wave-runner (B-b) compounds them. Three fixes, all skill-prose: **(1) FB-LR-28 — SURFACE path-anchoring.** The persona briefs mixed worktree-absolute and main-checkout-absolute paths (in the V-2 re-run only `qa-advisor` got the worktree-correct path; `architect`/`ux` got main-checkout paths and self-healed via Glob **only because** the file happened to be not-found). SURFACE now resolves the feature root ONCE and builds every persona brief's paths from that single root — a present-but-edited file handed at the wrong checkout is a **silent stale read** (no not-found to trigger a heal), so consistent anchoring is a correctness rail, not a nicety. **(2) FB-LR-29 — ESCALATE writes the canonical PA ledger.** The ESCALATE leg wrote a worktree-local `pending-actions.md` without the DEC-DEV-0113 `PA_CANON` resolution (that guard had landed only in orchestrator P4/P5/P6). Ported verbatim: resolve the single canonical file via `git worktree list --porcelain` (the main checkout), allocate the next `PA-NNN` from it, never `git add`/commit it — so a product-gate run concurrent with an orchestrator run cannot mint a colliding PA-id. Open-question resolved: **product-gate PAs SHARE the canonical ledger** (not worktree-local by design). **(3) Advisor-findings persistence — contract → step.** The keyed idempotent write (`.product/.advisor-findings/<persona>-<ARTIFACT-ID>.md`) was described only in the Idempotency section; it is now an explicit action in SURFACE (each persona writes there, keyed on persona+id, a re-run overwrites), and the Idempotency bullet is trimmed to reference it (single-source, no duplicate drift). **Skill-prose only** — no code / oracle / hook change, no artifact-type/validation-rule change → counts unchanged (24/44); `npm run verify` green. A worktree-context smoke that replays the `a2aaf44a` conditions without FB-LR-28/29 lands on the B-d pilot leg. Work-order: `dev/ECOSYSTEM_VISION_BATCH_2.md` (B-a); ledger: `dev/ORCHESTRATOR_LIVE_RUN_FB_LEDGER.md`.
- **Orchestrator P2 (`decide-architecture-foundation`) closes the two consilium blind spots the profiling A/B surfaced — the jury now reads the raw fork source (not only a lossy lift), and a weakness spread across all three lenses is no longer summed into a false "split"** (DEC-DEV-0135; profiling study DEC-DEV-0132). The P2 dogfood A/B (3-prior jury vs 1 general-purpose subagent) found two mechanism defects, both now fixed. **(1) Lossy-brief cap (Fix A).** The 3 architects saw ONLY the P2-lifted `ForkBrief`; when that lift dropped or mis-framed a load-bearing fact (verified on the PA-040 fork — the lift dropped "the worker already runs BullMQ" and called a NEW worker), the WHOLE panel inherited the loss. The Brief phase now also captures a verbatim `source_excerpt` (the raw PA block + cited constraint lines), and each architect reads it as the **ground truth** — on a fact disagreement the raw source WINS over the lift (the enumerated options are still exactly the fork's; the source is not a license to invent one). **(2) Distributed-weakness blind spot (Fix synthesis).** Independent fixed-lens scoring + a deterministic SUM could rank an option no lens actually endorsed as a viable "split" survivor, and a full-panel agreement on a weak-everywhere option read as `strong`. `consilium-synth.cjs` gains a **soft-veto** rule: a surviving option no lens scores ≥ 3 is flagged (never removed — only a hard veto removes), and a soft-vetoed top pick is **demoted out of `strong`** (agreement on the least-bad option is not a rubber-stamp). Complementing the deterministic rule, a **post-panel integration pass** (one reasoner reads the whole fork after the jury, adversarially) surfaces the cross-lens failures a per-lens sum cannot see — a distributed must-not-ship no single lens vetoed, or one lens's fact undercutting another's score — as an owner-facing disclosure; it is **surfacing-only** and never changes the deterministic recommendation (the jury stays; its value is insurance/auditability). **Additive** — `source_excerpt`, `soft_vetoed`, and the `integration` flag are new optional fields (absent == prior behavior), no artifact-type/validation-rule change → counts unchanged (24/44). Tests: `consilium-synth` 15→20 (soft-veto: flag/not-remove, strong→split demotion, hard-subsumes-soft), `decide-architecture-foundation-wiring` 9→12 (raw-source, soft-veto threading, surfacing-only integration pass); `npm run verify` green. Study: `dev/ORCHESTRATOR_P2_PROFILING_STUDY.md`.
- **Orchestrator process scripts pinned to LF — CRLF in the shipped `.mjs` broke the `scriptPath`-Workflow permission validator on Windows** (DEC-DEV-0114; FB-LR-27, N+2 OWED-batch live-run). The ecosystem repo's own `orchestrator/processes/*.mjs` were CRLF (`validate-feature-impl.mjs` = 513 CR / 513 LF), delivered to the pilot as-is; on Windows the harness `scriptPath`-Workflow permission validator rejects the CR byte (U+000D), so every orchestrator run had to LF-normalize the process script before launch and `git checkout --` to restore it. New root **`.gitattributes`** pins `*.mjs`/`*.cjs`/`*.js`/`*.sh` to `text eol=lf` so a fresh checkout/clone of the source produces LF working-tree files (overriding `core.autocrlf=true`) and `/ecosystem:update` copies LF (the committed blobs were already LF under autocrlf; the CRLF lived only in the checked-out working tree that delivery copies). **Pilot-side remainder (QUEUED):** an installed project's autocrlf will re-convert the delivered `.mjs` unless a `.gitattributes` is also delivered into the pilot's `.claude/` — that delivery + a one-time LF-normalize of the already-committed pilot copies is a follow-up (the source fix is not retroactive on the pilot). **Additive** — a build/delivery line-ending fix, no artifact-type/validation-rule change → counts unchanged (24/44). Ledger: `dev/ORCHESTRATOR_LIVE_RUN_FB_LEDGER.md`.
- **Orchestrator parallel-worktree PA-id safety — PA-writes now target a single canonical pending-actions file, not a worktree-local copy** (DEC-DEV-0113; FB-LR-23, Fork C live-run). Parallel git worktrees share one `.git`/checkout but have separate working trees, so the gate's PA-emitting prompts — writing to a CWD-relative `.claude/pending-actions.md` — let two concurrent runs each scan a DIFFERENT file and mint the SAME `PA-NNN` (G-1 minted `PA-027` twice); a remediation Edit also briefly hit the main checkout (reverted). The 5 PA-writes across the 3 processes (P4 product/coverage-route, P5 block/concern, P6 escalate-conflict) now carry a shared `PA_CANON` instruction: resolve the SINGLE canonical pending-actions file (the main checkout, via `git worktree list --porcelain`) once, allocate the next PA-id from it, and never commit it (it may live in another checkout) — plus a commit-zone advisory on the block-commit (a shared-worktree `.git` makes an explicit-path commit of only your zone the isolation; never `git add -A`). Backward-compatible: in a single checkout the canonical path == the prior relative one. Same shared-checkout hazard that bit manual parallel sessions, now surfacing in the orchestrator's autonomous write-path. **OPEN-remaining (low):** full cross-process commit serialization on the shared index is outside the prompt layer (single-writer + explicit-path keep "no content lost"). **Additive** — prompt instructions + a commit advisory, no new artifact-type/validation-rule → counts unchanged (24/44). Tests: P4 / P5 / P6 wiring each +1 (PA_CANON present + `git worktree list` resolver); `npm run verify` green. Ledger: `dev/ORCHESTRATOR_LIVE_RUN_FB_LEDGER.md`.
- **Orchestrator readiness probe — a USED substrate left unprobed (no `pg_isready`/`redis-cli`) is now `DEGRADED`, not a silent full `READY`** (DEC-DEV-0112; FB-LR-24, Fork C / G-2 live-run grade). `env-readiness.cjs` (the readiness axis of the P5/P6 verdict×readiness contract) marked a substrate the project **uses** but whose probe binary is absent as `skipped` — indistinguishable from "not used" — and `classifyReadiness` passed both `skipped` and `unknown` through as `READY`. So a host lacking `pg_isready`/`redis-cli` was declared fully `READY` having never directly confirmed Postgres/Redis (a "lying" READY; on the pilot 2 of 3 substrate checks were skipped, leaving READY resting on docker-up + a green suite). **Fix:** `probe` now marks a used-but-unprobed `postgres`/`redis`/`docker-daemon` as **`unknown`**, and `classifyReadiness` raises any `unknown` to **`DEGRADED`** (`down`/`inconsistent` still dominate as `ENV_NOT_READY`). DEGRADED — not ENV_NOT_READY — because an unprobed substrate is **not proven down** (a green suite is strong indirect evidence it is up): the gate discloses the unconfirmed substrate and an advisory GO stays possible, never a false block. (The migration-history check keeps `skipped` when the prisma CLI is absent — that is an extra integrity check over an already-probed DB, not a substrate-confirmation gap.) Revises the DEC-DEV-0092 stance "uncertainty never degrades". **Additive** — a behavioral probe fix, no new artifact-type/validation-rule → counts unchanged (24/44). Tests: `env-readiness` 8→10 (`unknown⇒DEGRADED`, `down dominates unknown`); `npm run test:orchestrator` + `npm run verify` green. Findings ledger: `dev/ORCHESTRATOR_LIVE_RUN_FB_LEDGER.md`.
- **Orchestrator gate-contract N+2 follow-ups — a dropped validator no longer reduces the lens set silently, a non-READY remediation discloses its commits, and RA-10 surfaces a spec-sanctioned orphan** (DEC-DEV-0101 / 0102 / 0106; N+2 re-validation FB-LR-15/16/20/21/22). Three live-run observability/robustness edges in the P6 `validate-feature-impl` gate, closed in one increment. **(1) FB-LR-15 → 0101:** a validator (RA-8/9/10) that dies on a **terminal API error** previously yielded `null` and was silently dropped by `.filter(Boolean)` — the verdict synthesised on a reduced lens set with no disclosure (a dropped RA-10 could let a real integration defect through under a clean GO). Now a dropped slot is **bounded re-spawned** (`maxValidatorRespawn`, default 2); a still-empty slot is recorded as `validators_incomplete` which **degrades a would-be GO to MANUAL_VERIFY** (never NO-GO — the lens is UNKNOWN, not failed) and is surfaced in a finding + the return. **(2) FB-LR-16 → 0102:** a remediation under a **non-READY** gate (substrate down / upstream degraded) used to commit a fix with no disclosure that the full suite was not a green safety net. Policy **(a) keep-but-disclose**: a substrate-independent confirmed fix (verify-finding-before-act already proved it real vs ground truth) stays committable, but the commit is **marked** (` [readiness=…: re-verify on a READY re-run]`) and the run **discloses** it (`committed_under_non_ready` + a finding) — silent mutation under ENV_NOT_READY is closed without discarding real verified work. **(3) FB-LR-21 → 0106:** the RA-10 (integration-boundary) prompt now requires a **deferred / spec-sanctioned orphan** to surface as a finding (`kind:orphan-export`, low) instead of being silently cleared `clean:true`. Plus two documentation riders (0106): the `remediation-guard.cjs` header records its **scope boundary** (it is a transient/infra/capability classifier; cross-spec/design detection is best-effort and rides the agent's reading by contract — FB-LR-22), and the work-order codifies the **"shape=conform / wiring=escalate" seam-split** as an accepted pattern, not a defect (FB-LR-20). **Additive** — `validators_incomplete` + `committed_under_non_ready` are new optional return fields (absent == prior behavior), a prompt clause, and docs → no artifact-type/validation-rule change → counts unchanged (24/44). Tests: P6 `validate-feature-impl-wiring` 24→26; `npm run verify` green. Live re-validation of all five lands in one batch with T5-transient / V-2 / FB-LR-19. Plan: `dev/ORCHESTRATOR_N2_GATE_FOLLOWUPS_LIVE_PLAN.md`; ledger: `dev/ORCHESTRATOR_LIVE_RUN_FB_LEDGER.md`.
- **`product-module/SPEC §3` command catalog reconciled to the 20 commands that actually ship** (DEC-DEV-0105; the deferred Tier-0 follow-up). The "command catalog of record" listed a phantom `/product:clarify` (no command file) and omitted the real `/product:complete` + `/product:lesson`, and its count said `15`/`19` against 20 files on disk. Removed `clarify`, added `complete` (Epic B completeness-loop) + `lesson` under §3.2 (now 6), corrected the counts (`15→20`, `19→20`). (`/product:clarify` was also the dead command the `handoff-spec §11` note pointed at — fixed in the Tier-0 pass.)
- **Doc-trust pass — corrected user-facing drift the doc-review surfaced** (front-door "trust-killers" a new reader hits first). **README:** removed the stale hardcoded DEC-DEV count (`62 на 2026-06-06` — the journal is far past it; the count is now delegated, not duplicated) and corrected the Integrator scope (`6 read-only` → `9 команд`). **Orchestrator status reconciled** — it understated reality across four docs (README `P3+P5 … P2/P4/P6/P7 pending`, `docs/README` `(planned, post-pilot)`, `docs/MAP` `планируется`, `orchestrator-module/SPEC` banner `v1.0-draft … ещё не implementation-ready`) — now they say the truth: **P3–P6 are built and live-validated** (DEC-DEV-0073…0104), with P2/P7 + the §6-channel still open, and status delegated to ROADMAP. **`validation.md §4`** ASCII namespace counts fixed (`V-* 14→16`, `V-H-* 10→11`, matching the canonical §0 header; `check-counts` green). **`processes.md §15`** changed from a pre-build `[ ] Реализовать /product:init …` TODO list (for already-shipped commands) to a "what is shipped" status. **`handoff-spec §11`** note that claimed "NFR not yet introduced in v1" corrected (NFR shipped). **`commands/product/lesson.md`** dead `/product:note` reference removed (no such command — redirected to NOTE-capture + `/product:promote-note`). **Deferred follow-ups** (own pass): the `product-module/SPEC §3` per-command roster (lists phantom `clarify`/`feedback-intake`, omits `complete`/`lesson`, count `15/19` vs 20 on disk) needs a careful catalog reconciliation, and the Phase-7 `/integrator:debug|verify` next-action suggestions want a consistent "(when available)" caveat.
- **Orchestrator P5 (`feature-to-tdd-impl`) now surfaces the nested P6 gate's `conflicts[]` and `findings[]` in its run envelope** (DEC-DEV-0104; FB-LR-19, Run C glossary live-run). P5 delegates the feature-level gate to P6 (`validate-feature-impl`, T3) but read only `result`/`readiness`/`findings` off the P6 result — never `p6.conflicts` — and dropped `findings` at its own return boundary. So a run where P6 **escalated** cross-spec/design conflicts came back with `result.conflicts: []` and no `findings`, hiding real escalations from anything reading the structured result (they survived only in `pending-actions.md` + ephemeral logs — the FB-LR-02 class). Fix: P5 captures `p6.conflicts`, and the return now **merges** impl-time ⊕ gate conflicts and **includes** `findings`. **Additive** — `findings` is a new optional return field and `conflicts` is widened (an absent/empty value == prior behavior), no artifact-type/validation-rule change → counts unchanged (24/44). Tests: `feature-to-tdd-impl-wiring` 8→9 (capture + merge + findings surfaced); `npm run verify` green. Findings ledger: `dev/ORCHESTRATOR_LIVE_RUN_FB_LEDGER.md`.
- **Epic A profile-persona + critic agents now register as spawnable `subagent_type`s — an unquoted `description:` (a YAML plain scalar containing `": "`) silently blocked discovery** (DEC-DEV-0103; Block-2 / Track-V pilot live-run, FB-LR-17). The three completeness-loop personas (`architect-advisor`, `qa-advisor`, `ux-advisor`) **and** the existing `product-devils-advocate` carried a long unquoted `description:` whose value contained a colon-space (`"…BEFORE handoff: can…"`, `"…DEC-DEV-0012): self-…"`) — **invalid YAML**, so Claude Code's frontmatter parser rejected the file and every spawn returned «Agent type not found …»; the whole `agents/{product,design}/` layer was unspawnable in the pilot. (The integrator agents, whose descriptions have no colon-space, registered fine — confirming nested-subdir discovery works; CC scans `.claude/agents/` **recursively**, identity is the `name:` field only.) This is the «registration root-cause … separate live-harness fix» the A1 entry anticipated. **Fix:** double-quote the `description:` value in all four agents (no internal quotes → safe). **Bug-class sweep:** a `js-yaml` pass over all **109** agent/command/skill frontmatters caught **3 more** files with the same colon-space defect — `commands/product/handoff.md`, `commands/product/validate.md`, `skills/orchestrator/gate-risk-classifier.md` (the last also had embedded `"…"` → escaped) — fixed in the same pass; the corpus is now **0 invalid**. Verified: `js-yaml` now parses all (was `bad indentation of a mapping entry` at the colon-space column); `npm run verify` green. After pilot re-delivery (`/ecosystem:update`) the V-2 personas + the completeness-loop's SURFACE step resolve. Findings ledger: `dev/ORCHESTRATOR_LIVE_RUN_FB_LEDGER.md`.
- **`/product:complete` invoked the completeness-oracle by a repo-relative path that does not exist in an installed project** (DEC-DEV-0103; Block-2 / Track-V live-run, FB-LR-18). `commands/product/complete.md` + `skills/product/completeness-loop.md` hard-coded `node hooks/product/lib/completeness-oracle.cjs`, but in a bootstrapped project the lib lives at `.claude/hooks/product/lib/…` — so the first SCORE call threw `MODULE_NOT_FOUND` (the loop self-corrected to the `.claude/` path, but a shipped command must not depend on the agent guessing). **Fix:** both files now use the installed `.claude/hooks/…` path (matching every sibling command, e.g. `commands/design/map.md`); the skill's Related pointers for the oracle / router / personas are `.claude/`-prefixed too. `npm run verify` green.
- **completeness-oracle (Epic B) — B4 now recognizes real-world VC↔SC link shapes (a list-valued field, and the `scenario`/`scenarios` name variance), so mature features stop showing a false coverage gap** (DEC-DEV-0099; dogfood of DEC-DEV-0098 against the `my-first-test` pilot). The B1 oracle was built spec-first from the artifact catalog, where VC→SC is `scenario: SC-NNN` (a scalar). Run against the pilot's real `.product/`, every one of the 7 `F.10-complete` features scored a false `0.8` with B4 failing ("active SC have no active VC") — because real VCs link via **`scenario: [SC-001, SC-001a, ...]`** (one VC covering an SC family, a list) and some (billing) VCs use the field name **`scenarios:` (plural)**, neither of which the scalar `===` check matched. Fix: B4 accepts the link field as a list (`includes`) **or** a scalar (`===`), and reads `scenario` **or** `scenarios`. Post-fix all 7 pilot features correctly score `1.0 / met:true`. The zone-router (A2) + personas (A1) ran clean against the same real data — no fix needed. Tests: `completeness-oracle` 7 → 9 (new list-form + plural-field cases pinning the real pilot shape); `npm run verify` + `check-counts` green; counts unchanged (24/44 — coverage logic, no new artifact-type/rule). **Pilot-side data finding (not fixed here, owner's project state):** the pilot itself is inconsistent — VC-001..023 use `scenario:`, VC-024+ use `scenarios:` — a `/product:validate` / LESSON candidate noted in the unified pilot plan.
- **Orchestrator P4 — repeated audits no longer append near-duplicate pending-actions (idempotency), and the misleading `fabricated-trace` finding kind is renamed** (DEC-DEV-0089 / FB-LR-10 + FB-LR-12; cheap N+2 riders). **PA-dedup (FB-LR-10):** three successive P4 runs over the same un-reconciled feature appended three near-duplicate pending-actions (PA-013/014/015) — the gate blindly appended a new PA instead of recognizing "already-routed → update the existing one"; a human caught the noise the process didn't. Repeated-run idempotency is a first-class property of any gate invoked more than once over the same target. Fix: both PA-emitting prompts in `audit-spec-fidelity.mjs` (product-route + spec-completion/coverage-route) now **scan the open pending-actions first and UPDATE in place** when one already routes the same `(feature, route, ids/paths)`, appending only on no match — matching on the stable signature, not the run-to-run drift wording. (PA writes happen inside agent prompts per the no-FS-in-script constraint, so the dedup is a prompt pre-filter, not a new lib.) **Rename (FB-LR-12):** the deterministic trace-integrity finding kind `fabricated-trace` was a misnomer — it also fires on *real* cross-feature owned contracts (e.g. BR-074 / IC-028) whose routing + detail are correct and only the `.product` **source** is missing, so the label libeled valid ids as "fabricated". Renamed to **`missing-trace-source`** (emit ↔ exempt-guard ↔ wiring-test assertion changed together; `fidelity-oracle.cjs` is unaffected — the kind label is minted by the relay prompt). Tests: `audit-fidelity-wiring` 10→11 (new dedup assertion + renamed exempt assertion); `npm run verify` + `check-counts` green; counts unchanged (24/44 — prompt edits + a kind-constant rename, no new artifact-type/rule). Findings ledger: `dev/ORCHESTRATOR_LIVE_RUN_FB_LEDGER.md`.
- **Orchestrator P6 validator findings are constrained to a DEFECT enum — a positive "coverage confirmed" assertion can no longer masquerade as an unresolved finding and block a GO** (DEC-DEV-0094; FB-028 / FB-LR-06). A P6 validator `finding.kind` was a **free string**, so RA-8 (requirements-coverage) could emit a *positive* assertion ("requirement X **is** covered") as a finding; `verifyFinding` then confirmed the positive ("yes, coverage is real" → `confirmed:true`), it survived to `residual` as an unresolved defect, and forced `MANUAL_VERIFY_REQUIRED` over a clean `GO`. (The live session mis-diagnosed this as a phantom `kind:coverage_confirmation` constant; the real cause is the free-string kind — the pilot's "exclude that kind" fix was a no-op.) **Fix, two layers:** (1) `VALIDATOR_SCHEMA.kind` is now a **defect enum** (`uncovered-requirement` / `missing-test` / `no-call-site` / `design-divergence` / `orphan-export` / `dead-seam` / `unhandled-event` / `dangling-import` / `other-defect`) — a positive-confirmation kind is *unrepresentable*; a satisfied requirement is reported via `clean:true`, never as a finding. (2) `verifyFinding` gains a **polarity gate**: a "finding" that asserts something IS present/covered/correct (not a defect) is `disposition:refuted` before any disposition check — the verifier no longer confirms a positive. Validator role prompts (esp. RA-8) reinforce "findings are gaps only". Tests: P6 wiring 18→19; `npm run verify` green; counts unchanged (enum tightening, no new artifact-type/rule). Findings ledger: `dev/ORCHESTRATOR_LIVE_RUN_FB_LEDGER.md`.
- **Orchestrator verify-finding-before-act is now ORDER-AWARE — a confirmer reading an already-fixed tree no longer mislabels a REAL finding a "hallucination"** (DEC-DEV-0093; P4 + P6, FB-LR-03/13). Both gates' verify step grepped only the **current** tree, so it was TOCTOU-sensitive: a finding remediated between detection and confirmation (a prior sequential fix, or a racing committer) read as "not present" and was dropped as a hallucination, even though it was genuinely real pre-fix — and a racing commit could thereby **mask an unresolved defect** so it never reached `residual`/`concerns` (in live-run B this hid a real cross-spec defect). Fix: each gate captures a **pre-gate baseline git sha** (an agent runs `git rev-parse HEAD` before any remediation — no FS in the Workflow script, DEC-DEV-0073 §D.1) and `verifyFinding`/`verifyDrift` now classify against BOTH the worktree and that baseline into a 3-way **`disposition`**: `present` (in the worktree → real & unresolved → remediate), `already-resolved` (gone from the worktree but in the baseline → was real, fixed since the gate started → surfaced in P6's new `already_resolved` return field + `findings`, **not** re-fixed and **not** called a hallucination; flagged for genuine-fix-vs-mask verification), `refuted` (absent in both → a true hallucination → dropped). Remediation/spec-fix act only on `present`; the post-fix recheck keys on `disposition`. The baseline is anchored to a fixed sha → robust to tree churn; full single-writer serialization of concurrent committers remains the **T5** follow-up. Tests: P6 wiring 17→18, P4 wiring 8→9 (baseline capture + 3-way disposition); `npm run verify` green. Findings ledger: `dev/ORCHESTRATOR_LIVE_RUN_FB_LEDGER.md`.
- **Orchestrator P5→P6 delegation now invokes P6 by `{scriptPath}`, not by name (the by-name call silently fell back to advisory on every run)** (DEC-DEV-0091; P4/P6 live-run finding). `feature-to-tdd-impl.mjs` delegated its feature-level gate via `workflow('validate-feature-impl', …)` — **by name** — but orchestrator processes are `.mjs` invoked by `scriptPath`, not registered named-workflows, so the call threw `no workflow with that name` on **every** P5 run and fell through to the inline `kiro-validate-impl` advisory lift: the real P6 (mechanical + RA-8/9/10 + verify-finding-before-act) never ran inside P5. A dev comment had mis-attributed this to a one-level nesting wall; the live-run audit **disproved that empirically** (official docs + 2 nesting probes + the harness error text — one-level nesting from a tool-launched P5 IS permitted, and P6 itself calls no nested `workflow()`). **Fix:** delegate by `workflow({ scriptPath: '.claude/orchestrator/processes/validate-feature-impl.mjs' }, …)`; the `try/catch` fallback is kept (a genuinely-unresolvable scriptPath still degrades to the advisory lift) and the degradation is now **surfaced in the returned findings** so a degraded gate is never read as a clean GO. The wiring test pins the scriptPath form. `npm run verify` green; re-confirm on a pilot C re-run. Findings ledger: `dev/ORCHESTRATOR_LIVE_RUN_FB_LEDGER.md`.

- **`/ecosystem:update` no longer deletes its own command file mid-run (self-deletion abort)** (DEC-DEV-0088; first class-B live-run finding). Step 5.1's namespace-aware sync re-created each managed namespace with `Remove-Item -Recurse` + copy. For `commands/`, the `ecosystem` namespace contains `update.md` — **the command running right now** — so the harness blocked its removal (`Remove-Item on system path '/ecosystem:update' is blocked`) and `ErrorActionPreference=Stop` aborted the whole update partway through (`commands/ecosystem` onward never synced). Unix `rm -rf` of an already-read file masked the bug; it surfaces under the harness sandbox (Windows/PowerShell here). **Fix:** Step 5.1 now mirrors namespace **contents** (`robocopy /MIR` on Windows; `rsync -a --delete` / prune-and-copy fallback on bash) instead of removing the namespace dir — the running `update.md` is overwritten in place, never deleted; stale files are still purged; non-managed (third-party / project-state) namespaces stay untouched. `robocopy`/`rsync` are native exes whose internal file ops fall outside the `Remove-Item`/`rm` guard. (Flat subdirs in Step 5.2 keep the delete-then-copy path — no running primitive lives there.) **Delivery note:** a project still on the buggy `update.md` must break the chicken-and-egg with a one-time manual mirror of `commands/ecosystem` from `.claude-ecosystem-tmp` before the next `/ecosystem:update` runs clean.

- **Orchestrator P5 (`feature-to-tdd-impl`) propagates implementer CONCERNS instead of dropping them; GO summaries must disclose mock/stub test substrate** (DEC-DEV-0081; S6 dogfood root-cause). The S6 §6-channel re-test surfaced that the P5 FSM declared a `concerns` field in its implementer schema but **never read it** — so an implementer's correct upward signal ("real provider/adapter satisfied by a Mock because real access is *deferred*; unwired skeleton") was silently discarded, and the feature closed at GO with the deferred capability hidden. Fix in `orchestrator/processes/feature-to-tdd-impl.mjs`: (a) READ the implementer's `concerns`; (b) a **non-blocking `surfaceConcern` routing branch** (keyed on a concern, not only `status=BLOCKED`) that records a deferred-capability tracking item in `pending-actions.md` (route + provisioning-tier; tracking/disclosure, **not** a blocking request); (c) concerns threaded into the feature-level GO-gate; (d) `concerns[]` returned in the process result. `commands/orchestrator/run.md` "After the run" gains a mandatory **test-substrate disclosure rule** — a GO over an unwired real seam is an over-claim; surface every concern + its route. The §6 grading rubric (`dev/ORCHESTRATOR_S6_BRIEF.md`) now gates **§6-E (QUALITY) as N/A** when no capability request was produced, instead of counting a phantom FAIL. Test-locked: `tests/orchestrator/concerns-propagation.test.cjs` (6 static invariants; `npm run verify` green). **Deferred to an S7 re-test** (needs an unequipped/blocking provider substrate): the detection-leg fixes (env-probe enumerating spec-mocked provider secrets + a non-blocking `EXPECTED-ABSENT-BUT-DEFERRED` disposition; recasting the deferred-capability rule as tracking).

- **`/ecosystem:update` now re-stamps `ecosystem_version`; stale catalog counts corrected** (DEC-DEV-0083; D7 process-hardening). `/ecosystem:update` previously synced `.claude/CHANGELOG.md` but never touched `product.yaml`, so `ecosystem_version` stayed **frozen at its bootstrap value** after every update (stale-by-default; `/ecosystem:verify`'s version-drift check could not catch it). New **Step 5c** surgically re-stamps **only that one line** from the first released CHANGELOG version — every other `product.yaml` field stays verbatim. Plus two count-drift fixes that a new deterministic reconciler (`check-counts.js`) caught: `commands/ecosystem/verify.md` artifact count (Step 3 "23 type files" → 24, "24 files" → 25, + summary) and `skills/product/validation-runner.md` rule count ("25 rules" → 44). (The enforcement layer that backs this — a blocking commit-msg gate, the reconciler, and the auto-loaded CLAUDE.md "Process triggers" contract — is ecosystem-dev-only under `dev/` and is **not** shipped to product projects.)

- **`/ecosystem:verify` — stale per-namespace command counts + Orchestrator blind spot** (DEC-DEV-0082). Step 4's hardcoded `ecosystem: 2` and `integrator: 6` expectations had drifted (actual: 5 and 9), and the `orchestrator/` namespace (shipped in 1.6.0) was absent from both the checks and the summary `COMMANDS` template — so on a healthy install `/ecosystem:verify` would raise a **false drift-warning** and **silently omit Orchestrator** from the health report. Corrected the counts (`ecosystem 2→5`, `integrator 6→9`), added the `orchestrator/` namespace to Step 4 and the summary block, and added a baseline-snapshot caveat so a count mismatch prompts investigation rather than a false alarm. Root cause: the patch-cut count-drift sweep covers only canonical *catalog* counts, not the per-command expectations baked into `verify`/`status` prompts. **Deferred** (recorded in DEC-DEV-0082): the `status.md` App-Map (`AM`) dashboard row and a `patch-cut.md` Step 4 sub-item to sweep per-command counts + feature-awareness across verify/status/templates.

---

## Релизы 1.0.0 – 1.6.0 (2026-04-18 .. 2026-06-18)

Дословно в архиве: [`dev/_archive/changelog/CHANGELOG_1.0-1.6.md`](dev/_archive/changelog/CHANGELOG_1.0-1.6.md) (ротация 2026-07-11, DEC-DEV-0185; правило — checklists/patch-cut.md: на cut версии при файле >~150 КБ релизы старше текущего квартала уезжают в архив).

## Future versions (planned)

- **v1.1** — Orchestrator Module concept + `/product:patterns` pattern dictionary expansion based on real usage data.
- **v1.2** — `P3 Feedback Integration` activation when D5 tooling is available via Integrator.
- **v2.0** — Multi-product workspace support; `P5 Actuality Refresh` automation when usage data shows real refresh patterns.

---

## Reference: Design history (NOT in this repo)

Full design history (10 iterations from audit through 4 modules) is preserved in author's design archive. This repo contains only the operational ecosystem.

