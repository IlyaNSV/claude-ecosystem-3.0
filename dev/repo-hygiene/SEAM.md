# SEAM — трек «Repo Hygiene» (гигиена репо как контекста агента) (DEC-DEV-0227)

status: ACTIVE
seam_written: 2026-07-29 (шаблон v3 — `dev/deferred/CONTEXT_SEAM_PROTOCOL.md` §4; трек заведён и исполнен сессией 2026-07-28/29)
track_ssot: этот файл (шов трека) · процедура — [`skills/repo-hygiene.md`](../meta-improvement/skills/repo-hygiene.md) · измеритель — [`scripts/doc-health.cjs`](../meta-improvement/scripts/doc-health.cjs) · решение и уроки — `DEV_JOURNAL.md` DEC-DEV-0227 · правила ротации — `CONVENTIONS.md §5.1` · снятые механизмы — `CONVENTIONS.md §13`

## 🛑 СТОП-БЛОК — интенции и инварианты (ре-инжектится после компактации)

> **Precedence внутри шва объявлена текстом:** при противоречии этого СТОП-БЛОКА и секции
> «Состояние» — **прав СТОП-БЛОК**. Знание, спрятанное в порядке секций, не читается.

### Интенции владельца — ДОСЛОВНО (цитировать, не пересказывать)

> «Сделай в отдельном worktree компактацию + чистку + оптимизацию этого репозитория, только
> действовать важно с умом. Сначала выяви возможные проблемы и несостыковки в текущей структуре
> и содержимом этого репо… Репо большой, текста много… **я боюсь, что периодически он разрастается
> на тысячи строк, что плохо для harness, т.к. для него весь репо — контекст, а если он
> разрозненный и неактуальный (одна только папка dev чего стоит), то сбои и неоднозначности
> в результате — неминуемы.**» (2026-07-28, старт трека)

> «Не бойся задавать мне множество вопросов о ценности того или иного содержимого, пиши также свои
> предположения и вердикты. **Главное не сломать работающие и полезные документы.**»

> «Я хочу привести проект к **единому источнику общего чекпоинта всех веток и ворктри**, чтобы все
> последующие доработки опирались на полный результат моих треков.» (2026-07-29)

> «Главное, что результат сегодняшней чистки должен быть в main + **остальные отсталые артефакты
> должны быть также будто "почищенными"**.» (2026-07-29 — уточнение: слияние без приведения
> принесённого к новым правилам НЕ считается выполнением)

### Инварианты И ЗАПРЕТЫ

1. **Опасность документа = возраст × механизированная доставка.** Шов со `status: ACTIVE`
   впрыскивается `seam-reinject-compact.js` после КАЖДОЙ компактации. Закрытый трек с таким
   статусом не лежит — он приказывает переделать сделанное. Чистку начинать с вопроса «что
   читается автоматически», а не «что старое».
2. **Проблема репо — правдивость, а не объём.** Замерено: always-on ≈ 19-30k токенов = 2.7-4.1%
   окна. Найдено 38 устаревших утверждений при НУЛЕ битых ссылок. Здоровье графа ничего не говорит
   о правдивости узлов.
3. **Ложное «покрыто» опаснее честного «не покрыто»** — оно снимает бдительность там, где защиты
   нет (прецеденты: обещанный `gen:map:check` для `docs/MAP.md`; карта «всех процессов», знавшая 4 из 9).
4. **Не удалять знание — чинить статус.** Счёт первого прогона: 12 × FIX-STATUS, ~30 × ARCHIVE,
   **0 × DELETE** среди содержательных документов.
5. **Перед удалением копии — поднять её уникальное в канон.** В двух волнах так спасено ~12 и 11
   правил соответственно.
6. **Механику — детерминированным скриптам, суждение — моделям.** Из 8 субагентов первой волны
   **5 погибли** на механических задачах (watchdog/API); те же задачи скриптами — секунды и
   идемпотентно.
7. 🛑 **`[[wikilinks]]` в этом репо — в основном НЕ ссылки:** 91 из 103 — ключи persistent-памяти
   вне репозитория. Чекер, не отличающий их, даст 91 ложное срабатывание.
8. 🛑 **`docs/guide/vendor/` НЕ трогать** — это живые офлайн-зависимости интерактивных карт
   (cytoscape, elk), покрытые смоуком `mapshell.smoke.mjs`. Однажды было ошибочно предложено
   вырезать «как балласт»; решение отменено проверкой.
9. **`merge` в `main` — всегда владелец** (`CLAUDE.md` §Autoflow). В сессии 2026-07-29 был выдан
   **разовый явный мандат** на сведение чекпоинта; он **исчерпан** и на будущие сессии НЕ
   распространяется. Дефолт — довести до готового PR и остановиться.
10. **Отказ исполнителя при неверной посылке — обязателен, а не опционален.** Верифицированные
    отказы этого трека: `battery/panes/*.txt` (несущая улика `VERDICT.md`), `EPIC_E_READINESS.md`
    (10+ ссылок из рантайма), `vendor/`, Design §16 (4 входящие ссылки).

### Режим работы

- **Оркестратор — main-модель.** Разведка/сбор фактов — субагенты (MDP: sonnet — сбор без анализа,
  opus — анализ и правки с суждением). Ревью результата дешёвого тира обязательно.
- 🛑 **Механический массовый обход НЕ делегировать модели** — только скрипт с `--dry-run` по
  умолчанию и инвариантом-сверкой, который отказывается писать при расхождении.
- Правки — волнами по **непересекающимся** файлам, границы объявлять пофайлово.

<!-- SEAM-REINJECT-END -->

## Состояние (2026-07-29)

**Единый чекпоинт достигнут.** `main` = `e11fc8f`, несмёрженной работы не осталось.

| Проверка | Команда | Ожидаемый вывод |
|---|---|---|
| HEAD main | `git log --oneline -1 origin/main` | `e11fc8f Merge pull request #239…` |
| Несмёрженное | `git for-each-ref --format='%(refname:short)' refs/heads/` + `git rev-list --count origin/main..<b>` | `0` для всех, кроме 2 архивных веток |
| Здоровье | `node dev/meta-improvement/scripts/doc-health.cjs` | 1 находка (12 неразрешимых ссылок в `_archive/`), exit 0 |
| Полная цепь | `npm run verify` | EXIT=0 (в worktree нужен `NODE_PATH`, см. «Грабли») |

**Сведено в чекпоинт (5 PR, в порядке мёржа):** #237 гигиена → #232 import-mode →
#231 Tech-Uplift → #204 Informed Fetch → #238 ledger Global Loop (104 коммита) → #239 гигиена чекпоинта.

**Достигнутые числа** (дословно, не пересказ): `DEV_JOURNAL` 738 → **208 КБ** (104 → 30 живых
записей) · `CHANGELOG` 270 → **102 КБ** · `ROADMAP` 73 → **39 КБ** (строка на 36 694 символа
разрезана) · `CLAUDE.md` 335 → **255 строк** · битые ссылки в `_archive/` 158 → **12** ·
карта процессов 21 → **26** (lane orchestrator **9/9** = число `.mjs` на диске).

**Построено:** `doc-health.cjs` (warn-only, в цепи `verify`; 5 осей: пороги ротации · ACTIVE-швы
без коммитов · ссылки в `_archive/` · always-on объём · слипшиеся строки) + скилл `repo-hygiene.md`.

**Ветки/worktree после уборки:** удалено 43 локальные смёрженные; остались `main`,
`docs/global-loop-assist-ledger` (живая — передаётся исполнителю), `docs/research-parallel-sessions`
и `fix/dev-env-delivery` (архивные, **запушены** для сохранности). Worktree: главный (на `main`),
`ce3-wt-global-loop` (живой), `ce3-wt-hygiene` (этот трек); `ce3-wt-uja` и `ce3-wt-informed-fetch`
удалены как отработавшие.

## Критерий достаточности (SEAM-ACK)

**Обязательные чтения** (файл → зачем):

| Файл | Зачем |
|---|---|
| этот шов целиком | интенции + инварианты + остаток |
| `dev/meta-improvement/skills/repo-hygiene.md` | процедура: 5 линз, протокол верификации, анти-паттерны |
| `DEV_JOURNAL.md` → запись **DEC-DEV-0227** | решение, отвергнутые альтернативы, 6 уроков |
| `dev/meta-improvement/CONVENTIONS.md` §5.1 и §13 | правила ротации (чинились дважды) + реестр снятых механизмов |

**Контрольные вопросы — ответить владельцу ДО первой правки:**

1. Почему приоритет чистки задаётся не возрастом документа, и что именно делает старый файл
   опасным? Назови конкретный механизм-доставщик.
2. Репо имел 0 битых ссылок и 38 лгущих утверждений одновременно. Что это говорит о том, что
   меряет `check-links`, и почему потребовался отдельный измеритель?
3. Какие задачи этого трека ЗАПРЕЩЕНО делегировать модели и почему? Приведи число из эмпирики.
4. Что нужно сделать перед удалением дублирующей копии правила — и что случится, если пропустить
   этот шаг?
5. Кто выполняет `merge` в `main` по умолчанию и в каком статусе находится мандат, выданный
   2026-07-29?

**Декларация доступов (v3):** продолжатель перечисляет в отчёте **каждый** инструментальный доступ
сверх файлов, названных этим швом, — включая grep-сниппеты, принёсшие цитаты из неоткрытых файлов.

## Следующий шаг

**Первым:** ничего не начинать — трек в состоянии «исполнен, остаток событийный». Если владелец
даёт «го» на очередь ниже, начинать с п.1 (он снимает шум для всех последующих сессий).

**Очередь остатка:**

1. **Уборка 46 смёрженных remote-веток.** Не выполнена сознательно: массовое действие наружу без
   явного разрешения. Готовая команда:
   ```bash
   for b in $(git for-each-ref --format='%(refname:short)' refs/remotes/origin/ | grep -v HEAD | grep -v 'origin/main$'); do
     [ "$(git rev-list --count origin/main..$b)" = "0" ] && echo "${b#origin/}"
   done | xargs -r -n1 git push origin --delete
   ```
2. **Флип `doc-health` warn → strict** (`check:dochealth:strict` уже есть в `package.json`) — после
   того как числа обкатаются и подтвердится отсутствие ложных срабатываний. Сейчас warn
   сознательно: новый гейт без обкатки клинит чужой цикл.
3. **Браузерные смоуки карты процессов** (`smoke:procmap`, `smoke:mapshell`) — пропущены в worktree
   (`puppeteer-core` не установлен). Карта изменена существенно (21 → 26 процессов), проверена
   только статически + selftest генератора. Прогнать там, где puppeteer есть.
4. **`ROADMAP.md` «Где мы сейчас»** — проверить, отражает ли чекпоинт `e11fc8f` (5 треков сведены).
5. **memory-sync** — записи памяти отражают состояние до сведения чекпоинта.

## Отброшено (что пробовали, почему не сработало)

- **Вырезать `docs/guide/vendor/` (2 МБ) из поставки** — владелец согласовал по моей неверной подаче
  («балласт»); проверка показала живые офлайн-зависимости карт → решение **возвращено с исправленной
  посылкой и не исполнено**.
- **Адоптировать community-плагин `docs-hygiene`** (6 скиллов) — generic-markdown, не знает ни
  SSOT-решётки, ни швов, ни механизированной доставки. Заимствованы только линзы
  (`audit-derivability`, `extract-ssot`).
- **Резать SessionStart-хуки как «раздутый push-канал»** — гипотеза опровергнута замером (4.1 КБ на
  все хуки, генерируются из живых файлов).
- **Архивировать `EPIC_E_READINESS.md`** — оказался дизайн-SSOT с 10+ ссылками из живого рантайма.
- **Удалить `dev/context-audit/battery/panes/*.txt`** — `VERDICT.md` ссылается на них как на
  несущую улику (JSONL терял финальное сообщение при kill).
- **Вырезать Design SPEC §16 целиком** — 4 входящие ссылки; сжат вместо удаления.

## Грабли среды (этой сессии)

- 🔴 **`git checkout -B main origin/main` в worktree передвигает ветку под ДРУГИМ worktree.** Так
  `ce3-wt-uja` получил 108 «изменений» — обратную дельту всей чистки (возврат `HOME.md`, удаление
  `browse.md`). Закоммить это — откатить трек. Лечится `git reset --hard origin/main` в
  пострадавшем worktree. **Проверяй `git worktree list` перед манипуляциями с ветками.**
- **`NODE_PATH` обязателен в свежем worktree:** `NODE_PATH=<корень>/node_modules`, иначе чекеры
  дают ЛОЖНЫЙ результат (js-yaml fallback).
- **Watchdog субагентов:** пофайловый обход тысяч файлов = смерть агента через 600s. В брифе
  запрещать явно, требовать крупных shell-команд.
- **`check-counts` не различает scope:** «10 скиллов в `skills/design/`» читается как заявление о
  ГЛОБАЛЬНОМ числе (65) → ложное красное. Формулировать «10 файлов в `skills/design/`».
- **`process-gate` требует `DEV_JOURNAL` в ТОМ ЖЕ коммите** при упоминании `DEC-DEV-N`, и
  `CHANGELOG` при `feat:`/`fix:` в consumer-zone. Срабатывал дважды — оба раза по делу.
- **Накопительные каноны конфликтуют в каждом мёрже** — это контракт накопления: «выбрать сторону»
  всегда неверно, нужно сохранить обе в правильном порядке (в журнале — по возрастанию ID, вплоть
  до вставки записи МЕЖДУ соседями; в CHANGELOG — свежие сверху).
- **CRLF:** git предупреждает «LF will be replaced by CRLF» на каждом `add` — ожидаемо на Windows,
  не ошибка.

## Вопросы владельцу

1. **Уборка 46 remote-веток** — выполнять (команда готова, п.1 очереди)?
2. **`doc-health` warn → strict** — когда флипаем? Предлагаю после одного-двух спокойных прогонов
   без ложных срабатываний.
3. **Судьба двух архивных веток** (`docs/research-parallel-sessions`, `fix/dev-env-delivery`) —
   теперь они на remote; удалять локальные копии или оставить?
4. **`dev/context-audit/SEAM.md` не имеет поля `status:`** — его не видят ни хуки реинжекта, ни
   `doc-health`. Проставить статус (какой?) или это сознательно?
