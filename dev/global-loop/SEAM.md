# SEAM — трек «Global Loop» (Кондуктор × Release DoD) (DEC-DEV-0216)

status: SUPERSEDED
seam_written: 2026-07-15 (сессия заведения+ратификации треков; шаблон v3 — CONTEXT_SEAM_PROTOCOL §4)
superseded: 2026-07-19 — последняя правка этой копии (`b813744`); живая версия ушла на ветку, см. баннер
track_ssot: `dev/global-loop/PLAN.md` (единый план) · при противоречии секций этого шва прав 🛑 СТОП-БЛОК (precedence текстом, канон v3)

> ## 🛑 ЭТА КОПИЯ НА `main` — НЕКАНОНИЧНА. НЕ ВХОД В РАБОТУ.
>
> **Живой шов трека — на ветке `docs/global-loop-assist-ledger`** (worktree
> `WebstormProjects/ce3-wt-global-loop`). Там же живут `ASSIST_LOG.md`,
> `ECOSYSTEM_FEEDBACK_RUN-B.md`, `NEXT-HORIZON.md` — **на `main` этих файлов нет вообще**.
> Решение владельца: содержимое ledger-ветки сюда **не переносить**; эта копия помечается
> неканоничной, читать надо оттуда.
>
> **Текст ниже отстал от 2026-07-19** и описывает Волну 0 *до* прогонов. Фактически:
> RUN-2026-07-17-A завершён 2026-07-20, **RUN-B закрыт владельцем 2026-07-23**
> (`366fe60` на ledger-ветке, «RUN-B завершён владельцем»; вход следующей единицы =
> `ECOSYSTEM_FEEDBACK_RUN-B.md`). Ассист-ledger заведён и исчерпан двумя полными прогонами —
> **начинать «старт ассист-режима» по этому файлу нельзя**.
>
> **Почему `status: SUPERSEDED`, а не `ACTIVE`:** `seam-reinject-compact.js` и
> `seam-freshness-warn.js` матчат значение ровно `active` (регистр не важен, первые 30 строк) —
> при `SUPERSEDED` устаревший СТОП-блок больше не впрыскивается после компактации. Онбординг-пак
> `host-onboard.cjs` грузит этот файл с диска **безусловно**, поэтому баннер обязан стоять первым.

## 🛑 СТОП-БЛОК — интенция и инварианты (ре-инжектится после компактации)

### Интенция владельца — ДОСЛОВНО (2026-07-15; цитировать, не пересказывать)

Трек 1 (Release DoD):
> «Релизный DoD — полный чеклист (может быть прямым или ссылочным) на подтверждение того,
> что релиз выполнен именно как и ожидалось, все требования соблюдены, протестированы,
> stage контур готов к миграции на prod, т.е. пока этот чек-лист не выполнен — экосистема
> итеративно работает над продуктовым проектом по нашим канонам.» + «Первый пункт по сути
> про loop механизм с критерием остановки, когда релиз выкачен и готов к использованию.»

Трек 2 (Host Console): полная хартия — `dev/host-console/TRACK.md` §0 (обязательное чтение
ниже); ядро: «…тонкая прослойка, которая ловит мои интенции и перекладывает на исполнение
процессов в продуктовом проекте на VM… Это своего рода global loop управляющий claude
сессиями на продукте в VM.»

Слияние и консолидация:
> «Проект кондуктора по сути сводится к текущему global loop, поэтому давай сведем всё это
> к единому проекту и соберем цельную картинку без размазывания по разным проектам.»
> «Давай сольем эти два трека в единый план и сделаем ШОВ для следующей сессии.»

Знаниевая плоскость (та же сессия, позже):
> «Кондуктор должен знать как работает экосистема еще лучше, чем я, надо подумать как ему
> дать все необходимые для корректной работы описания и документацию + сделать возможность
> хождения и добора информации об экосистеме.»

Дефиниция и стратегия входа (та же сессия, ещё позже):
> «Зафиксируй, что кондуктор - это мультилпикатор сессий и слой более автономного
> управления ими. Сначала я попробую из этого проекта (терминала) управлять тестовым
> проектом с ассистансом, накопим опыт и пойдем в кондуктора уже после.»

### Инварианты И ЗАПРЕТЫ (решены — НЕ пересматривать без новых данных)

1. **Merge в `main` ЛЮБОГО репо — только владелец.** Мандаты на merge в «Состоянии» ниже —
   сессионные, на новую сессию НЕ переносятся (при противоречии прав этот пункт).
2. **Floor autonomy непробиваем** (`prod_deploy, destructive, spend_money,
   provision_real_secret` → всегда человек); **`evt:owner.*` эмитит только владелец**
   (ANOM-OD7-2) — пульт никогда не проецирует решения владельца.
3. **Субстрат READ-ONLY** для пульта/диспетчера (уроки 54-56); **ghost-защита ввода** в
   чужие TUI-сессии: `C-u` → файл/base64 → capture-верификация → Enter (8+ ghost-случаев
   за кампанию, были опасные директивы).
4. **Анти-дублирование SSOT:** архитектура/инварианты/roadmap пульта — ТОЛЬКО
   `factory-conductor/CONDUCTOR.md`; решения — журнал DEC-DEV-0216; хартии — TRACK.md
   треков; порядок работ — PLAN.md. Вторых копий не заводить.
5. **DoD — ссылочный агрегатор** существующих вердиктов (P6/P7/Epic E/graduation/
   impl-sync/DA-RL), НЕ новый судья и НЕ новый артефакт-тип; терминатор цикла = зелёный
   DoD + owner ratify.
6. **H2 gated на events.jsonl** — Q&A-автопилот по pane-peek запрещён (канон «hook-relay,
   не pane-peek»). **R3 gated на реальный prod-хост** — не начинать.
7. **Пре-регистрация перед live-прогонами** ($/sweep и stop-критерии для H0 — до прогона,
   не после); не жечь лимиты (negative exemplar: сессия 100+ агентов).
8. **Знание экосистемы — резолвером, не пересказом** (K0, `CONDUCTOR.md` §Knowledge plane):
   лестница добора INFORMATION-MAP → генерируемые каталоги → SSOT → живое состояние;
   **version-skew** (про проект — из его `.claude/`, про канон — из host-клона);
   **ответ пульта без указателя на SSOT-источник = не ответ**. Зеркала/копии доков не заводить.
9. **Вход в стройку Кондуктора (H-фазы) — только после ассист-периода Волны 0 и явного
   «го» владельца.** До того пульт = владелец в терминале + ассистент; опыт пишется в
   `ASSIST_LOG.md` — не начинать H0 «потому что план его называет».

### Режим работы

Тонкий оркестратор: main — брифы, ревью, синтез, решения; recon — sonnet; строительство
кода — opus; context firewall (объёмные чтения — субагентам). Управление трека — в
ecosystem; код пульта — коммиты в локальный клон `WebstormProjects/factory-conductor`
(PLAN.md §1). Verify в свежем worktree — только с
`NODE_PATH=<корень>/node_modules` (иначе ложный STALE каталога команд).

<!-- SEAM-REINJECT-END -->

## Состояние (верифицируемо, не по памяти)

| Что | Проверка | Ожидаемое |
|---|---|---|
| Ecosystem main | `git log origin/main --oneline -3` | merge-коммит PR этого шва поверх `afb129d` (закрытие кампании #214) |
| Этот трек | `ls dev/global-loop/` | `PLAN.md`, `SEAM.md` |
| Треки-источники | `dev/release-dod/TRACK.md` и `dev/host-console/TRACK.md`, строка `status:` | оба `RATIFIED … DEC-DEV-0216`, + указатель на план |
| Решения | хвост `DEV_JOURNAL.md` | запись `DEC-DEV-0216` (9 решений + консолидация) |
| Дом пульта | `git -C ../factory-conductor log --oneline -2` (от корня ecosystem: `C:/Users/pw201/WebstormProjects/factory-conductor`) | `7c4667f` merge PR #1 + `7084eef` (CONDUCTOR.md создан) |
| Verify | `NODE_PATH=<корень>/node_modules npm run verify` | EXIT 0 |
| Чужое (НЕ трогать) | кампания до-PROD закрыта соседней сессией (#211/#213/#214), её шов `dev/plans/SEAM.md` — не наш | — |

Сделано в треке (одна сессия 2026-07-15): kickoff-доки двух треков (PR #210) → сводка 9
развилок владельцу → ратификация + консолидация (PR #212 ecosystem + PR #1
factory-conductor: CONDUCTOR.md) → слияние в единый план + этот шов. Далее:
**R0 исполнен** 2026-07-17 (автономный прогон кондуктора RUN-2026-07-17-A —
DEC-DEV-0221, PR #229; ручная проверка DoD на живом RL-001 пилота — хвост прогона
RUN-A, результат смотри в `ASSIST_LOG.md`/журнале, шов тогда не обновлялся).
**2026-07-19 — онбординг-пак хост-сессии** (DEC-DEV-0222): в начале КАЖДОЙ выбранной
сессии-кондуктора грузить прогрев L0-L2 лестницы K0 из живых файлов (~38k ток.).
Активация: `CONDUCTOR_SESSION=1 claude` (авто-хук) · `/host:onboard` · вручную
`node dev/meta-improvement/scripts/host-onboard.cjs` (PLAN §1-bis — детали);
+ правило coverage-check в CLAUDE.md (проверка AS IS-покрытия интенций владельца).

## Критерий достаточности для продолжателя (SEAM-ACK)

**Обязательные чтения (файл → зачем):**
1. Этот файл целиком — рамка, инварианты, следующий шаг.
2. `dev/global-loop/PLAN.md` — единый план: волны, зависимости, где живёт работа.
3. `dev/release-dod/TRACK.md` — хартия DoD + решения D1-D4 + скелет 6 категорий.
4. `dev/host-console/TRACK.md` — хартия пульта + интерфейс экосистемы (каналы).
5. `factory-conductor/CONDUCTOR.md` — архитектура/мандат/инварианты/roadmap пульта (SSOT).
6. Хвост `DEV_JOURNAL.md` — запись DEC-DEV-0216 (rationale, отвергнутые альтернативы).

**Контрольные вопросы (вернуть ответы владельцу ДО первой правки — это и есть ACK):**
1. Почему DoD — ссылочный агрегатор, а не новый артефакт-тип/судья, и какие вердикты он
   агрегирует?
2. Где живёт архитектура пульта, где — порядок работ, где — хартии; почему копировать
   между ними запрещено?
3. Какие два жёстких закона ограничивают Q&A-автопилот пульта и что такое ghost-защита?
4. Почему Brain пульта — событийные пробуждения, а не вечная сессия?
5. Что входит в Волну 0, каков гейт входа в H-фазы, и какой паттерн-гейт прогоняется
   перед стартом R0?

**v3-обязательство:** продолжатель декларирует в артефакте прогона КАЖДЫЙ инструментальный
доступ сверх названных выше файлов (включая grep-сниппеты из неоткрытых файлов).

## Следующий шаг — ИСТОРИЧЕСКИЙ КОНТЕКСТ (приказ снят, работа исполнена)

⚠️ Ниже — то, что этот шов предписывал 2026-07-15. **Всё исполнено; как императив НЕ читать.**
Действующая очередь — только в живом шве на ветке `docs/global-loop-assist-ledger`.

1. SEAM-ACK перед первой правкой — постоянное правило протокола швов (не разовый шаг);
   продолжателю трека возвращать ACK **по живому шву ledger-ветки**, не по этой копии.
2. **Волна 0, единица 1 — R0:** ✅ исполнен 2026-07-17 (DEC-DEV-0221, PR #229) — термин
   «Release DoD» + soft body-секция в каноне RL. Хвост «ручная проверка DoD на живом RL-001»
   закрыт прогоном RUN-A.
3. **Волна 0, единица 2 — ассист-режим:** ✅ прожит целиком. `ASSIST_LOG.md` заведён и ведётся
   **на ledger-ветке** (на `main` его нет); прогон RUN-2026-07-17-A завершён 2026-07-20,
   прогон RUN-B закрыт владельцем 2026-07-23. Заводить ledger и «стартовать ассист-режим»
   заново — **не нужно**; накопленная обратная связь консолидирована в
   `dev/global-loop/ECOSYSTEM_FEEDBACK_RUN-B.md` (там же, на ветке) — это и есть вход
   следующей единицы.
4. **H0 — по-прежнему ТОЛЬКО после явного «го» владельца** (инвариант 9). Единственный
   пункт списка, который не устарел: гейт не снят, два прогона Волны 0 его не открывают.

## Отброшено (невосстановимая часть)

- **3-repo split** (runtime/контракты/канон по трём репо) — отвергнут владельцем:
  «размазывание»; принята консолидация в factory-conductor.
- **Вечная пульт-сессия** — отвергнута: ресурсная бомба idle-TUI×statusline (vm-factory-ops
  §4.5), деградация контекста, лимиты.
- **Новый артефакт-тип DoD (24→25)** — отвергнут: цена count-sweep/каталога без выгоды.
- **Спека prod-миграции сейчас** — отвергнута: spec-first риск, built ≠ validated.
- **Широкий мандат Q&A с fork-decision** — отвергнут: тихие решения за владельца до
  накопления ledger-данных.

## Грабли среды (вскрылись в этой сессии)

- **Verify в свежем worktree без `node_modules` → ложный STALE каталога команд**
  (`gen-command-catalog` молча падает на regex-fallback без js-yaml). Лечение:
  `NODE_PATH=<корень>/node_modules npm run verify`. Память: `env-worktree-verify-needs-node-path`.
- `git worktree remove` на Windows иногда «Device or resource busy» — retry `rm -rf` +
  `git worktree prune` через минуту.
- Харнесс памяти нормализует frontmatter записей (обрезает `description` на `#N`,
  оборачивает в кавычки) — Edit по памяти сверяй с фактическим содержимым, не с тем, что писал.
- Номера DEC-DEV раздаёт соседняя сессия тоже — только `npm run next-dec-dev -- --claim`
  (0214/0215 были заняты параллельно).
- main двигается параллельными сессиями несколько раз за день — `git fetch` +
  worktree от `origin/main` перед КАЖДОЙ единицей.
- Упоминание `DEC-DEV-N` в сообщении коммита требует правку DEV_JOURNAL в ТОМ ЖЕ коммите
  (process-gate) — в чисто структурных коммитах номер не упоминать.
- gh-токен на VM невалиден (унаследовано из кампании) — H0-sweep по ssh не требует gh,
  но проверить до H1.

## Вопросы владельцу (историческое — актуальные см. в живом шве ledger-ветки)

1. ~~**Тестовый проект для ассист-режима Волны 0**~~ — **снят**: Волна 0 отработана двумя
   прогонами (RUN-A, RUN-B); выбранный проект и его состояние — в `ASSIST_LOG.md` на ветке.
2. **Состав первого sweep H0** — по-прежнему открыт, но актуален только после «го» на H-фазы.
3. gh-токен VM — открыт (на Волну 0/H0 не влиял).
