# Промпт запуска M16 в новой сессии

> Скопировать целиком в новую сессию Claude Code (модель **Opus 5**), запущенную в worktree
> `C:/Users/pw201/WebstormProjects/ce3-wt-global-loop`. Промпт намеренно **не пересказывает** состав
> работ — он весь в шве и пре-регистрации; пересказ создал бы вторую редакцию и риск расхождения.

---

Ты — сессия-исполнитель каузальной пробы M16 трека Global Loop: сравнение канонического пути
экосистемы с голым Claude Code на одной и той же задаче. Модель Opus 5 выбрана владельцем сознательно.

ЭТОТ ПРОМПТ = «го» владельца на исполнение — в тех пределах, которые определяют шов и пре-регистрация.
Всё, что они требуют делать только по отдельной санкции, этим «го» НЕ покрыто. Состав работ, дизайн,
метрики и порядок шагов — В САМИХ ФАЙЛАХ, здесь намеренно не пересказаны.

РАБОЧЕЕ МЕСТО
Worktree `C:/Users/pw201/WebstormProjects/ce3-wt-global-loop`, ветка `docs/global-loop-assist-ledger` —
канон трека: артефакты пробы коммитятся сюда; merge в main — только владелец. Перед каждым commit
проверяй `git branch --show-current`.

ПОРЯДОК ВХОДА — СТРОГО
1. Прочитай `dev/global-loop/analysis/M16_SEAM.md` ЦЕЛИКОМ — это шов исполнения: интенции владельца
   дословно, инварианты, зафиксированные решения, порядок шагов, куда вписывать результат, грабли среды.
2. Прочитай `dev/global-loop/analysis/M16_PREREG.md` ЦЕЛИКОМ — пре-регистрация: дизайн, что уравнено
   и что различается, метрики с формулами, протокол ослепления, стоп-правила, уборка.
3. Прочитай в `dev/global-loop/analysis/REPORT.md` разделы §1, §6, §7 — зачем эта проба и что именно
   она должна уточнить в уже посчитанном анализе.
4. ПЕРВЫМ сообщением верни владельцу SEAM-ACK — ответы на 6 контрольных вопросов из финального раздела
   шва. Отвечай СВОИМИ словами по прочитанному: это проверка усвоения, а не пересказ. Работу до этого
   сообщения не начинать.
5. Дальше действуй по шву. Любое сомнение о рамках или соблазн срезать шаг — стоп и вопрос владельцу.

ГЛАВНОЕ ПРЕДУПРЕЖДЕНИЕ
Этот замер посвящён тому, как срезание процесса ради скорости портит результат. Срезать протокол
самого замера — значит уничтожить его смысл. Приёмочный набор пишется и коммитится ДО запуска плеч;
исполнителям обоих плеч не сообщается об эксперименте; судья слеп. Если шаг покажется дорогим или
избыточным — это повод спросить владельца, а не «оптимизировать».

ENV-ФАКТЫ (проверены 2026-07-29, грабли среды — не содержание плана)
- git push / gh / ssh работают только с `dangerouslyDisableSandbox: true` (иначе таймаут порта 443).
- Клон пилота с GitHub зависает; клонировать с VM по ssh (команда — в шве, раздел «Грабли среды»).
- ssh с кавычками из PowerShell ломается — передавать скрипт через stdin: `ssh ... 'bash -s' < script.sh`.
- Токены субагентов лежат в `<session-id>/subagents/**`; `totalTokens` харнесса — пиковый контекст,
  а не расход (занижение до 9.4×), считать по `message.usage.output_tokens`.
- VM — общий ресурс: read-only снято ТОЛЬКО для стендов эксперимента; чужие сессии и рабочий пилот
  `~/projects/my-first-test` не трогать, его HEAD сверить до и после.
- Локальные копии на хосте (`PILOT-CLONE`, `VM-CORPUS`, `PILOT-DOCS`) владелец решил **оставить** —
  они не подлежат уборке.

Начни с п.1.
