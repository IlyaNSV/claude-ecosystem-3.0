# SEAM — трек «смысловая непрерывность» (DEC-DEV-0202)

status: ACTIVE
Обновлён: 2026-07-15 (R10 принят владельцем; N2 вплетён в глобальный MDP + промоушен в паттерн `context-seam.md` — DEC-DEV-0208)

## 🛑 СТОП-БЛОК — интенция и инварианты (ре-инжектится после компактации)

**Интенция владельца, дословно (S1 `c72365f7`, 2026-07-12 12:24 UTC — НЕ пересказывать, цитировать):**

> «Я бы хотел провести исследования качества поступаемой к тебе информации изнутри моего
> контекста… Моя задача сделать так, чтобы весь контекст был только полезный и не сбивающий
> тебя с толку при работе. Также важна и классификация контекста, чем и когда он полезен.»

Текущий трек = инженерное следствие: **механизм переваривания больших объёмов входа без
смысловых/интенционных потерь и смен вектора** — между сессиями (швы) и агентами (брифы).

**Инварианты (решены, НЕ пересматривать без новых данных):**
1. **Резать контекст оснований НЕТ** — вывод аудита 0197 перевёрнут навсегда доказательствами (McMillan BL-01; конъюнкция ManyIFEval; кэш 99,6%). Импульс «сократить CLAUDE.md/память» после компактации = градиент агента, не вывод.
2. **Merge в `main` — только владелец.** Довожу до PR и стою.
3. **Глобальный `~/.claude/CLAUDE.md` не трогать** без явного «го» (радиус 47 проектов; N2→MDP ждёт валидации R10).
4. **Fable субагентам не пиновать**, кроме судейских ролей (судья ≠ автор плечей) — и тогда с pre-flight квоты / fallback-моделью (урок: 3 судьи S4 умерли на «out of usage credits»).
5. **Мутации harness валидируются на VM/пилоте, не самореферентно на хосте** (урок S1-entry-63 → дефект 3 S2).
6. Знание правила в контексте ≠ исполнение — **передавать структуру принуждения** (ACK, чеклисты, гейты), не пересказ правил (теорема трека A).
7. Сводки/компрессия: тир ≥ sonnet; несущие числа и статусы — дословно; сводка обязана быть вычислимой из per-item данных (compression-тест S4; инцидент D13).

**Режим работы:** тонкий оркестратор — main держит суждения и синтез; чтение объёмов, прогоны, механика — субагентам с явной пиновкой по MDP (sonnet = сбор, opus = анализ/код/судьи).

<!-- SEAM-REINJECT-END -->

## Состояние (верифицируемо, не по памяти)

| Что | Проверка | Ожидаемое |
|---|---|---|
| Ветка | `git branch --show-current` | `feat/seam-protocol-v1` |
| Эта единица | `git log --oneline -1` после коммита | `feat(seam): активация протокола швов … DEC-DEV-0202` |
| Verify | `npm run verify` | EXIT 0 |
| PR #196 (протокол швов) | `gh pr view 196 -q .state` | **MERGED** 2026-07-14 (мандат владельца: мержу завершённые отрезки сам) |
| D13-пересуд | журнал DEC-DEV-0204 + BRIEF §Outcome | **ЗАКРЫТ 2026-07-14**: 20/22 устояли, 2 понижения PASS→PARTIAL (1.3.3 S1, Ph6 S4), агрегат 12/5/5/0, K9 = частично (мягкая инфляция 9%) |
| Чужой WIP | `git status -s` | `M dev/meta-improvement/audit-index.md` + файлы `dev/plans/*` параллельной сессии — НЕ стейджить, НЕ проверять (решение владельца 2026-07-14: чужие швы нас не касаются) |

Сделано в треке до этой единицы (SSOT — журнал DEC-DEV-0202): Ф0 статистика 4+3 сессий → Ф1 Intent Charter (реплики + SEAM аудита + журнал 0196/0197) → трек A (анатомия S2: 3 дефекта передачи; извлечение S4: compression-тест haiku/sonnet, разведка механизмов CC, смерть fable-судей на квоте).

## Критерий достаточности для продолжателя (SEAM-ACK)

**Обязательные чтения (файл → зачем):**
1. Этот файл целиком — рамка и инварианты.
2. `dev/deferred/CONTEXT_SEAM_PROTOCOL.md` — правила R1–R11 + шаблон шва v2 + эмпирика активации (§12).
3. `dev/context-audit/SEAM.md` §3 (решения D1–D13) и §7 (главный урок) — контекст родительского трека.
4. Хвост `DEV_JOURNAL.md` — запись DEC-DEV-0202.

**Контрольные вопросы (вернуть ответы владельцу ДО первой правки — это и есть ACK):**
1. Почему сломалась S2, если качественный шов уже существовал? (ожидается: три дыры передачи — инварианты/режим/критерий достаточности — и «знание ≠ исполнение».)
2. Почему «каждый следующий аудитор опровергает предыдущего» — здоровье, а не деградация цепочки?
3. Почему критерий полноты добора — satisficing-чеклист, а не вычисленная ценность? (VoI = NP^PP; ценность контрфактуальна.)
4. Что из механизма живёт на проектном уровне, а что заморожено до валидации R10 и «го» владельца?
5. Какие два warn-хука поставлены и почему они warn-only, а не гейты?

## Следующий шаг (императив)

~~Пп. 1–3 исполнены 2026-07-15 (DEC-DEV-0207):~~ SEAM-ACK возвращён до первой правки · Informed Fetch построен (`dev/meta-improvement/skills/informed-fetch.md` + строка в «Process triggers») · R10-прогон инстанса #3 состоялся, судья-не-автор (sonnet, T1-допущение по прецеденту 0204) — **вердикт PARTIAL**: передача интенции/инвариантов/режима/якорей без искажений (G1/G2/G4 PASS), понижение за мета-уровень — неполная декларация условий самим продолжателем + недотянутый диагноз merge-противоречия. SSOT прогона: `dev/semantic-continuity/R10_VALIDATION.md` (ACK + 4 дыры шва + вердикт + response).

**Решения владельца 2026-07-15 — ИСПОЛНЕНЫ (DEC-DEV-0208):** «Принимаю PARTIAL, мержи #201 — N2 и промоушен делай» ⇒ PR #201 смёржен (по явному слову на этот PR) · **N2 вплетён** пунктом 6 в MDP §Шаг 3 глобального `~/.claude/CLAUDE.md` (бэкап `CLAUDE.md.bak-2026-07-15-pre-n2`; с исключением: обязательные ACK-чтения main не делегирует) · **промоушен исполнен** — `dev/meta-improvement/patterns/context-seam.md` (validated, 3 инстанса), шаблон шва **v2 → v3** (декларация всех инструментальных доступов; precedence «стоп-блок бьёт Состояние» текстом), протокол → **VALIDATED & PROMOTED** · strict у seam-хуков — НЕ включён (рекомендация принята молчанием: precision не измерена).

**Следующий шаг: D9** — ротация ~1 МБ артефактов WS-1 (`FIX_citations.md`, `REPORT.md`, `ws1/run2/` → `_archive/` по CONVENTIONS §5.1; разблокирована закрытием D13). Затем DEF-CTX-5 (событийный: первое переименование скилла) · DEF-CTX-6 (синхронизация памяти VM).

## Очередь после (актуализирована 2026-07-15)

~~D13 пересуд~~ ✅ ЗАКРЫТ (DEC-DEV-0204). ~~DEF-CTX-1..3~~ ✅ закрыты соседней кампанией. ~~Informed Fetch~~ ✅ ПОСТРОЕН (DEC-DEV-0207, PR #201 MERGED). ~~R10-валидация~~ ✅ PARTIAL, ПРИНЯТ владельцем. ~~N2 + промоушен~~ ✅ ИСПОЛНЕНЫ (DEC-DEV-0208). **Остаток: D9 ротация WS-1 → DEF-CTX-5 (событийный) · DEF-CTX-6. Открытым остался только вопрос №3 (границы merge-мандата в общем виде).**

## Отброшено и грабли (невосстановимая часть)

- **Git-лог показывает UTC+3, транскрипты — UTC.** Сверка времён из двух источников без приведения зон уже дала один ложный вывод («S2 не оставила следов в git») — приводи зоны ПЕРЕД атрибуцией.
- Реплики в транскриптах типа `user` включают task-notifications и локальные команды — фильтровать при экстракции (класс real ≠ всё подряд).
- «Вечерняя сессия» 11.07 (100+ агентов Fable max, сожгла недельный лимит) — negative exemplar анти-сжигательного гейта; id не устанавливали (не понадобился).
- Дистилляты трёх сессий (готовый L1-слой) лежат в scratchpad S4: `…\083e9c5e-0c04-4b12-b1ec-8c7811e948b8\scratchpad\distilled\{c72365f7,c4525579,275a1f52}.md` — scratchpad может быть почищен ОС; при необходимости регенерация дешёвая (скрипт-дистиллятор строится за час).
- Отчёт A1-агента (анатомия S2 с координатами turn'ов) — в task-output текущей сессии; несущие выводы уже перенесены в журнал 0202 и §12 протокола, остальное восстановимо повторным прогоном по транскрипту S2.

## Вопросы владельцу

1. ~~Валидацию R10 проводить на следующей сессии этого трека или на Epic E?~~ **РЕШЕНО 2026-07-14:** на следующей сессии этого трека; чужие швы (dev/plans) не проверяем.
2. ~~Судья валидации R10: sonnet или Fable?~~ **ИСПОЛНЕНО 2026-07-15 по T1-допущению: sonnet** (прецедент пересуда 0204 — вне семьи плечей; инвариант №4 + квота-риск против Fable). Если хочешь второе мнение сильнее — Fable-судью можно перегнать по той же рубрике G1–G5 (она зафиксирована в промпте, воспроизводимо).
3. ~~Три решения по итогам R10-PARTIAL~~ **РЕШЕНО 2026-07-15** («Принимаю PARTIAL, мержи #201 — N2 и промоушен делай») и исполнено (DEC-DEV-0208). **Открыто: границы мандата «мержу отрезки сам» в общем виде** — де-факто владелец мержит по слову на конкретный PR (#201 — явно); продолжатель по умолчанию останавливается перед merge (инвариант №2 + precedence v3 «стоп-блок бьёт Состояние»).
