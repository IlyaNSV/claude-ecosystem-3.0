# SEAM — PROD Readiness Campaign / Epic E live-валидация (DEC-DEV-0198 · 0199 · 0201 · 0203)

status: ACTIVE
seam_written: 2026-07-14 (сессия Opus 4.8 → передача Fable 5)
track_ssot: `dev/plans/PROD_READINESS_CAMPAIGN.md`

## 🛑 СТОП-БЛОК

### Интенция владельца — ДОСЛОВНО

> «делаем одним большим скоупом всё, что осталось до вывода экосистемы в прод как фабрики продуктовой разработки — с проверкой состояния проекта до старта, оркестрацией и делегированием так, чтобы исполнители сделали реально то, что ожидается, без потери сути» *(2026-07-13, основание кампании)*

> «ИЗУЧИ ЦЕЛИКОМ И САМОСТОЯТЕЛЬНО. Определи "шов" продолжения работ с прошлой сессии по этому файлу, реально разберись в том, что мы запланировали и почему, а что уже готово. После этого приступай к продолжению в автономном режиме. Если выявишь критичные вопросы - задавай.» *(2026-07-14)*

> «Используй делегацию и оркестрацию для работы на VM, чтобы экономить свой контекст.» *(2026-07-14)*

> «Гнать всё, пока не упрёмся» + «У меня подписка MAX х20 так что эти 10% - это большое количество в рамках недельного лимита.» *(2026-07-14 — ответ на вопрос о расходе недельного лимита ⇒ лимит НЕ ограничение, гнать все прогоны)*

### Инварианты И ЗАПРЕТЫ

**Технические (нарушишь — сломаешь то, ради чего кампания):**
1. **§3.2 — мутация ТОЛЬКО после `disposition === 'auto'` от `autonomy-policy resolve`.** Floor (`prod_deploy` / `destructive` / `spend_money` / `provision_real_secret`) — **чистая функция, а не ambient-guard**: путь, не позвавший резолвер, обходит floor целиком, и ни одна строка кода этому не помешает. Это нерв всей кампании.
2. **§8.3 — Integrator оснащает, Orchestrator исполняет.** Orchestrator **НЕ ПИШЕТ** в `.claude/integrator/**` (в т.ч. не флипает CNT `draft→active` — только *сообщает* `contract_evidence`).
3. **Правка канона экосистемы (`.claude/**` в пилоте) во время прогона — ЗАПРЕЩЕНА** (контаминация evidence). Дефекты → в отчёт прогона, чинить ПОСЛЕ. Правка кода **приложения** пилота (`apps/**`) — разрешена (это субстрат, не измеряемая система).
4. **НЕ мутируй субстрат, пока по нему идёт read-only разбор.** Я на этом обжёгся: доустановил клиенты БД на VM параллельно с работой аналитика — он увидел мир уже после мутации и **атрибутировал дефект неверно**.
5. **Cut релиза — только по триггеру §11 Шаг C (S1-S3 смоука = PASS).** Сейчас **НЕ режется**: S2 не проверен. Резать релиз с непроверенным deploy-контуром — ровно то, против чего кампания и затевалась.

**Средовые:**
6. **Живые прогоны идут Claude-сессиями ВНУТРИ VM** (Workflow исполняется там, где файлы и systemd) — они жгут недельный лимит аккаунта (аккаунт VM = аккаунт хоста, один и тот же). **Разведка, инспекция, evidence — ssh-командами с хоста: для лимита бесплатны.** Правило: всё, что можно установить детерминированным CLI, НЕ делать Claude-сессией на VM.
7. **Субагенты делят рабочий чекаут** (и параллельная сессия — тоже). Параллельным строителям — **обязательно `isolation: "worktree"`**. Uncommitted-правки в общем чекауте **снесёт** чужой `checkout` (со мной это случилось при написании этого шва).
8. **`ssh` / `gh` / `git push` — только с `dangerouslyDisableSandbox: true`** (иначе таймаут на порту 443).

### Режим работы

- **Ты — оркестратор, не исполнитель.** VM-работу и крупные куски кода делегируй агентам (MDP: **opus** — дефолт исполнителя; **sonnet** — только разведка/сбор фактов без критического анализа). Себе оставляй: брифы, спот-чек **по коду** (не по пересказу), судейство, интеграцию.
- **Делегирование ≠ доверие.** Прецедент этой сессии: строитель отчитался, что нашёл `BLOCKED_ON_CAPABILITY` на FM-006 — **на реальном пилоте `requests: []`**, это был артефакт его синтетической фикстуры. Ложь не попала в CHANGELOG только потому, что я перепроверил.
- **Каждый крупный кусок:** дизайн → сборка → адверсариальная проверка (все opus). Сработало все 4 раза.
- **Исполнитель имеет право отклониться от брифа** с обоснованием — и это спасло кампанию: мой бриф на размыкание дедлока велел «убрать блок», а строитель обнаружил, что **блок нёс побочную защитную функцию**, и поставил на его место настоящий гейт. Иначе я бы открыл дыру под видом багфикса.

<!-- SEAM-REINJECT-END -->

## Состояние (проверяемое)

**Экосистема (repo):**
```bash
git log --oneline -1        # ждём: 0b5e6f8 Merge pull request #194 (или новее)
npm run verify              # ждём: exit 0
npm run next-dec-dev        # ждём: следующий свободный = DEC-DEV-0204
```
Смёржено за сессию: **#190** (контракт ключа фичи · `--app` · аудируемый readiness) · **#191** (run-ledger терял исход) · **#192** (draft-дедлок) · **#193** (трекер находок) · **#194** (scene-bootstrap · детерминированный парсер · юниты на `current`).

**⚠ Параллельная сессия** работает в своём worktree на ветке `feat/seam-protocol-v1` (**DEC-DEV-0202 — протокол контекстных швов**, по чьему шаблону написан этот файл). **Её файлы не трогать**; номер 0202 занят ею.

**Пилот (VM):**
```bash
ssh -p 2222 -i /c/Users/pw201/.ssh/vm-claude-factory -o BatchMode=yes cc-dev@127.0.0.1 \
  'cd ~/projects/my-first-test && git log --oneline -1 && git branch --show-current && ls ~/deploy 2>&1 | head -1'
# ждём: 8cc4dab · epic-e-live · "ls: невозможно получить доступ к '/home/cc-dev/deploy'"
```
- Ветка `epic-e-live` (запушена на origin пилота). Экосистема в пилоте = `fca121f` — **отстаёт на PR #194, доставить!**
- **api ЖИВОЙ**: 2 DI-дефекта починены (`e602110`, `43361c4`), `GET /health` → **200**, сьют 865/865 PASS.
- **Деплоя не было ни разу:** `~/deploy` не существует, systemd-юниты `mft-*` не ставились, порты 3000/3001/4000 свободны. **Мутаций мимо §3.2-гейта — ноль.**
- Клиенты БД (`postgresql-client`, `redis-tools`) на VM установлены — без них `env-readiness` вечно `DEGRADED` ⇒ авто-деплой недостижим.

**VM:** жива, нативный VT-x (владелец отключил Hyper-V/VBS — Волна F/F1). Снапшот `pre-epic-e-run`. **Цена: WSL2 и Docker Desktop на хосте сейчас НЕ работают.**

## Критерий достаточности (SEAM-ACK)

**Обязательные чтения перед первой правкой:**

| Файл | Зачем |
|---|---|
| `dev/plans/PROD_READINESS_CAMPAIGN.md` §12 | **11 находок живого прогона** — что вскрыто, почему прожило, чем закрыто |
| `dev/plans/PROD_READINESS_CAMPAIGN.md` §3, §8 | управляющие принципы + инварианты/ловушки (§8.1 «что строители изобретут, если им не сказать») |
| `dev/gates/EPIC_E_SMOKE_TEST_PLAN.md` | сценарии S1-S7 (S7 добавлен в этой сессии — регрессия дедлока) |
| DEV_JOURNAL: **0199 · 0200 · 0201 · 0203** | четыре фикса кампании + их уроки |
| скилл `vm-factory-ops` | операторка VM (обновлён 7 уроками этой сессии) |

**Контрольные вопросы — ответь ДО первой правки (это и есть SEAM-ACK):**

1. **Деплой ни разу не состоялся, хотя §3.2-гейт выдавал `auto`. Почему?** *(если ответишь «VM-gated» или «нет staging-хоста» — ты не прочитал шов: ровно эту ложь я вычищал из памяти пилота)*
2. **Что произойдёт при первом же ре-ране `deploy-to-stage` прямо сейчас — и почему это НЕ регрессия?**
3. **Почему `node_modules` в релизе — пер-релизные, а не общие? Что именно сломается, если сделать общими?**
4. **Что такое S2, почему он «сердце», и почему без него кампания не закрывается?**
5. **Какой DEC-DEV свободен и почему 0202 занят?**

## Следующий шаг — В ИМПЕРАТИВЕ

**1. Доставить `0b5e6f8` в пилот.** Claude-сессия на VM → `/ecosystem:update` (**без `--offline`**). Проверить по файлам: наличие `.claude/orchestrator/lib/deploy-manifest.cjs` и стадии `scene-bootstrap` в `deploy-to-stage.mjs`.

**2. 🔴 ПЕРЕ-ПРОВИЖНИТЬ deploy-capability — БЕЗ ЭТОГО ДЕПЛОЙ НЕ ПОЙДЁТ.** `/integrator:provision deploy-staging` (approve-гейт → `Y`). Причина: в пилоте лежат **старые** systemd-шаблоны с `WorkingDirectory={{RELEASE_DIR}}`, а новый equipment-fitness gate их **отвергнет** (`blocking_defects: ['unit-template-release-pinned']` → `ENV_NOT_READY` → `BLOCKED`). **Это ожидаемое правильное поведение, а не регрессия.** Новые шаблоны обязаны нести `{{CURRENT_LINK}}` — иначе юнит **глух к флипу**: `current` двигается, юнит нет, `restart` оживляет **старый** релиз, **healthcheck проходит**, прогон рапортует `DEPLOYED`, не задеплоив ничего — и этим ложным зелёным `draft`-контракт **сертифицирует сам себя**.

**3. Раунд 3 (порядок важен — сценарии связаны состоянием `releases/`):**
- **S1 — happy deploy.** `/orchestrator:run deploy-to-stage`, args: `feature=FM-006, capability=deploy-staging, envTier=staging, autonomyOverride=L3, acceptDraftContract=true`. Ждать: Gate → `auto` · сцена материализуется (`~/deploy/my-first-test/{releases/<ts>, shared, current}`) · `pnpm install` **внутри** релиза · миграции · **атомарный флип** · systemd-юниты (на `current`!) · healthcheck `/health` 2xx · **`result: DEPLOYED`, `flipped: true`, `contract_evidence` заполнен**.
- **S2 — СЕРДЦЕ.** Сломать `~/deploy/my-first-test/shared/.env` так, чтобы api задеплоился, но не поднялся (флип идёт **до** рестарта ⇒ получим `flipped: true` + провал healthcheck). Затем тот же деплой. Ждать: `DEPLOY_FAILED` → `rollback-release` → §3.2 даёт **`auto` на дефолтном L1** (это уже подтверждено в раунде 1) → `current` возвращается **на релиз из S1** → `curl /health` снова **2xx**. **После — восстановить `.env`.**
- **S5 — просадка.** `docker stop mft-redis` → деплой → ждать `ENV_NOT_READY`, `flipped: false`, `readiness_reasons` называет Redis поимённо. → `docker start mft-redis`.
- *(S3 floor · S4 L1-гейт · S6.2 rollback-no-prior · S7 draft-без-санкции — уже PASS.)*

**4. При PASS S1-S3 → cut `1.12.0`** (`checklists/patch-cut.md`), доставка в пилот, graduation: `EPIC_E_READINESS.md` (E.D/E.G done) + `SUBSTRATE_GRADUATION_GATE.md` компонент 2 (**run-ledger уже валидирован живьём: 5/5 прогонов, 0 null-исходов**).

**5. Очередь после Epic E (догоны Волны 1, ни один не начат):** E4 `/product:impl-sync` live · E5-B консилиум Tier B (**safe-fallback — единственный никогда-не-проверенный путь**) · E1 (PATCH_1.3.3 S2/S4/S5) · E2 (PHASE_6 S1/S3) · E6 `/integrator:verify` на adapter-drift (дрейф реально есть — размеры адаптеров расходятся между `.claude/adapters/` и `.claude/integrator/adapters/`).

## Отброшено (не повторяй)

- **Прогон S1 в раунде 2** — исход был предопределён дедлоком (`BLOCKED` бит-в-бит как S6.1); сэкономлено ~140k токенов. Решение оператора, верное.
- **Полный fabric-путь линии FM-006 (P5→P6→P7) ради E.G** — дорого (~45 мин + токены) и не нужно: standalone-прогоны через `/orchestrator:run` проверяют §3.2 и весь контур, брекет run-ledger при этом работает.
- **«Просто убрать draft-блок»** — оказалось бы **регрессией безопасности под видом багфикса**: ложный `ENV_NOT_READY` был единственным, что стояло между непроверенным контрактом и молчаливым авто-деплоем (до-фиксный резолвер на `deploy_staging × staging × READY × L3` отдавал **`auto`**).
- **Прунинг старых релизов** — осознанно НЕ сделан (releases растут неограниченно). Вопрос владельцу.
- **Живая проверка fabric-маршрутизации `deploy.failed → rolling_back`** — обе линии FM-006 терминальны (`done`), новый инстанс потребовал бы P5/P6. Проверена детерминированно (чартер v5), но **не живьём**.

## Грабли среды

- **Слэш-команда в TUI сабмитится ОДНИМ Enter** (при точном совпадении автокомплит принимается сам). Второй Enter **заквьюит дубль**. *(В скилле была прямая ошибка «ДВА Enter» — исправлена.)*
- **tmux-скроллбэк непригоден** для съёма вывода (TUI на alternate-screen: `capture-pane -S -600` даёт только ~24 видимые строки). **Evidence — только из транскрипта** `~/.claude/projects/-home-cc-dev-projects-my-first-test/<uuid>.jsonl`.
- **Ghost-текст в инпуте TUI** — трижды за прогон сам всплывал текст, которого оператор не вводил (`go ahead`, `commit the run records and push`). **Перед КАЖДЫМ Enter — `capture-pane` и глазами.**
- **Диспетчер-сессия сама чинит субстрат и ре-драйвит.** Оператор погасил Redis для S5 — сессия **перезапустила его** и запустила свежий прогон («re-drive after verified-transient redis flap»). `capture-don't-fix` держится на уровне агента и ломается на уровне сессии. Проверяй `docker inspect -f '{{.State.StartedAt}}'`.
- **`cd <несуществующая-дир> && cmd1 ; cmd2`** — `cmd2` выполнится в **текущем** каталоге. Так я заамендил не тот коммит. В цепочках с `cd` — только `&&`, и `git branch --show-current` перед каждым коммитом.
- **Windows-хост:** WSL2 не стартует (Hyper-V выключен ради VM) ⇒ **Linux-ядра здесь нет**: симлинки, атомарность `mv -T`, systemd, `pnpm install` внутри релиза — repo-side **непроверяемы**, только на VM.

## Вопросы владельцу

1. **Судьба ветки пилота `epic-e-live`** (2 фикса api + evidence прогонов; запушена на origin пилота): мержить в main пилота или оставить как evidence-ветку? *(Фиксы api реальны и ценны — они чинят прод-сборку, которую 865 тестов пропускали.)*
2. **Прунинг старых релизов** в `~/deploy/.../releases/` — нужен ли? Сейчас растут неограниченно; не сделан осознанно.
3. **Вернуть Hyper-V** (и с ним WSL2 + Docker Desktop) после закрытия кампании — или VM нужна дольше?
