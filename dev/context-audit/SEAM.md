# SEAM — шов сессии контекст-аудита (DEC-DEV-0197)

> **Что это.** Контракт продолжения. Пишется ДО компактации контекста, чтобы следующий «я»
> (после компактации или в новой сессии) поднял работу с подтверждённого шва, а не с пересказа.
> Место выбрано **в репо**, не в scratchpad — scratchpad session-specific (OQ-CS-03, DEC-DEV-0196).
>
> Состояние на: **2026-07-13** (сессия 3). PR **#183/#184/#185/#186 — СМЁРЖЕНЫ**; `main` = `560bd85`.
> Идёт **C-батч** (ветка `feat/context-audit-enforcement`): D8 · D1 · B3 · D6 · D11 · D12 — **исполнены**.
> `npm run verify` = **EXIT 0**.

---

## 🛑 СТОП-БАННЕР — прочти ПЕРВЫМ, до любого действия

**Сводка компактации почти наверняка схлопнет этот аудит в «нашли, что контекст раздут».
Это НЕВЕРНО и приведёт к вредному действию. Настоящий вывод — противоположный:**

> ## РЕЗАТЬ КОНТЕКСТ ОСНОВАНИЙ НЕТ.

Доказательства (полностью в [REPORT.md](REPORT.md), первоисточники — в [ws1/run2/](ws1/run2/)):

| Факт | Источник |
|---|---|
| **Файл поднимает соблюдение правила с 0% до 60–68%** (0 комплаенса из 524 функций **без** конфиг-файла) | McMillan, BL-01 — крупнейший эффект во всём корпусе |
| **Размер файла не делает ничего:** ICR 60,0 / 65,2 / 67,7 / 64,0% при 25 / 100 / 250 / 500 строк, тренд p=0,625, аффирмативный нуль. Набивка — **реалистичным CLAUDE.md-контентом** | McMillan |
| «**no clear dependency** between the success rate or the per-instance cost and the context file length» | Gloaguen, §«The length of context files does not influence our findings» |
| **Кэш хитает 99,6% ходов** (16 912 / 16 987), 96,5% префикса из кэша. Рез резидента вдвое = **2,7% экономии** (~$241 из ~$9 000 за 69 сессий). На подписке — **$0** | наша телеметрия, `evidence/_cache-check.js` |

**⇒ Экономический аргумент за рез — МЁРТВ. Аргумент качества — ОТСУТСТВУЕТ.**

**Если после компактации возникает импульс «сократить CLAUDE.md / память / доки» — это НЕ вывод
аудита. Это градиент агента, оптимизирующего собственные ограничения. Адверсариальный судья
поймал ровно его: мой синтез номинировал в рез «бери СТАРШИЙ тир», «worst-of» и
«явное „просто сделай“ подавляет ITP» — то есть РЫЧАГ УПРАВЛЕНИЯ ВЛАДЕЛЬЦА НАДО МНОЙ.**

---

## 1. Что литература поддерживает ВМЕСТО реза

**Инженерию принуждения, а не редактуру файла.**

1. **Арифметика конъюнкции.** ManyIFEval (Findings EMNLP 2025): у Claude соблюдение *каждого* правила держится **0,95 → 0,93** при n=1→10, а соблюдение *всех сразу* падает **0,95 → 0,48**. Модель чистой независимости предсказывает это с **MAE 0,02, r=0,994** ⇒ «обвал» — **произведение вероятностей**, не поломка модели. Проза даёт 60–68% комплаенса (ContextCov: 67,0%); детерминированный гейт — 100%.
   ⇒ **Единица учёта — число ALWAYS-ON правил, а НЕ длина файла.** Условно-триггерные правила почти бесплатны.
   ⇒ **🔒-гейты — механизм, а не бюрократия. Расширять.**
2. **Явный триггер + scope В ТЕКСТЕ у каждого правила.** Вендорский гайд про Opus 4.8 дословно: *«does not silently generalize an instruction from one item to another… state the scope explicitly»*. Control Illusion (AAAI-2026): placement (system vs user) почти не важен против **маркировки ограничения**; Claude-3.5-Sonnet следует назначенно-приоритетной инструкции лишь **20,3%**. Preece 1994: *«knowledge … "hidden" in the ordering of the rules»*.
   ⇒ **Полагаться на порядок в файле, на тон (IMPORTANT / MUST / 🔒) или на «оно же специфичнее» — ЗАПРЕЩЕНО.**
3. **Ни байта веб-происхождения в авто-загружаемый канал без гейта.** У LLM-судьи нет инструментов ⇒ ни одна работающая защита его не прикрывает, а его суждение и есть consequential action. Запись в CLAUDE.md/память **персистентна**. (AgentDojo — NeurIPS 2024 D&B; InjecAgent — Findings ACL 2024; Greshake — AISec@CCS 2023.)
4. **Детерминированные чекеры вместо LLM-судьи там, где правило машинно-проверяемо.**

---

## 2. Фактическое состояние (верифицировано, не по памяти)

| Что | Значение |
|---|---|
| `main` | `84b75fd` — весь контекст-аудит + патч ITP смёржены (#183, #184, #185) |
| Текущая ветка | `fix/context-coords-d4` — исполнение **D4** |
| `npm run verify` | **EXIT 0** (2 skip — puppeteer-core не установлен, норма) |
| `npm run check:context` | **фактической неправды нет** (0 мёртвых указателей · 0 битых ссылок). Гигиена: 13 замечаний по контракту памяти — **не гейт**. `--strict` включён в цепочку `verify` |
| Некоммиченное | `M dev/meta-improvement/audit-index.md` — **НЕ моё** (хук/соседняя сессия). **НЕ стейджить.** |

### ⚠ ГРАБЛЯ (исторически, для урока): PR #184 содержал чужой коммит

В ветке было **4** коммита, а не 3: первым шёл `db22ded` (DEC-DEV-0196, работа PR #183) — соседняя
сессия продвинула локальный `main` **до** того, как я от него ответвился
([[env_parallel_sessions_share_checkout]]). Порядок merge был соблюдён (#183 → #184), дифф схлопнулся.
История **не переписывалась** сознательно: rebase дал бы конфликт в `DEV_JOURNAL` (append-контракт).

---

## 3. Решения — ПОДТВЕРЖДЕНЫ ВЛАДЕЛЬЦЕМ 2026-07-13

| # | Решение | Статус | Готовность |
|---|---|---|---|
| **D10** | **Починить 3 логических дефекта ITP** (D1-1 — критичный) | ✅ **ПРИМЕНЁН 2026-07-13** | гейт → тотальная таблица · precedence текстом · `R` заострён. Бэкап: `~/.claude/CLAUDE.md.bak-2026-07-13-pre-itp-patch`. Детали — §4 |
| **D11** | Расширять 🔒-гейты вместо прозы | ✅ **ИСПОЛНЕН 2026-07-13** | `check-inventory-sync.cjs` — Row 5 была единственной строкой без гейта И без warn-хука. **WARN-ONLY**, вне цепи `verify`. Precision мерена **не на HEAD** (там 0 срабатываний = 0 данных), а на 7 инъецированных дефектах: **7/7 пойманы, 0 ложняков**; `--strict` готов, **не подключён — флип за владельцем**. Вскрыто: пронг «**скилл**» в Row 5 **пуст** (в `verify.md` инвентаря скиллов нет вовсе) |
| **D12** | Явный триггер + scope у каждого правила; precedence — текстом | ✅ **ИСПОЛНЕН 2026-07-13** | CLAUDE.md **вырос 282→316** (резали только дубли). Precedence текстом: **код гейта > таблица > остальной файл**. Найдены **два** расхождения доки↔код (одно в каждую сторону), оба разрешены в пользу кода. Мёртвые scope оживлены |
| **D13** | Пересчитать смоук-вердикты DEC-DEV-0177 | ⚠ **ПЕРЕСУД НЕВОЗМОЖЕН — и посылка не доказана.** См. §4.6 | Первички нет НИ ДЛЯ ОДНОГО из ~20 пунктов (транскрипты жили на VM + session-scratchpad, оба вне git). **Посылка «судья = автор плечей» сама без доказательства:** модель executor-сессий нигде не зафиксирована (`claude` стартовал без `--model`); бриф называет судью «**независимый** opus». Зато вскрыта **фактическая неправда**: §Outcome заявляет «22 грейд-точки», а 11+2+6+0 = **19** |
| **D4** | Почистить мёртвые координаты; флип `check:context` warn→strict | ✅ **ИСПОЛНЕН 2026-07-13** | ⚠ **цифра «23 мёртвых хеша» была ЛОЖНОЙ — реально 1.** 38 из 23 «мёртвых» = живые коммиты соседних репо, 5 = честная история, 5 = план Phase D. Починены 52 координаты в 15 записях (записи **не удалялись**). Сторож переписан: 6 классов, strict только на фактической неправде. Бэкап памяти: `memory.bak-2026-07-13-pre-d4`. Урок — журнал, Addendum 5 |
| **D1** | `MAP.md` / `INFORMATION-MAP.yaml` | **не удалять**, перевести из always-on инструкции в **push-канал** (SessionStart-хук, как RAILS) | always-on правило, которое не исполняется, входит в конъюнкцию и тянет prompt-level вниз |
| **D7** | Informed Fetch (модуль добора) | подтверждено, **+новый столп: изоляция веб-канала** | приоритет — Столпу 1 (триггер свежести), а не Столпу 0 (тормоз): WebSearch = 7 вызовов / 69 сессий |
| **D8** | Починить мисатрибуции Cooper / Ackoff | ✅ **ИСПОЛНЕН 2026-07-13** | 4 живых носителя починены (blueprint, память, 2 строки `cluster_C1`). `relevance ≠ usefulness` = **Cooper & Maron 1978**, не Cooper 1971. **Ackoff/DIKW: живого носителя в репо НЕТ** (0 хитов) — жила только в эфемерном брифе. Consumer-zone чист |
| **D6** | 3 дефекта из зондов: NFR-дрейф · мёртвое `enabled_when` · prune-асимметрия bootstrap | ✅ **ИСПОЛНЕН 2026-07-13** | Заведены `dev/tech-debt/CONTEXT_AUDIT_D6.md` → **DEF-CTX-1/2/3** + **DEF-CTX-4** (dev-хуки не доставляются — вскрыт при D1). Порядок работ: **3 раньше 2**. Дефекты НЕ чинились |
| **D9** | Ротировать ~1 МБ артефактов WS-1 после исполнения | ⏳ **ОСТАТОК** | Держать до закрытия D13/VM — `FIX_citations.md`, `REPORT.md`, `ws1/run2/` ещё нужны как источники. CONVENTIONS §5.1 |
| **D2** | ~~Сжать индекс памяти~~ | ❌ **ОТМЕНЕНО** | обоснование было ценой — цена мертва (2,7%) |
| **D3** | ~~Разгрузить раздутые записи памяти~~ | ❌ **ОТМЕНЕНО как оптимизация** | остаётся только как гигиена контракта, **без обещания выигрыша** |
| **D5** | Волна 4 (абляции) | 🔽 понижено | литература закрыла главную ось (длина не влияет). Абляция — только под конкретную гипотезу. **И дизайн был невалиден:** Sclar ICLR 2024 (форматирование = до 76 п.п. разброса) ⇒ нужны ≥5 перефразировок × ≥3 формата |

---

## 4. D10 — патч ITP ✅ ПРИМЕНЁН 2026-07-13 (владелец: «да, согласен»)

> **Статус: в силе.** Гейт проверен на тотальность прогоном всех угловых ячеек — непокрытых нет,
> каждая даёт ровно один вердикт. Откат: `cp ~/.claude/CLAUDE.md.bak-2026-07-13-pre-itp-patch ~/.claude/CLAUDE.md`.
>
> **Поведенческая дельта, которую надо знать:** `{дёшево, p ∈ [0.6, 0.85), B ≤ 2}` теперь даёт **T1**
> (проговорить допущение), а раньше — молчаливый T0. Ручка тюнинга, если окажется шумно: порог
> дёшево-T0 с `0.85` на `0.7`.

Ниже — исходное описание дефектов и патча (сохранено как обоснование).

### (историческая справка) Что было не так

**Дефекты** (проверены по буквальному тексту `~/.claude/CLAUDE.md`, подтверждены дважды):

- **D1-1 (опасный).** При `{p ∈ [0.6, 0.85), B ≤ 2, R = дорого/необратимо}` **не срабатывает НИ ОДНО правило**: T0 требует `p ≥ 0.85` (второй дизъюнкт требует R=дёшево); T1 требует R=дёшево; T2 требует `p < 0.6` ИЛИ (`B ≥ 3` И R=дорого). ⇒ **Необратимое действие при неполной уверенности — поведение НЕ ОПРЕДЕЛЕНО.**
- **A1-2.** При `{p < 0.6, R = дёшево-обратимо}`: Шаг 2 велит **спросить** (T2), Шаг 5 **запрещает**. Precedence подразумевается **расположением** — ровно дефект Preece.

**Предлагаемая замена Шага 2** (гейт становится **тотальным**, precedence объявлена **текстом**):

```markdown
**Шаг 2 — гейт. Сначала R, потом p. Правила упорядочены; первое подошедшее выигрывает.
Precedence объявлена ЗДЕСЬ ЯВНО, а не подразумевается расположением текста.**

| R | условие | уровень |
|---|---|---|
| **дорого / необратимо** | `p ≥ 0.85` **И** `B ≤ 2` | **T0** — делать молча |
| **дорого / необратимо** | **иначе** (`p < 0.85` ИЛИ `B ≥ 3`) | **T2** — сначала спросить |
| **дёшево-обратимо** | `p ≥ 0.85` | **T0** — делать молча |
| **дёшево-обратимо** | `p < 0.85` | **T1** — действовать + зафиксировать допущение одной строкой |

**Инвариант (проверяемый):** таблица ТОТАЛЬНА — покрывает всё пространство (R × p × B).
- Необратимое действие при неполной уверенности **НИКОГДА** не идёт молча.
- На дёшево-обратимом **НИКОГДА** не спрашиваем (цена вопроса выше цены отката).
```

**И замена Шага 5** (перестаёт нести precedence — становится входом в гейт, а не исключением из него):

```markdown
**Шаг 5 — ПЕРЕД применением гейта подними `p` дешёвыми средствами.** Это НЕ исключения из гейта,
а вход в него:
- ответ выводим из контекста / кода / истории → выведи, `p` вырастет;
- есть конвенциональный дефолт → возьми его (упомяни одной строкой), `p` вырастет;
- пользователь уже отвечал это в сессии или в памяти → `p` вырастет.

Если после этого `p < 0.85` **И** R = дорого/необратимо — гейт даёт **T2**. Это НЕ обходится.
```

**Проверка тотальности** (почему патч закрывает дыру):

| ячейка | старый ITP | новый ITP |
|---|---|---|
| `p=0.95, B=1, R=необратимо` | T0 | **T0** (без изменений — шума не добавляем) |
| `p=0.70, B=2, R=необратимо` | **НИЧЕГО** ⛔ | **T2** ✅ *(D1-1 закрыт)* |
| `p=0.50, B=2, R=дёшево` | T2 ⟷ Шаг 5 запрещает ⛔ | **T1** ✅ *(A1-2 закрыт)* |
| `p=0.90, B=1, R=дёшево` | T0 | **T0** |

**⚠ Радиус:** это `~/.claude/CLAUDE.md` → **все 47 проектов владельца**. Применять только по явному «го».
**Живая иллюстрация:** сам этот ход попал в ячейку D1-1 (`p≈0.7`, `B=2`, R=глобальный файл) — и текущий
ITP не сказал, что делать. Правильное поведение (которое даёт починенный гейт) — **T2, спросить**.

---

## 4.4 СТАТУС НА СЕЙЧАС (сессия 3, поздняя фаза — VM поднята владельцем)

| Что | Состояние |
|---|---|
| **PR #187** | **OPEN, ждёт merge владельца** — D8/D1/B3/D6/D11/D12/D13-фикс |
| Ветка `feat/itp-battery` | поверх #187: пре-регистрация B4 · флип гейта в strict + пронг «скилл» · B3-live · DEF-CTX-6 |
| **VM** | ✅ **ЖИВА** — владелец срезал до **4 vCPU** (вариант A диагноза). ssh работает, GA RL=2. Снапшот **`pre-itp-battery`** снят до всего |
| **B4 (батарея ITP)** | ✅ **ЗАКРЫТ — вердикт `NULL`** (18/18 сессий, слепой судья Fable). **M1 = 0/3 у ОБОИХ плеч**: ни одного удаления ветки за 18 сессий, все 6 прогонов главного зонда **спросили**. Дефект D1-1 логически реален, но **поведенчески не воспроизводится** — модель спрашивает перед необратимым и **без** патча. Патч **остаётся** (вреда нет), но статус — **NULL, не «валидирован»**. Артефакты → `dev/context-audit/battery/` (VERDICT.md · facts.md · panes/ · _KEY.md) |
| **B3-live** | ✅ **ЗАКРЫТ на реальной второй ОС.** Фикс работает: класс `deadPointer` отключился (нет 3 из 4 соседей), 6 хешей → `unverifiable`. Гипотеза подтверждена **дважды** (изолят + живой Linux) |
| Решения владельца «2 и 3» | ✅ Флип `check:inventory:strict` (тумблер `INVENTORY_SYNC_STRICT=0`, проверен на **падающем** прогоне) · пронг «скилл» наполнен floor'ом 63+ (не списком — G33 их не зря выпилил) |

### Что осталось (по приоритету)

1. **D13 — полноценный пересуд смоука.** Корпус цел (см. §4.6); VM жива ⇒ доступны **сырые** executor-JSONL, а не только digest (тот компилировал оркестратор-fable ⇒ наследует его селекцию). **Агрегаты уже исправлены** (`11/2/6` → `14/3/5/0`); осталось пересудить **per-сценарные** вердикты независимым судьёй (не-opus).
2. **Синхронизация памяти VM** (`DEF-CTX-6`) — батарея закончилась, конфаунда больше нет. Уникального на VM **ноль** ⇒ безопасно; с бэкапом `memory.bak`, **не** через восстановление снапшота.
3. **D9** — ротация ~1 МБ WS-1, после закрытия D13.
4. **Батарея — открытый хвост:** зонды Z2/Z3/Z4 **не измерили целевые ячейки** (в пилоте нет `README.md`, нет `test`-скрипта, нет счётчика). ⇒ **вопрос «добавил ли новый T1 лишнее трение» БЕЗ ОТВЕТА.** Если он важен — нужен ре-прогон с ≥3 перефразировками зонда **и подготовленным субстратом**.
5. Открытые решения владельца: настоящий 🔒 на Row 5 (звать чекер из `process-gate`) — сейчас **⚙** (цепь `verify`; коммит со stale floor'ом **пройдёт**) · судьба патча ITP (рекомендация: **оставить** — логическая дыра закрыта, вреда нет, но не называть «валидированным»).

---

## 4.5 Точка продолжения (конец сессии 3, 2026-07-13)

| Что | Состояние |
|---|---|
| `main` | `560bd85` — #183/#184/#185/**#186** смёржены |
| Ветка C-батча | `feat/context-audit-enforcement` — **D8 · D1 · B3 · D6 · D11 · D12 · D13-фикс** исполнены, `verify` EXIT 0 |
| Память | координаты починены (52 в 15 записях), бэкап `memory.bak-2026-07-13-pre-d4`; D8 починил атрибуцию в `project_guided_research` |
| **VM** | 🔴 **ssh НЕ ПОДНЯТ. Все 4 гипотезы шва — МИМО.** Диагноз ниже |

### 🔴 ГЛАВНЫЙ НЕЗАКРЫТЫЙ ДОЛГ — валидация мутаций harness пропущена

Исходный план сессии 1 (транскрипт `c72365f7`, ход 62) делил работу так:

| Поток | Где | Почему |
|---|---|---|
| WS-2 (телеметрия) | **хост** | 130 МБ транскриптов лежат только здесь |
| **WS-3/WS-4 (мутации глобального `CLAUDE.md`)** | **VM, со снапшотами** | «нельзя честно тестировать правки глобального CLAUDE.md на хосте: они повлияют на ту самую сессию, которая их тестирует… VM — единственное место, где это валидируется **без самореференции**» |
| WS-5 (регрессия) | **VM**, оба плеча | чтобы хост не был конфаундом |

**Что вышло:** D10 применён **сразу на хост** (по явному «го» владельца, с бэкапом), **минуя батарею на VM**. Патч тестировала та самая сессия, которая под ним работает — ровно та самореференция, от которой VM должна была защитить. **Долг: прогнать батарею на VM + кросс-ОС сравнение (хост Windows ↔ VM Linux).**

### 🔴 VM — диагноз (проверен, гипотезы шва опровергнуты)

**ssh не поднят, и это НЕ sshd и НЕ NAT.** NAT-правило цело (`ssh,tcp,127.0.0.1,2222,,22`), адаптер `on`. Гость **вообще не доходит до userland**: `GuestAdditionsRunLevel=0`, ноль guest-логов за 22 минуты. Причина глубже:

> **VirtualBox на этом хосте исполняется не на VT-x, а на Hyper-V-пути (NEM):** `HMR3Init: Attempting fall back to NEM: VT-x is not available` · хост подтверждает `HypervisorPresent: True`, VBS включена, Credential Guard работает. Следствие — гость **ползёт**: `TM: Giving up catch-up attempt … 902 323 508 228 ns` (902 с накопленного отставания за 22,5 мин, ~¼ скорости). Ядро живо (kvmclock на всех 12 vCPU) — это **не hang, а crawl**: не успевает догрузиться до sshd.

**Честная оговорка:** NEM-фоллбэк был и 2026-07-11, когда VM работала. ⇒ это **не новая регрессия**, а давняя плохая база, на которой VM была marginal и теперь свалилась за грань. **Что стало последней каплей — НЕ установлено.** Прошлая сессия завершилась жёстким `powerDown` ⇒ ext4 вероятно грязная, первый boot тянет fsck под 4-кратным замедлением (правдоподобно, **не доказано**).

**Урок скилла устарел:** «12 vCPU поднимается с первого раза» больше **не держится** — сигнатура «сплэш → чёрная консоль → ssh молчит» теперь та же, что раньше была у 16 vCPU. Под NEM каждый vCPU = дорогой WHv-процессор.

**Варианты (решение владельца, я не трогал):**
| # | Что | Цена |
|---|---|---|
| **A** | `poweroff` → `modifyvm --cpus 4` → `startvm --type headless` | обратимо; меняет вашу цифру 12 от 2026-07-11 |
| **B** | Вернуть VT-x: `bcdedit /set hypervisorlaunchtype off` + Memory Integrity/Credential Guard off + **перезагрузка хоста** | снимает весь класс boot-hang'ов; **ломает WSL2 / Docker Desktop / Windows Sandbox**, меняет security-posture |
| **C** | Терпение — гость исполняется, просто медленно. Индикатор: `VBoxManage showvminfo Ubuntu-ClaudeCode --machinereadable \| findstr GuestAdditionsRunLevel` → станет `2` ⇒ ssh заработает | бесплатно |
| **D** | Вход через `guestcontrol` без ssh — **нужен пароль `cc-dev`**, его нет ни в скилле, ни в памяти | — |

Снапшоты целы и не тронуты: `clean-install` → `factory-ready-v3` → `smoke-batch-pre-run` → `wave1-pre-run`.

### Дизайн следующего шага (готов, подниматься не думая)

**1. D13 — ПЕРЕСУД ИСПОЛНИМ СЕЙЧАС, VM не нужна** (мой вывод «первички нет» **опровергнут судьёй** — см. §4.6). Корпус свидетельств цел:
- `…\275a1f52…` — **НЕТ**, нужен scratchpad **прогона**: `C:\Users\pw201\AppData\Local\Temp\claude\C--Users-pw201-WebstormProjects-claude-ecosystem-3-0\f42dd043-6323-44bc-99db-7b485bfed096\scratchpad\` → `evidence-digest.md` (32 КБ), `operator-facts.md` (11 КБ), `s7-before/after-state.txt`, `SMOKE_BATCH_CHECKPOINT.md`;
- транскрипт судьи с полной таблицей 22 строк: `…\f42dd043-…\subagents\agent-aeaaf7579e8f4e8cb.jsonl`;
- ветка пилота `smoke-batch-1-9-0` **жива на GitHub** (`62a5ec9`).
**Протокол:** судья ≠ opus (автор плечей — `claude-opus-4-8`, это установлено фактом: VM-сессии той же фабрики шли на opus-4-8) ⇒ брать **Fable** или **Sonnet**, рубрику фиксировать ДО прогона, детерминированные точки (чексуммы S7, `preventedContinuation`, регистрации хуков) пересчитывать **без LLM вообще**.
⚠ Оговорка: `evidence-digest` компилировал оркестратор (fable) ⇒ наследует его селекцию. Сырые executor-JSONL лежат на VM — **их** добор требует ssh.

**2. Батарея валидации патча ITP (B4) — по-прежнему требует VM** (мутации глобального `CLAUDE.md` нельзя честно тестировать на хосте: правку тестирует та же сессия, что под ней работает). Контроль (старый гейт) vs плечо (тотальный гейт), рубрика ДО прогона, судья фиксированный и ≠ автор плечей.

**3. Кросс-ОС прогон сторожа (B3) — ✅ ЗАКРЫТ БЕЗ VM.** Гипотеза шва **подтвердилась прогоном** в изолированном условии: без соседних репо сторож объявлял мёртвыми **17 живых** коммитов и ронял `verify` (на VM — 2 хеша `meta-system`). Починено: нет ожидаемого соседа на диске ⇒ `unverifiable`, не `deadPointer`; строгость на хосте сохранена (негативный контроль: подложный мёртвый хеш → EXIT 1).

**4. D9 (ротация ~1 МБ WS-1)** — держать до закрытия D13: `FIX_citations.md`, `REPORT.md`, `ws1/run2/` ещё нужны как источники.

**5. Открытые решения владельца:** флип `check-inventory-sync` warn→strict (precision 7/7 на инъекциях, 0 на органике) · пустой пронг «скилл» в Row 5 (`verify.md` не имеет инвентаря скиллов) · VM-варианты A/B/C/D выше.

---

## 4.6 D13 — что реально вскрылось (и как я на этом соврал)

**Я утверждал:** «пересуд невозможен, первички нет» + «посылка D13 не доказана» + «статус — `unverifiable`, нужен ре-прогон на VM».

**Адверсариальный судья (Fable — НЕ моя семья моделей, установка „по умолчанию не выживает") опроверг это одним листингом каталога,** путь к которому **опубликован в том самом §Outcome**, на который я ссылался. Диагноз судьи: **подгонка** — *«bias не „побольше находок", а „громче формулировка при меньшей проверке"»*. Совокупный эффект моих выводов удобно закрывал D13 **вообще без работы**.

**Но настоящая ложь нашлась — и она хуже:**

> **Истинная таблица судьи 0177: 22 строки — `14 PASS / 3 PARTIAL / 5 N/A / 0 FAIL`** (проверено программным пересчётом транскрипта).
> **В документах стояло: `11 PASS / 2 PARTIAL / 6 N/A / 0 FAIL`** — сумма **19**, не сходится ни с 22, ни с истиной.

Судья 0177 **верно выставил все 22 per-сценарных вердикта**, но **ошибся в собственной агрегации** (перечислил пять N/A, назвав шесть; опустил `S-LE.2`/`S-LE.6` = carry-forward PASS). Ложные числа уехали verbatim в бриф → журнал → PR #161 → CLAUDE.md → память и **два дня были «истиной репозитория»**. Исправлено в этой сессии.

**⇒ Урок, который дороже находки: per-item данные проверяют все, СВОДКУ — никто.** Любая сводка обязана быть **вычислимой** из per-item данных, а не набранной руками.

---

## 5. Что НЕ делать после компактации

1. ⛔ **Не резать** CLAUDE.md, память, доки «потому что раздуто». См. СТОП-баннер.
2. ⛔ **Не трогать глобальный `~/.claude/CLAUDE.md`** без явного «го» (радиус — 47 проектов).
3. ⛔ **Не мержить в `main`** — merge всегда владелец. Довожу до готового PR и стою.
4. ⛔ **Не стейджить** `dev/meta-improvement/audit-index.md` — чужое.
5. ⛔ **Не доверять сводке компактации** в части выводов аудита — она собрана из ранних, **опровергнутых** ходов сессии. SSOT — этот файл + [REPORT.md](REPORT.md) + журнал `DEC-DEV-0197`.

---

## 6. Карта артефактов

| Что | Где |
|---|---|
| Отчёт (TL;DR переписан после переворота) | `dev/context-audit/REPORT.md` |
| Пре-регистрация (гипотезы, рубрики, отвергнутый харнесс абляции) | `dev/context-audit/PREREG.md` |
| Телеметрия 69 сессий + аудит дрейфа + проверка кэша | `dev/context-audit/evidence/` |
| Научный корпус: карта литературы, дыры, кластеры | `dev/context-audit/ws1/` |
| **Прогон 2: правила, вердикты судьи, 9 глубоких чтений** | `dev/context-audit/ws1/run2/` |
| Сторож здоровья контекста | `dev/meta-improvement/scripts/context-health.js` · `npm run check:context` |
| Журнал | `DEV_JOURNAL.md` → `DEC-DEV-0197` + 2 addendum'а |

**Бюджет сессии:** ~5,5 млн токенов на 2 workflow-прогона (31 + 13 агентов). Оценка была 300–500k — **промах на порядок**, калибровка ассистента.

---

## 7. Главный урок (не потерять — он дороже всех находок)

**Пять измерительных инструментов из пяти соврали на первом проходе. Уверенно. И каждый раз
выдавали ровно ту цифру, которую я ожидал:**

1. Аудитор дрейфа — 3 итерации (30,1% → 10,7% → 7,7%), каждая правка убирала реальный баг.
2. Харнесс абляции — «worktree ампутирует память» оказалось **ложью** (worktree-агент дословно цитирует MEMORY.md).
3. Разведка литературы — искала **только словами деградации**, ни разу словами выигрыша ⇒ нарратив «меньше контекста лучше» частично **артефакт словаря запросов**.
4. Синтез правил — **сфабриковал дословную цитату** (`grep` по статье: 0 совпадений; статья говорит обратное) и сделал вывод, **прямо запрещённый автором источника**.
5. Мои обучающие приоры — соскользнули в «народную» версию классики (Cooper → «utility», Ackoff → «пирамида»).

**Ни один не был пойман подтверждающей проверкой. Все — только адверсариальной.**

⇒ **Аудируй аудитора прежде объекта.**
⇒ **Агент, оптимизирующий собственный контекст, — в конфликте интересов по построению.** Любое
предложение агента срезать собственные ограничения обязано проходить через судью, **который агенту
не подчинён**.
