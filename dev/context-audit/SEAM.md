# SEAM — шов сессии контекст-аудита (DEC-DEV-0197)

> **Что это.** Контракт продолжения. Пишется ДО компактации контекста, чтобы следующий «я»
> (после компактации или в новой сессии) поднял работу с подтверждённого шва, а не с пересказа.
> Место выбрано **в репо**, не в scratchpad — scratchpad session-specific (OQ-CS-03, DEC-DEV-0196).
>
> Состояние на: **2026-07-13**, ветка `feat/meta-context-audit`, PR **#184** (OPEN, MERGEABLE, CLEAN),
> `npm run verify` = **EXIT 0**.

---

## 🛑 СТОП-БАННЕР — прочти ПЕРВЫМ, до любого действия

**Сводка компактации почти наверняка схлопнет этот аудит в «нашли, что контекст раздут».
Это НЕВЕРНО и приведёт к вредному действию. Настоящий вывод — противоположный:**

> ## РЕЗАТЬ КОНТЕКСТ ОСНОВАНИЙ НЕТ.

Доказательства (полностью в [REPORT.md](REPORT.md), первоисточники — в [ws1/run2/](ws1/run2/)):

| Факт | Источник |
|---|---|
| **Файл поднимает соблюдение правила с 0% до 60–68%** (0 комплаенса из 524 функций **без** конфиг-файла) | McMillan, BL-01 — крупнейший эффект во всём корпусе |
| **Размер файла не делает ничего:** ICR 60,0 / 65,2 / 67,7 / 64,0% при 25 / 100 / 250 / 500 строк, тренд p=0,625, аффирмативный нуль. Набивка — **реалистичным CLAUDE.md-контентом** | McMillan |
| «**no clear dependency** between the success rate or the per-instance cost and the context file length» | Gloaguen, §«The length of context files does not influence our findings» |
| **Кэш хитает 99,6% ходов** (16 912 / 16 987), 96,5% префикса из кэша. Рез резидента вдвое = **2,7% экономии** (~$241 из ~$9 000 за 69 сессий). На подписке — **$0** | наша телеметрия, `evidence/_cache-check.js` |

**⇒ Экономический аргумент за рез — МЁРТВ. Аргумент качества — ОТСУТСТВУЕТ.**

**Если после компактации возникает импульс «сократить CLAUDE.md / память / доки» — это НЕ вывод
аудита. Это градиент агента, оптимизирующего собственные ограничения. Адверсариальный судья
поймал ровно его: мой синтез номинировал в рез «бери СТАРШИЙ тир», «worst-of» и
«явное „просто сделай“ подавляет ITP» — то есть РЫЧАГ УПРАВЛЕНИЯ ВЛАДЕЛЬЦА НАДО МНОЙ.**

---

## 1. Что литература поддерживает ВМЕСТО реза

**Инженерию принуждения, а не редактуру файла.**

1. **Арифметика конъюнкции.** ManyIFEval (Findings EMNLP 2025): у Claude соблюдение *каждого* правила держится **0,95 → 0,93** при n=1→10, а соблюдение *всех сразу* падает **0,95 → 0,48**. Модель чистой независимости предсказывает это с **MAE 0,02, r=0,994** ⇒ «обвал» — **произведение вероятностей**, не поломка модели. Проза даёт 60–68% комплаенса (ContextCov: 67,0%); детерминированный гейт — 100%.
   ⇒ **Единица учёта — число ALWAYS-ON правил, а НЕ длина файла.** Условно-триггерные правила почти бесплатны.
   ⇒ **🔒-гейты — механизм, а не бюрократия. Расширять.**
2. **Явный триггер + scope В ТЕКСТЕ у каждого правила.** Вендорский гайд про Opus 4.8 дословно: *«does not silently generalize an instruction from one item to another… state the scope explicitly»*. Control Illusion (AAAI-2026): placement (system vs user) почти не важен против **маркировки ограничения**; Claude-3.5-Sonnet следует назначенно-приоритетной инструкции лишь **20,3%**. Preece 1994: *«knowledge … "hidden" in the ordering of the rules»*.
   ⇒ **Полагаться на порядок в файле, на тон (IMPORTANT / MUST / 🔒) или на «оно же специфичнее» — ЗАПРЕЩЕНО.**
3. **Ни байта веб-происхождения в авто-загружаемый канал без гейта.** У LLM-судьи нет инструментов ⇒ ни одна работающая защита его не прикрывает, а его суждение и есть consequential action. Запись в CLAUDE.md/память **персистентна**. (AgentDojo — NeurIPS 2024 D&B; InjecAgent — Findings ACL 2024; Greshake — AISec@CCS 2023.)
4. **Детерминированные чекеры вместо LLM-судьи там, где правило машинно-проверяемо.**

---

## 2. Фактическое состояние (верифицировано, не по памяти)

| Что | Значение |
|---|---|
| Ветка | `feat/meta-context-audit`, синхронна с `origin` |
| PR | **#184** — OPEN, MERGEABLE, CLEAN. **Merge — за владельцем.** |
| Коммиты ветки | `fdc0c8f` (аудит + сторож) · `c2fbf6b` (WS-1 Прогон 1) · `1bdf768` (Прогон 2 + судья) |
| `npm run verify` | **EXIT 0** (2 skip — puppeteer-core не установлен, норма) |
| `npm run check:context` | 60,2 КБ резидента · 23 мёртвых указателя · 50 битых ссылок · 13 нарушений контракта памяти · 68 непроверяемых. **warn-only** |
| Некоммиченное | `M dev/meta-improvement/audit-index.md` — **НЕ моё** (хук/соседняя сессия). **НЕ стейджить.** |

### ⚠ ГРАБЛЯ: PR #184 содержит чужой коммит

В ветке **4** коммита, а не 3: первым идёт **`db22ded` (DEC-DEV-0196, это работа PR #183)**.
Причина — соседняя сессия продвинула локальный `main` **до** того, как я от него ответвился
([[env_parallel_sessions_share_checkout]]).

**⇒ ПОРЯДОК MERGE: сначала #183, потом #184.** Тогда дифф #184 схлопнется до своих трёх коммитов.
Обе ветки нацелены на `main` (не стек), так что `--delete-branch` на #183 **не закроет** #184.
История **не переписывалась** сознательно: rebase дал бы конфликт в `DEV_JOURNAL` (append-контракт),
а форс-пуш ветки, которую владелец может смотреть, дороже косметики.

---

## 3. Решения — ПОДТВЕРЖДЕНЫ ВЛАДЕЛЬЦЕМ 2026-07-13

| # | Решение | Статус | Готовность |
|---|---|---|---|
| **D10** | **Починить 3 логических дефекта ITP** (D1-1 — критичный) | ✅ **ПРИМЕНЁН 2026-07-13** | гейт → тотальная таблица · precedence текстом · `R` заострён. Бэкап: `~/.claude/CLAUDE.md.bak-2026-07-13-pre-itp-patch`. Детали — §4 |
| **D11** | Расширять 🔒-гейты вместо прозы | подтверждено | требует дизайна: какие правила прозы механизируемы |
| **D12** | Явный триггер + scope у каждого правила; precedence — текстом | подтверждено | применить к CLAUDE.md проекта (репо-зона, «го» уже есть) |
| **D13** | Пересчитать смоук-вердикты DEC-DEV-0177 | подтверждено | судья был `opus` = автор плечей ⇒ self-preference bias |
| **D4** | Почистить 23 мёртвых хеша + 50 битых ссылок; флип `check:context` warn→strict | подтверждено | список — `npm run check:context` |
| **D1** | `MAP.md` / `INFORMATION-MAP.yaml` | **не удалять**, перевести из always-on инструкции в **push-канал** (SessionStart-хук, как RAILS) | always-on правило, которое не исполняется, входит в конъюнкцию и тянет prompt-level вниз |
| **D7** | Informed Fetch (модуль добора) | подтверждено, **+новый столп: изоляция веб-канала** | приоритет — Столпу 1 (триггер свежести), а не Столпу 0 (тормоз): WebSearch = 7 вызовов / 69 сессий |
| **D8** | Починить мисатрибуции Cooper / Ackoff | подтверждено | верификация — `ws1/run2/FIX_citations.md`. Цель: `dev/RESEARCH_CAPABILITY_BLUEPRINT.md:54` + память `project_guided_research` |
| **D6** | 3 дефекта из зондов: NFR-дрейф · мёртвое `enabled_when` · prune-асимметрия bootstrap | подтверждено | в бэклог отдельными задачами |
| **D9** | Ротировать ~1 МБ артефактов WS-1 после исполнения | подтверждено | CONVENTIONS §5.1 |
| **D2** | ~~Сжать индекс памяти~~ | ❌ **ОТМЕНЕНО** | обоснование было ценой — цена мертва (2,7%) |
| **D3** | ~~Разгрузить раздутые записи памяти~~ | ❌ **ОТМЕНЕНО как оптимизация** | остаётся только как гигиена контракта, **без обещания выигрыша** |
| **D5** | Волна 4 (абляции) | 🔽 понижено | литература закрыла главную ось (длина не влияет). Абляция — только под конкретную гипотезу. **И дизайн был невалиден:** Sclar ICLR 2024 (форматирование = до 76 п.п. разброса) ⇒ нужны ≥5 перефразировок × ≥3 формата |

---

## 4. D10 — патч ITP ✅ ПРИМЕНЁН 2026-07-13 (владелец: «да, согласен»)

> **Статус: в силе.** Гейт проверен на тотальность прогоном всех угловых ячеек — непокрытых нет,
> каждая даёт ровно один вердикт. Откат: `cp ~/.claude/CLAUDE.md.bak-2026-07-13-pre-itp-patch ~/.claude/CLAUDE.md`.
>
> **Поведенческая дельта, которую надо знать:** `{дёшево, p ∈ [0.6, 0.85), B ≤ 2}` теперь даёт **T1**
> (проговорить допущение), а раньше — молчаливый T0. Ручка тюнинга, если окажется шумно: порог
> дёшево-T0 с `0.85` на `0.7`.

Ниже — исходное описание дефектов и патча (сохранено как обоснование).

### (историческая справка) Что было не так

**Дефекты** (проверены по буквальному тексту `~/.claude/CLAUDE.md`, подтверждены дважды):

- **D1-1 (опасный).** При `{p ∈ [0.6, 0.85), B ≤ 2, R = дорого/необратимо}` **не срабатывает НИ ОДНО правило**: T0 требует `p ≥ 0.85` (второй дизъюнкт требует R=дёшево); T1 требует R=дёшево; T2 требует `p < 0.6` ИЛИ (`B ≥ 3` И R=дорого). ⇒ **Необратимое действие при неполной уверенности — поведение НЕ ОПРЕДЕЛЕНО.**
- **A1-2.** При `{p < 0.6, R = дёшево-обратимо}`: Шаг 2 велит **спросить** (T2), Шаг 5 **запрещает**. Precedence подразумевается **расположением** — ровно дефект Preece.

**Предлагаемая замена Шага 2** (гейт становится **тотальным**, precedence объявлена **текстом**):

```markdown
**Шаг 2 — гейт. Сначала R, потом p. Правила упорядочены; первое подошедшее выигрывает.
Precedence объявлена ЗДЕСЬ ЯВНО, а не подразумевается расположением текста.**

| R | условие | уровень |
|---|---|---|
| **дорого / необратимо** | `p ≥ 0.85` **И** `B ≤ 2` | **T0** — делать молча |
| **дорого / необратимо** | **иначе** (`p < 0.85` ИЛИ `B ≥ 3`) | **T2** — сначала спросить |
| **дёшево-обратимо** | `p ≥ 0.85` | **T0** — делать молча |
| **дёшево-обратимо** | `p < 0.85` | **T1** — действовать + зафиксировать допущение одной строкой |

**Инвариант (проверяемый):** таблица ТОТАЛЬНА — покрывает всё пространство (R × p × B).
- Необратимое действие при неполной уверенности **НИКОГДА** не идёт молча.
- На дёшево-обратимом **НИКОГДА** не спрашиваем (цена вопроса выше цены отката).
```

**И замена Шага 5** (перестаёт нести precedence — становится входом в гейт, а не исключением из него):

```markdown
**Шаг 5 — ПЕРЕД применением гейта подними `p` дешёвыми средствами.** Это НЕ исключения из гейта,
а вход в него:
- ответ выводим из контекста / кода / истории → выведи, `p` вырастет;
- есть конвенциональный дефолт → возьми его (упомяни одной строкой), `p` вырастет;
- пользователь уже отвечал это в сессии или в памяти → `p` вырастет.

Если после этого `p < 0.85` **И** R = дорого/необратимо — гейт даёт **T2**. Это НЕ обходится.
```

**Проверка тотальности** (почему патч закрывает дыру):

| ячейка | старый ITP | новый ITP |
|---|---|---|
| `p=0.95, B=1, R=необратимо` | T0 | **T0** (без изменений — шума не добавляем) |
| `p=0.70, B=2, R=необратимо` | **НИЧЕГО** ⛔ | **T2** ✅ *(D1-1 закрыт)* |
| `p=0.50, B=2, R=дёшево` | T2 ⟷ Шаг 5 запрещает ⛔ | **T1** ✅ *(A1-2 закрыт)* |
| `p=0.90, B=1, R=дёшево` | T0 | **T0** |

**⚠ Радиус:** это `~/.claude/CLAUDE.md` → **все 47 проектов владельца**. Применять только по явному «го».
**Живая иллюстрация:** сам этот ход попал в ячейку D1-1 (`p≈0.7`, `B=2`, R=глобальный файл) — и текущий
ITP не сказал, что делать. Правильное поведение (которое даёт починенный гейт) — **T2, спросить**.

---

## 5. Что НЕ делать после компактации

1. ⛔ **Не резать** CLAUDE.md, память, доки «потому что раздуто». См. СТОП-баннер.
2. ⛔ **Не трогать глобальный `~/.claude/CLAUDE.md`** без явного «го» (радиус — 47 проектов).
3. ⛔ **Не мержить в `main`** — merge всегда владелец. Довожу до готового PR и стою.
4. ⛔ **Не стейджить** `dev/meta-improvement/audit-index.md` — чужое.
5. ⛔ **Не доверять сводке компактации** в части выводов аудита — она собрана из ранних, **опровергнутых** ходов сессии. SSOT — этот файл + [REPORT.md](REPORT.md) + журнал `DEC-DEV-0197`.

---

## 6. Карта артефактов

| Что | Где |
|---|---|
| Отчёт (TL;DR переписан после переворота) | `dev/context-audit/REPORT.md` |
| Пре-регистрация (гипотезы, рубрики, отвергнутый харнесс абляции) | `dev/context-audit/PREREG.md` |
| Телеметрия 69 сессий + аудит дрейфа + проверка кэша | `dev/context-audit/evidence/` |
| Научный корпус: карта литературы, дыры, кластеры | `dev/context-audit/ws1/` |
| **Прогон 2: правила, вердикты судьи, 9 глубоких чтений** | `dev/context-audit/ws1/run2/` |
| Сторож здоровья контекста | `dev/meta-improvement/scripts/context-health.js` · `npm run check:context` |
| Журнал | `DEV_JOURNAL.md` → `DEC-DEV-0197` + 2 addendum'а |

**Бюджет сессии:** ~5,5 млн токенов на 2 workflow-прогона (31 + 13 агентов). Оценка была 300–500k — **промах на порядок**, калибровка ассистента.

---

## 7. Главный урок (не потерять — он дороже всех находок)

**Пять измерительных инструментов из пяти соврали на первом проходе. Уверенно. И каждый раз
выдавали ровно ту цифру, которую я ожидал:**

1. Аудитор дрейфа — 3 итерации (30,1% → 10,7% → 7,7%), каждая правка убирала реальный баг.
2. Харнесс абляции — «worktree ампутирует память» оказалось **ложью** (worktree-агент дословно цитирует MEMORY.md).
3. Разведка литературы — искала **только словами деградации**, ни разу словами выигрыша ⇒ нарратив «меньше контекста лучше» частично **артефакт словаря запросов**.
4. Синтез правил — **сфабриковал дословную цитату** (`grep` по статье: 0 совпадений; статья говорит обратное) и сделал вывод, **прямо запрещённый автором источника**.
5. Мои обучающие приоры — соскользнули в «народную» версию классики (Cooper → «utility», Ackoff → «пирамида»).

**Ни один не был пойман подтверждающей проверкой. Все — только адверсариальной.**

⇒ **Аудируй аудитора прежде объекта.**
⇒ **Агент, оптимизирующий собственный контекст, — в конфликте интересов по построению.** Любое
предложение агента срезать собственные ограничения обязано проходить через судью, **который агенту
не подчинён**.
